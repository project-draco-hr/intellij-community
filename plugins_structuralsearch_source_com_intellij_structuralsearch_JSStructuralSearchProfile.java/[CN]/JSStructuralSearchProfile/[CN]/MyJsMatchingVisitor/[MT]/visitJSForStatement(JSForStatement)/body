{
  JSForStatement for2=(JSForStatement)myGlobalVisitor.getElement();
  myGlobalVisitor.setResult(myGlobalVisitor.match(for1.getVarDeclaration(),for2.getVarDeclaration()) && myGlobalVisitor.match(for1.getInitialization(),for2.getInitialization()) && myGlobalVisitor.match(for1.getCondition(),for2.getCondition())&& myGlobalVisitor.match(for1.getUpdate(),for2.getUpdate())&& myGlobalVisitor.matchOptionally(for1.getBody(),for2.getBody()));
}
