{
  JSDoWhileStatement while2=(JSDoWhileStatement)myGlobalVisitor.getElement();
  myGlobalVisitor.setResult(myGlobalVisitor.match(while1.getCondition(),while2.getCondition()) && myGlobalVisitor.matchOptionally(while1.getBody(),while2.getBody()));
}
