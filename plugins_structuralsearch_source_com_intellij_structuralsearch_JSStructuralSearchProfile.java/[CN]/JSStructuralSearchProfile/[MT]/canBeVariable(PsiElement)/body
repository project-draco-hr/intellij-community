{
  if (element instanceof JSExpression || element instanceof JSParameter || element instanceof JSVariable|| (element instanceof LeafElement && ((LeafElement)element).getElementType() == JSTokenTypes.IDENTIFIER)) {
    return true;
  }
  return false;
}
