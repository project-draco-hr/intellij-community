{
  return new JBIterable.StatefulFilter<E>(){
    boolean b;
    @Override public boolean value(    E e){
      if (b)       return false;
      b=Comparing.equal(e,o);
      return true;
    }
  }
;
}
