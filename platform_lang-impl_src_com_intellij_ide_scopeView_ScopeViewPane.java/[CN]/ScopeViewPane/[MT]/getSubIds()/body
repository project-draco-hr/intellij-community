{
  return ContainerUtil.map2Array(getShownScopes(),String.class,new Function<NamedScope,String>(){
    @Override public String fun(    NamedScope scope){
      return scope.getName();
    }
  }
);
}
