{
  setStringToReplace("$qualifier$::" + myMethodName);
  if (methodReference.getParent() instanceof PsiExpressionList && methodReference.getParent().getParent() instanceof PsiMethodCallExpression && isPredicates((PsiMethodCallExpression)methodReference.getParent().getParent())) {
    setReplaceByString("$qualifier$::" + myTargetMethodName);
  }
  setReplaceByString("$qualifier$");
}
