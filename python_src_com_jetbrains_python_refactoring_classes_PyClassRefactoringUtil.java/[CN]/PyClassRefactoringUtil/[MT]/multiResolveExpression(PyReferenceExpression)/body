{
  return ContainerUtil.mapNotNull(expr.getReference().multiResolve(false),new Function<ResolveResult,PsiElement>(){
    @Override public PsiElement fun(    ResolveResult result){
      return result.getElement();
    }
  }
);
}
