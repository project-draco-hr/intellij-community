{
  if (myForce) {
    int answer=Messages.showOkCancelDialog(myProject,getConfirmationMessage(),"Force Push","&Force Push",CommonBundle.getCancelButtonText(),Messages.getWarningIcon());
    if (answer != OK)     return;
  }
  myController.push(myForce);
  close(OK_EXIT_CODE);
}
