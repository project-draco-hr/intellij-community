{
  int answer=Messages.showOkCancelDialog(myProject,"You're going to force push. It will overwrite commits at the remote. Are you sure you want to proceed?","Force Push","&Force Push",CommonBundle.getCancelButtonText(),Messages.getWarningIcon());
  if (answer == OK) {
    myController.push(true);
    close(OK_EXIT_CODE);
  }
}
