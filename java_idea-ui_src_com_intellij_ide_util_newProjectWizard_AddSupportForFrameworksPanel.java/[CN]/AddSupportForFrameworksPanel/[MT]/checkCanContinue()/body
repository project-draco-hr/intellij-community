{
  if (!downloadLibraries()) {
    int answer=Messages.showYesNoDialog(getMainPanel(),ProjectBundle.message("warning.message.some.required.libraries.wasn.t.downloaded"),CommonBundle.getWarningTitle(),Messages.getWarningIcon());
    if (answer != Messages.YES) {
      return false;
    }
  }
  return true;
}
