{
  if (start < 0 || end > source.length || end < start) {
    LOG.error("Invalid block range: [" + start + ", "+ end+ "); length - "+ source.length);
  }
  mySource=source;
  myStart=start;
  myEnd=end;
}
