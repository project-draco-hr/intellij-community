{
  SVNURL repositoryUrl=myExecutor.getCommand().getRepositoryUrl();
  if (needAskAuthentication(isUserName)) {
    authentication=(SVNPasswordAuthentication)myRuntime.getAuthenticationService().requestCredentials(repositoryUrl,ISVNAuthenticationManager.PASSWORD);
  }
  return sendData(isUserName);
}
