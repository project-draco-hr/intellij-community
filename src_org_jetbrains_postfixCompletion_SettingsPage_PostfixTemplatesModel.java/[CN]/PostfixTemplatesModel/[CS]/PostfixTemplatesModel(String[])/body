{
  PostfixTemplatesService templatesService=ServiceManager.getService(PostfixTemplatesService.class);
  myTemplates=templatesService != null ? templatesService.getAllTemplates() : ContainerUtil.<TemplateProviderInfo>newArrayList();
  myColumns=columns;
}
