{
  List<String> res=new ArrayList<String>();
  for (int i=0; i != myTableModel.getRowCount(); ++i) {
    if ((Boolean)myTableModel.getValueAt(i,0) == true)     res.add((String)myTableModel.getValueAt(i,1));
  }
  return res;
}
