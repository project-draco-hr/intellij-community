{
  int fullname=in.names.readQualifiedName(in);
  int flags=in.readByte();
  return mkImport(fullname,BitUtil.isSet(flags,IS_STATIC),BitUtil.isSet(flags,IS_ON_DEMAND),BitUtil.isSet(flags,HAS_ALIAS) ? in.readInt() : 0);
}
