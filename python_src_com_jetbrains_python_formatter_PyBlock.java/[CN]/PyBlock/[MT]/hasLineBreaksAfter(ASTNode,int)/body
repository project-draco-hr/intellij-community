{
  final ASTNode treeNext=child.getTreeNext();
  return (treeNext != null && isWhitespaceWithLineBreaks(TreeUtil.findLastLeaf(treeNext),minCount)) || isWhitespaceWithLineBreaks(child.getLastChildNode(),minCount);
}
