{
  runWithLanguageLevel(LanguageLevel.PYTHON35,new Runnable(){
    @Override public void run(){
      doTest("int","async def foo(x):\n" + "    await x\n" + "    return 0\n"+ "\n"+ "async def bar(y):\n"+ "    expr = await foo(y)\n");
    }
  }
);
}
