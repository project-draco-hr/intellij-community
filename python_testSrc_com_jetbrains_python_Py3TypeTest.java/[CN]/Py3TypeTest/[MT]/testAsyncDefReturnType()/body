{
  runWithLanguageLevel(LanguageLevel.PYTHON35,new Runnable(){
    @Override public void run(){
      doTest("__coroutine[int]","async def foo(x):\n" + "    await x\n" + "    return 0\n"+ "\n"+ "def bar(y):\n"+ "    expr = foo(y)\n");
    }
  }
);
}
