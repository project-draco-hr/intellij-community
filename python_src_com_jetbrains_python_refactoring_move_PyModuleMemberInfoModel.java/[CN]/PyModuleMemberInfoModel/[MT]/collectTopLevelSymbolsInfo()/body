{
  return ContainerUtil.mapNotNull(collectTopLevelSymbols(),new Function<PyElement,PyModuleMemberInfo>(){
    @Override public PyModuleMemberInfo fun(    PyElement element){
      return new PyModuleMemberInfo(element);
    }
  }
);
}
