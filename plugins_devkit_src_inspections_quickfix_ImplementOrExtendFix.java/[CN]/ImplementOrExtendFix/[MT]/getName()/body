{
  PsiClass clazz=myCompClassPointer.getElement();
  if (clazz == null)   return "Invalid";
  return (clazz.isInterface() ? StringUtil.capitalize(DevKitBundle.message("keyword.implement")) : StringUtil.capitalize(DevKitBundle.message("keyword.extend"))) + " '" + clazz.getQualifiedName()+ "'";
}
