{
  final PsiDoWhileStatement while2=(PsiDoWhileStatement)myMatchingVisitor.getElement();
  myMatchingVisitor.setResult(myMatchingVisitor.match(while1.getCondition(),while2.getCondition()) && compareBody(while1.getBody(),while2.getBody()));
}
