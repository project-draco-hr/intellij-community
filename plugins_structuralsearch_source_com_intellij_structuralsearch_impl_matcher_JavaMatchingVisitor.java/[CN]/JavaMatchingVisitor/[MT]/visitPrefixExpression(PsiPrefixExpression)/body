{
  final PsiPrefixExpression prefix2=(PsiPrefixExpression)myMatchingVisitor.getElement();
  myMatchingVisitor.setResult(prefix.getOperationSign().textMatches(prefix2.getOperationSign()) && myMatchingVisitor.match(prefix.getOperand(),prefix2.getOperand()));
}
