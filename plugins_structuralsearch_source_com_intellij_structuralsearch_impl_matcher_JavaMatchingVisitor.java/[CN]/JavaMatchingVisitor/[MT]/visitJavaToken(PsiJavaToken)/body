{
  final PsiJavaToken anotherToken=(PsiJavaToken)myMatchingVisitor.getElement();
  myMatchingVisitor.setResult(token.getTokenType() == anotherToken.getTokenType() && token.textMatches(anotherToken));
}
