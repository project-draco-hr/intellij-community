{
  final PsiIfStatement if2=(PsiIfStatement)myMatchingVisitor.getElement();
  myMatchingVisitor.setResult(myMatchingVisitor.match(if1.getCondition(),if2.getCondition()) && compareBody(if1.getThenBranch(),if2.getThenBranch()) && compareBody(if1.getElseBranch(),if2.getElseBranch()));
}
