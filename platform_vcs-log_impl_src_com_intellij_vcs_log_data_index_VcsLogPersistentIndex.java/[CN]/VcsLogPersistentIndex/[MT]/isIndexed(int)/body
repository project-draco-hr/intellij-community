{
  try {
    return myMessagesIndex.get(commit) != null && (myTrigramIndex == null || myTrigramIndex.isIndexed(commit));
  }
 catch (  IOException e) {
    myFatalErrorsConsumer.consume(e);
  }
  return false;
}
