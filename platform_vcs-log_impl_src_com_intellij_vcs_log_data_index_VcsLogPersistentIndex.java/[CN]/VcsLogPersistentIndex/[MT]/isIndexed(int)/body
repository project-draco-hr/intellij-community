{
  try {
    return myMessagesIndex.get(commit) != null && (myUserIndex == null || myUserIndex.isIndexed(commit)) && (myTrigramIndex == null || myTrigramIndex.isIndexed(commit));
  }
 catch (  IOException e) {
    myFatalErrorsConsumer.consume(e);
  }
  return false;
}
