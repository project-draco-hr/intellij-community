{
  return ContainerUtil.map(arguments,new Function<String,String>(){
    @Override public String fun(    String argument){
      return needQuote(argument) && !isQuoted(argument) ? quote(argument) : argument;
    }
  }
);
}
