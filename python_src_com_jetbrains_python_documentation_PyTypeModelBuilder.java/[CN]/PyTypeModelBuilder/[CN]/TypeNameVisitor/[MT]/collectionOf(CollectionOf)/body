{
  myDepth++;
  if (myDepth > MAX_DEPTH) {
    add("...");
    return;
  }
  addType(collectionOf.collectionName);
  add("[");
  processList(collectionOf.elementTypes,", ");
  add("]");
  myDepth--;
}
