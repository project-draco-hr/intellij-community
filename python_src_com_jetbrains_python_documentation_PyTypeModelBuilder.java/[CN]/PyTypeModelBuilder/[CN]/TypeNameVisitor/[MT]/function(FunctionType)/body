{
  myDepth++;
  if (myDepth > MAX_DEPTH) {
    add("...");
    return;
  }
  add("(");
  processListCommaSeparated(function.parameters,", ");
  add(") -> ");
  function.returnType.accept(this);
  add("\n");
  myDepth--;
}
