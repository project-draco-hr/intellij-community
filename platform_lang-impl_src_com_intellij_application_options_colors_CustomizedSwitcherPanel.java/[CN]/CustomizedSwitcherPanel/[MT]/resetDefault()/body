{
  if (myActive != null) {
    final PaintLocker locker=new PaintLocker(this);
    try {
      setPreferredSize(getSize());
      remove(myActive.getPanel());
      myActive=null;
    }
  finally {
      locker.release();
    }
  }
}
