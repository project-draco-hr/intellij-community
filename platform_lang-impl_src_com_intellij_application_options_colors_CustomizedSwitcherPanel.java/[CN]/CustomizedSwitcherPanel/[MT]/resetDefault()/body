{
  myActive=null;
  if (getComponentCount() != 0) {
    try {
      setIgnoreRepaint(true);
      setPreferredSize(getSize());
      remove(0);
    }
  finally {
      setIgnoreRepaint(false);
      revalidate();
    }
  }
}
