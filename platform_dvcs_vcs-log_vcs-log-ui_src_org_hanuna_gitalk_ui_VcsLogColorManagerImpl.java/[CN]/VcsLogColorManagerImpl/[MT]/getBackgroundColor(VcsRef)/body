{
switch (ref.getType()) {
case HEAD:
    return Color.GREEN;
case LOCAL_BRANCH:
  return Color.ORANGE;
case REMOTE_BRANCH:
return Color.CYAN;
case TAG:
return Color.WHITE;
default :
throw new IllegalArgumentException("Unknown ref type: " + ref.getType() + ", ref: "+ ref);
}
}
