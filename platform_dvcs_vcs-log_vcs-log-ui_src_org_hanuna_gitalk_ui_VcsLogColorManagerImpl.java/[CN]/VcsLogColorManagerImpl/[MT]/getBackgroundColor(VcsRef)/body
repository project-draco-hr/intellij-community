{
switch (ref.getType()) {
case HEAD:
    return HEAD;
case LOCAL_BRANCH:
  return LOCAL_BRANCH;
case REMOTE_BRANCH:
return REMOTE_BRANCH;
case TAG:
return TAG;
default :
throw new IllegalArgumentException("Unknown ref type: " + ref.getType() + ", ref: "+ ref);
}
}
