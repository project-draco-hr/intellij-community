{
  PyType type=getNamedTupleType(referenceTarget,anchor);
  if (type != null) {
    return type;
  }
  type=getEnumType(referenceTarget,context);
  if (type != null) {
    return type;
  }
  return null;
}
