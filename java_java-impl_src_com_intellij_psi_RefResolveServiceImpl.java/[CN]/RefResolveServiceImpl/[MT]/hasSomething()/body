{
  if (DumbService.isDumb(myProject) || myApplication.isWriteActionInProgress() || RefreshQueueImpl.isRefreshInProgress()|| HeavyProcessLatch.INSTANCE.isRunning()) {
    return false;
  }
synchronized (filesToResolve) {
    return !filesToResolve.isEmpty();
  }
}
