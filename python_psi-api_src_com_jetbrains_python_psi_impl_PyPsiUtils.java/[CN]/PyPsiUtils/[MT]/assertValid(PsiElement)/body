{
  if (element == null) {
    return;
  }
  if (!element.isValid()) {
    throw new PsiInvalidElementAccessException(element,"Element is invalid. You should never work with invalid elements. " + "See _possible_ (and not guaranteed) reason before this message ");
  }
}
