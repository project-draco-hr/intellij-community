{
  return new ReadAction<String>(){
    @Override protected void run(    @NotNull final Result<String> result) throws Throwable {
      result.setResult(module.getName());
    }
  }
.execute().getResultObject();
}
