{
  return ContainerUtil.map2List(myState.RECENTLY_FILTERED_USER_GROUPS,new Function<UserGroup,List<String>>(){
    @Override public List<String> fun(    UserGroup group){
      return group.users;
    }
  }
);
}
