{
  myOriginType=varSubstitutor != null ? varSubstitutor.substitute(var.getType()) : var.getType();
  myType=evalSubstitutor != null ? evalSubstitutor.substitute(myTypeEvaluator.getType(var)) : myTypeEvaluator.getType(var);
  myChanged=!myType.equals(myOriginType);
}
