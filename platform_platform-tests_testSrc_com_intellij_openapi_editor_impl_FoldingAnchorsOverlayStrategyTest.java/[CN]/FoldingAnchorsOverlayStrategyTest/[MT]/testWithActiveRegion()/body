{
  prepareEditor("<body><div>\n" + "</div><div>\n" + "some text\n"+ "some text\n"+ "some text\n"+ "</div><div>\n"+ "</div></body>");
  collapseFoldingRegion(2);
  verifyAnchors(myFixture.getEditor().getFoldingModel().getAllFoldRegions()[1],0,Type.EXPANDED_TOP,1,Type.EXPANDED_BOTTOM,2,Type.EXPANDED_BOTTOM);
}
