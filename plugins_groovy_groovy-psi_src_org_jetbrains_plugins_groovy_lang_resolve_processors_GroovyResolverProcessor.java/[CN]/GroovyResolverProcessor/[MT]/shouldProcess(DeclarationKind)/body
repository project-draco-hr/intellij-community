{
  if (kind == DeclarationKind.METHOD && isPropertyResolve() && !myAcceptableKinds.contains(GroovyResolveKind.METHOD)) {
    return false;
  }
  for (  GroovyResolveKind resolveKind : myAcceptableKinds) {
    if (resolveKind.declarationKinds.contains(kind))     return true;
  }
  return false;
}
