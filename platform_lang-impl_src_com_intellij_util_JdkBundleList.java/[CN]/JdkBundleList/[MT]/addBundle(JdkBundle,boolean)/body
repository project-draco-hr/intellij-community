{
  JdkBundle bundleDescr=bundleMap.get(bundle.getBundleAsFile().getAbsolutePath());
  if (bundleDescr == null) {
    addMostRecent(bundle,forceOldVersion);
  }
 else {
    if (bundle.isBundled())     bundleDescr.setBundled(true);
    if (bundle.isBoot())     bundleDescr.setBoot(true);
  }
}
