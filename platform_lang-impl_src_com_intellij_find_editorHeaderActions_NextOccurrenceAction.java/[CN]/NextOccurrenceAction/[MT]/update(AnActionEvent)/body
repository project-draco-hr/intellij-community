{
  String textToSearch=mySearch ? myEditorSearchComponent.getSearchTextComponent().getText() : myEditorSearchComponent.getReplaceTextComponent().getText();
  e.getPresentation().setEnabled(myEditorSearchComponent.hasMatches() && !StringUtil.isEmpty(textToSearch));
}
