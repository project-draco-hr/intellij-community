{
  final List<CourseInfo> courses=myGenerator.getCoursesUnderProgress(true,"Refreshing Course List",DefaultProjectFactory.getInstance().getDefaultProject());
  if (!courses.contains(CourseInfo.INVALID_COURSE)) {
    refreshCoursesList(courses);
  }
}
