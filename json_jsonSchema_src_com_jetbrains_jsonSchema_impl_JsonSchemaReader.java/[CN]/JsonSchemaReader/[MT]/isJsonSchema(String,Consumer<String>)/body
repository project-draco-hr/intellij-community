{
  try {
    new JsonSchemaReader().read(new java.io.StringReader(string));
    return true;
  }
 catch (  IOException e) {
    errorConsumer.consume(e.getMessage());
    return false;
  }
catch (  Exception e) {
    errorConsumer.consume(e.getMessage());
    return false;
  }
}
