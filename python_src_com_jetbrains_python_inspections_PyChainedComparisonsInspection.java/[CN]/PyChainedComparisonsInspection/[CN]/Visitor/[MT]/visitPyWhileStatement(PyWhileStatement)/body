{
  if (node.getWhilePart().getCondition() instanceof PyBinaryExpression) {
    if (checkBinaryExpression((PyBinaryExpression)node.getWhilePart().getCondition()))     registerProblem(node,"Simplify chained comparison",new ChainedComparisonsQuickFix());
  }
}
