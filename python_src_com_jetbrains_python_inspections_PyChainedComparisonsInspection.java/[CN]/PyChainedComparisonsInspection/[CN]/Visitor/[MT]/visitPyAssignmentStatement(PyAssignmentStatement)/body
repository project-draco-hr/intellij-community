{
  if (node.getAssignedValue() instanceof PyBinaryExpression) {
    if (checkBinaryExpression((PyBinaryExpression)node.getAssignedValue()))     registerProblem(node,"Simplify chained comparison",new ChainedComparisonsQuickFix());
  }
}
