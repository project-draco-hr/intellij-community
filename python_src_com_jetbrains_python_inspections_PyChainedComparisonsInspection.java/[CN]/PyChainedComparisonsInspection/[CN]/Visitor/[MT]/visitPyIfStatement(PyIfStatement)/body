{
  if (node.getIfPart().getCondition() instanceof PyBinaryExpression) {
    if (checkBinaryExpression((PyBinaryExpression)node.getIfPart().getCondition()))     registerProblem(node,"Simplify chained comparison",new ChainedComparisonsQuickFix());
  }
}
