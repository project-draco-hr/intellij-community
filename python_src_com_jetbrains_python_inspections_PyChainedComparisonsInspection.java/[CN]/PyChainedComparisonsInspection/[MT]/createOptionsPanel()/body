{
  final JPanel rootPanel=new JPanel(new BorderLayout());
  rootPanel.add(new CheckBox("Simplify statements with a constant in the middle",this,"simplifyWithConstantInTheMiddle"),BorderLayout.PAGE_START);
  return rootPanel;
}
