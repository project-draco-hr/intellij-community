{
  String version=properties.getVersion();
  if (version.equals(RepositoryUtils.LatestVersionId) || version.equals(RepositoryUtils.ReleaseVersionId) || version.endsWith(RepositoryUtils.SnapshotVersionSuffix)) {
    return true;
  }
  for (  OrderRootType orderRootType : OrderRootType.getAllTypes()) {
    if (library.getFiles(orderRootType).length != library.getUrls(orderRootType).length) {
      return true;
    }
  }
  return false;
}
