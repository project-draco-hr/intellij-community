{
  String message=e.getMessage();
  if (message.contains(JGitText.get().notAuthorized) || message.contains("Auth cancel") || message.contains("Auth fail")) {
    throw new AuthenticationException(message,e);
  }
 else {
    throw e;
  }
}
