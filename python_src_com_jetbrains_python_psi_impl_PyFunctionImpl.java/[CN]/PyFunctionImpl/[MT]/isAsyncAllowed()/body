{
  final LanguageLevel languageLevel=LanguageLevel.forElement(this);
  final String functionName=getName();
  return languageLevel.isAtLeast(LanguageLevel.PYTHON35) && (functionName == null || ArrayUtil.contains(functionName,PyNames.AITER,"__anext__","__aenter__","__aexit__") || !PyNames.getBuiltinMethods(languageLevel).containsKey(functionName));
}
