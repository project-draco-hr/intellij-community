{
  String commitMessage=StringUtil.shortenTextWithEllipsis(commit.getSubject(),20,0);
  return String.format("<code><b>%s</b> \"%s\"</code> by <code>%s</code>",commit.getId().toShortString(),commitMessage,commit.getAuthor().getName());
}
