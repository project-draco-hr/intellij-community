{
  return isAlphaSorted(ContainerUtil.mapNotNull(propertiesFile.getProperties(),new NullableFunction<IProperty,String>(){
    @Nullable @Override public String fun(    IProperty property){
      return property.getKey();
    }
  }
));
}
