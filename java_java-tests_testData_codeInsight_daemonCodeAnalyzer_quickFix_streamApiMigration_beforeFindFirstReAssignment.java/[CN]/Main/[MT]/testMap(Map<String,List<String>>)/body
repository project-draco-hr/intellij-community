{
  int firstSize=10;
  System.out.println(firstSize);
  firstSize=getInitialSize();
  for (  List<String> list : map.valu < caret > es()) {
    if (list != null) {
      firstSize=list.size();
      break;
    }
  }
  System.out.println(firstSize);
}
