{
  while (true) {
    if (adjuster != null) {
      adjuster.consume(myWizard.getCurrentStepObject());
    }
    if (myWizard.isLast()) {
      break;
    }
    myWizard.doNextAction();
  }
  myWizard.doOk();
}
