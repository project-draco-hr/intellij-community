{
  return ContainerUtil.exists(registered,new Condition<T>(){
    @Override public boolean value(    T parent){
      return isSamePath(child,parent) || isSameSupposedUrl(child,parent);
    }
  }
);
}
