{
  File runner=new File(new File(new File("/usr","local"),"bin"),PY_TEST);
  if (!runner.exists())   runner=new File(new File("/usr","bin"),PY_TEST);
  if (runner.exists() && checkVersion(runner,sdkHome))   return runner.getPath();
  return null;
}
