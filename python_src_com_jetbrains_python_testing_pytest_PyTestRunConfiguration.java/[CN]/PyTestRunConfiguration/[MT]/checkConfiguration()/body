{
  if (StringUtil.isEmptyOrSpaces(myTestToRun)) {
    throw new RuntimeConfigurationError("Please specify target folder or script");
  }
  if (!PyTestFrameworksUtil.isPyTestInstalled(getProject(),getSdkHome()))   throw new RuntimeConfigurationError("No py.test runner found in selected interpreter");
}
