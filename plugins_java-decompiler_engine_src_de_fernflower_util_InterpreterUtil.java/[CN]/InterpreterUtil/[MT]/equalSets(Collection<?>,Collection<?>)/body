{
  if (c1 == null) {
    return c2 == null ? true : false;
  }
 else {
    if (c2 == null) {
      return false;
    }
  }
  if (c1.size() != c2.size()) {
    return false;
  }
  HashSet<?> set=new HashSet(c1);
  set.removeAll(c2);
  return (set.size() == 0);
}
