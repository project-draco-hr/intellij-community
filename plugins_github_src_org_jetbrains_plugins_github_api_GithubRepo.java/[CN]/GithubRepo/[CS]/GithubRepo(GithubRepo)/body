{
  this(repo.id,repo.name,repo.fullName,repo.description,repo.isPrivate,repo.isFork,repo.url,repo.htmlUrl,repo.cloneUrl,repo.gitUrl,repo.sshUrl,repo.mirrorUrl,repo.size,repo.masterBranch,repo.owner);
}
