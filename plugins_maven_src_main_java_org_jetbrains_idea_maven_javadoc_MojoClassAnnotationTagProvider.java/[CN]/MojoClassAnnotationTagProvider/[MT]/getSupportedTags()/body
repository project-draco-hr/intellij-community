{
  return ContainerUtil.map(ANNOTATION_NAMES,new Function<String,JavadocTagInfo>(){
    @Override public JavadocTagInfo fun(    String name){
      return new MojoAnnotationInfo(name);
    }
  }
);
}
