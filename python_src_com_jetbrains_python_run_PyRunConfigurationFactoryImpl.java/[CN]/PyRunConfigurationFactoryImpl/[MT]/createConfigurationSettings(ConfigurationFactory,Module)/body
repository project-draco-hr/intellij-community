{
  final RunnerAndConfigurationSettings settings=RunManagerEx.getInstanceEx(module.getProject()).createConfiguration(module.getName(),factory);
  ModuleBasedConfiguration configuration=(ModuleBasedConfiguration)settings.getConfiguration();
  configuration.setModule(module);
  return settings;
}
