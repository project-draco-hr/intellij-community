{
  myVisibleNodes=visibleNodes;
  myVisibleHashes=ContainerUtil.map2Set(myVisibleNodes,new Function<Node,Integer>(){
    @Override public Integer fun(    Node node){
      return node.getCommitIndex();
    }
  }
);
}
