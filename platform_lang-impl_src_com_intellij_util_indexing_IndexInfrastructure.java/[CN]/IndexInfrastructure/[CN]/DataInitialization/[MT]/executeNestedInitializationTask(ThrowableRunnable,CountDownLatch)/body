{
  try {
    if (ApplicationManager.getApplication().isDisposed())     return;
    callable.run();
  }
 catch (  Throwable t) {
    onThrowable(t);
  }
 finally {
    proceedLatch.countDown();
  }
}
