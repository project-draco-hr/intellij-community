{
  return ContainerUtil.filter(RemoteServersManager.getInstance().getServers(),new Condition<RemoteServer<?>>(){
    @Override public boolean value(    RemoteServer<?> server){
      return server.getType().getCustomToolWindowId() == null;
    }
  }
);
}
