{
  runWithDocStringFormat(DocStringFormat.REST,new Runnable(){
    @Override public void run(){
      doChangeSignatureTest(null,Arrays.asList(new PyParameterInfo(0,"a",null,false),new PyParameterInfo(3,"d",null,false)));
    }
  }
);
}
