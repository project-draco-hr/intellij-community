{
  SuspendContextImpl suspendContext=context.getSuspendContext();
  if (suspendContext != null && suspendContext.getSuspendPolicy() == EventRequest.SUSPEND_EVENT_THREAD && context.getThreadProxy() != suspendContext.getThread()) {
    ((XDebugSessionImpl)getSession()).unsetPaused();
  }
}
