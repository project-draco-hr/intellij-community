{
  LogEntry result=null;
  if (entry != null) {
    result=new LogEntry(entry.getChangedPaths(),entry.getRevision(),entry.getAuthor(),entry.getDate(),entry.getMessage(),entry.hasChildren());
  }
  return result;
}
