{
  return mySettings.isWhitespacesShown() && (offset < currentLeadingEdge ? mySettings.isLeadingWhitespaceShown() : offset >= currentTrailingEdge ? mySettings.isTrailingWhitespaceShown() : mySettings.isInnerWhitespaceShown());
}
