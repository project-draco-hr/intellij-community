{
  for (  CaretRectangle location : myLocations) {
    myEditorComponent.repaintEditorComponent(myUseNewRendering ? location.myPoint.x - getCharHeight() * 3 : location.myPoint.x,location.myPoint.y,myUseNewRendering ? getCharHeight() * 6 : location.myWidth,getLineHeight());
  }
}
