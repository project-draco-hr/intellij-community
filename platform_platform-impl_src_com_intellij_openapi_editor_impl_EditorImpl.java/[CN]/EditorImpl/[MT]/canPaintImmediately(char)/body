{
  return myDocument instanceof DocumentImpl && myHighlighter instanceof LexerEditorHighlighter && myDocument.getTextLength() > 0 && myCaretModel.getCaretCount() == 1 && !mySelectionModel.hasSelection() && !KEY_CHARS_TO_SKIP.contains(c);
}
