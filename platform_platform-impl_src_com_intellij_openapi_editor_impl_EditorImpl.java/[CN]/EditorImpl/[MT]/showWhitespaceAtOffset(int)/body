{
  return mySettings.isWhitespacesShown() && (myShowWhitespace != null ? myShowWhitespace : offset < myCurrentLeadingEdge ? mySettings.isLeadingWhitespacesShown() : offset >= myCurrentTrailingEdge ? mySettings.isTrailingWhitespacesShown() : mySettings.isInnerWhitespacesShown());
}
