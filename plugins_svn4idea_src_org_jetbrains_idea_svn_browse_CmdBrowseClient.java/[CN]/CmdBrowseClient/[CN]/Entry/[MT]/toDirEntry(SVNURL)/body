{
  SVNDirEntry entry=new SVNDirEntry(url.appendPath(name,false),null,name,SVNNodeKind.parseKind(kind),size,false,revision(),date(),author());
  entry.setRelativePath(null);
  entry.setLock(lock != null ? lock.toLock(entry.getRelativePath()) : null);
  return entry;
}
