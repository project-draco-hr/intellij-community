{
  SVNDirEntry entry=new SVNDirEntry(url.appendPath(name,false),null,PathUtil.getFileName(name),SVNNodeKind.parseKind(kind),size,false,revision(),date(),author());
  entry.setRelativePath(name);
  entry.setLock(lock != null ? lock.toLock(entry.getRelativePath()) : null);
  return entry;
}
