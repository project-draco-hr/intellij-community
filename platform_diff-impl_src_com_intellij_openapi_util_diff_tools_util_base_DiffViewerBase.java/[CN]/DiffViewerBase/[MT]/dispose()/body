{
  myDisposed=true;
  Disposer.dispose(myAlarm);
  abortRediff();
  UIUtil.invokeLaterIfNeeded(new Runnable(){
    @Override public void run(){
      abortRediff();
      onDisposeAwt();
    }
  }
);
  onDispose();
}
