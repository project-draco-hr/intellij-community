{
  PushTarget target=spec.getTarget();
  return (target == null || StringUtil.isEmptyOrSpaces(target.getPresentation())) ? new ValidationInfo("Please, specify remote push path for selected repos!") : null;
}
