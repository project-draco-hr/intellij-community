{
  return iterable instanceof JBIterable ? (JBIterable<E>)iterable : new JBIterable<E>((Iterable<E>)iterable){
    @Override public Iterator<E> iterator(){
      return myIterable.iterator();
    }
  }
;
}
