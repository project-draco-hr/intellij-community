{
  return new JBIterable<E>(){
    @Override public Iterator<E> iterator(){
      return new JBIterator<E>(){
        E cur=seed;
        @Override public E nextImpl(){
          return (cur=generator.fun(cur)) == null ? stop() : cur;
        }
      }
;
    }
  }
;
}
