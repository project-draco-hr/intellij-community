{
  int windowHeight=(myShouldEnlarge) ? myWindow.getHeight() + SHEET_ANIMATION_STEP : myWindow.getHeight() - SHEET_ANIMATION_STEP;
  myWindow.setSize(myWindow.getWidth(),windowHeight);
  setPositionRelativeToParent();
  if (myWindow.getHeight() > SheetController.SHEET_HEIGHT) {
    myAnimator.stop();
    myWindow.setContentPane(myController.getPanel(myWindow));
    myController.requestFocus();
    myShouldEnlarge=false;
  }
  if (myWindow.getHeight() < 0) {
    myAnimator.stop();
    myWindow.dispose();
  }
}
