{
  doTest("import java.util.Scanner;" + "class A {" + "    void a() throws java.io.IOException {"+ "        try (Scanner scanner = new Scanner(\"\").useDelimiter(\"\\\\A\")) {"+ "            String sconf = scanner.next();"+ "            System.out.println(sconf);"+ "        }"+ "    }"+ "}"+ "");
}
