{
  doTest("import java.io.*;" + "class X {" + "    public void m() throws IOException {"+ "        FileInputStream str;"+ "        str = new /*'FileInputStream' used without 'try'-with-resources statement*/FileInputStream/**/(\"bar\");"+ "        try {"+ "        } finally {"+ "            str.close();"+ "        }"+ "    }"+ "}");
}
