{
  doTest("import java.io.*;" + "class X {" + "    public void m() throws IOException {"+ "        FileInputStream str;"+ "        str = /*'FileInputStream' used without 'try'-with-resources statement*/new FileInputStream(\"bar\")/**/;"+ "        try {"+ "        } finally {"+ "            str.close();"+ "        }"+ "    }"+ "}");
}
