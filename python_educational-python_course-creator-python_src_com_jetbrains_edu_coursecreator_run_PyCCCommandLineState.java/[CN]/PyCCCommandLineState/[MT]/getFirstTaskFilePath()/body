{
  String firstTaskFileName=StudyUtils.getFirst(myTask.getTaskFiles().keySet());
  return myTaskDir.findChild(EduNames.SRC) != null ? FileUtil.join(myTaskDir.getPath(),EduNames.SRC,firstTaskFileName) : FileUtil.join(myTaskDir.getPath(),firstTaskFileName);
}
