{
  int count=0;
  for (  Set<String> element : neste < caret > d) {
    if (element != null) {
      for (      String str : element) {
        if (str.startsWith("xyz")) {
          count++;
        }
      }
    }
  }
}
