{
  boolean useSvnKit=Registry.is("svn.use.svnkit.for.https.server.certificate.check");
  return error.startsWith(CERTIFICATE_ERROR) || error.contains(UNTRUSTED_SERVER_CERTIFICATE) || useSvnKit && isCertificateVerificationFailed(error) || !useSvnKit && isValidButUntrustedCertificate(error);
}
