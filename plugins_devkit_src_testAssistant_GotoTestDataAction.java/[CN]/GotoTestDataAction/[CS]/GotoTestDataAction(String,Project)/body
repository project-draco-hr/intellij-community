{
  super("Go to " + FilePathSplittingPolicy.SPLIT_BY_SEPARATOR.getPresentableName(new File(basePath),50),null,FileTypeManager.getInstance().getFileTypeByFileName(basePath).getIcon());
  myBasePath=basePath;
  myProject=project;
}
