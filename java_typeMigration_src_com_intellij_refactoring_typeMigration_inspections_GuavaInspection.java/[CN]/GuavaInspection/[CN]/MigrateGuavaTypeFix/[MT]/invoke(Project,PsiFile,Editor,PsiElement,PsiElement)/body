{
  if (myInitialType == null) {
    performTypeMigration(Collections.singletonList(startElement),Collections.singletonList(myTargetType));
  }
 else {
    performChainTypeMigration(project,Collections.singletonList(startElement),Collections.singletonList(myInitialType),Collections.singletonList(myTargetType));
  }
}
