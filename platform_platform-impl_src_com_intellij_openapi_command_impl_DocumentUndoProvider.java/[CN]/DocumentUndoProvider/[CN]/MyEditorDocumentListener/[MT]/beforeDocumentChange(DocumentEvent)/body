{
  if (!CommandProcessor.getInstance().isUndoTransparentActionInProgress() && CommandProcessor.getInstance().getCurrentCommand() == null) {
    LOG.error("Must not change document outside command or undo-transparent action.");
  }
}
