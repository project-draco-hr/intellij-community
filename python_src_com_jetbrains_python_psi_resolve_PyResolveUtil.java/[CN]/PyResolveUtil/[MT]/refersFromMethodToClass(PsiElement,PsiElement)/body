{
  return (inner != null && PsiTreeUtil.isAncestor(outer,inner,false) && (PyUtil.getConcealingParent(outer) instanceof PyClass) && (PsiTreeUtil.getParentOfType(inner,PyFunction.class,false) != null));
}
