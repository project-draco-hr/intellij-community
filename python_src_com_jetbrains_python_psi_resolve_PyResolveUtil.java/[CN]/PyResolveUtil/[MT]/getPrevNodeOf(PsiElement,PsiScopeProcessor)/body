{
  if (elt instanceof PsiFile)   return null;
  if (proc instanceof PyClassScopeProcessor) {
    return getPrevNodeOf(elt,((PyClassScopeProcessor)proc).getTargetCondition());
  }
 else   return getPrevNodeOf(elt,IS_NAME_DEFINER);
}
