{
  if (innerFunction == null) {
    return false;
  }
  PsiElement outerClass=PyUtil.getConcealingParent(outer);
  if (outerClass instanceof PyClass && innerFunction.getContainingClass() == outerClass) {
    return true;
  }
  return false;
}
