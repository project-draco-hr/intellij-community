{
  BuildLauncher result=connection.newBuild();
  List<String> extraJvmArgs=vmOptions == null ? ContainerUtil.<String>emptyList() : ContainerUtil.newArrayList(StringUtil.split(vmOptions.trim()," "));
  prepare(result,id,settings,listener,extraJvmArgs,connection,standardOutput,standardError);
  return result;
}
