{
  if (element == null || element.getTextLength() == 0) {
    return;
  }
  if (quickFix != null)   myHolder.registerProblem(element,message,quickFix);
 else   myHolder.registerProblem(element,message);
}
