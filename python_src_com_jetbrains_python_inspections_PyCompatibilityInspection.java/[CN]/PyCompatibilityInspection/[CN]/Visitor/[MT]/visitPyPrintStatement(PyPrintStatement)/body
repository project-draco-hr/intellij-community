{
  PsiElement[] arguments=node.getChildren();
  for (  PsiElement element : arguments) {
    if (!(element instanceof PyParenthesizedExpression))     registerProblem(element,"Python versions >= 3.0 do not support this syntax. The print statement has been replaced with a print() function");
  }
}
