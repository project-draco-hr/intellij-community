{
synchronized (mySocketObject) {
    final InputStream myInputStream=socket.getInputStream();
    final Reader reader=new InputStreamReader(myInputStream,CharsetToolkit.UTF8_CHARSET);
    return new DebuggerReader(reader);
  }
}
