{
  logFrame(frame,true);
  try {
    final byte[] packed=frame.pack();
    final OutputStream os=mySocket.getOutputStream();
    os.write(packed);
    os.write('\n');
    os.flush();
  }
 catch (  SocketException se) {
    disconnect();
    fireCloseEvent();
  }
catch (  IOException e) {
    LOG.error(e);
  }
}
