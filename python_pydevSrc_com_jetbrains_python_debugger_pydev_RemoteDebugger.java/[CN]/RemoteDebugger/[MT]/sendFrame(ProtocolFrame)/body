{
  logFrame(frame,true);
  try {
    final byte[] packed=frame.pack();
    final OutputStream os=mySocket.getOutputStream();
    os.write(packed);
    os.write('\n');
    os.flush();
  }
 catch (  SocketException se) {
    LOG.error(se);
    disconnect();
  }
catch (  IOException e) {
    LOG.error(e);
  }
}
