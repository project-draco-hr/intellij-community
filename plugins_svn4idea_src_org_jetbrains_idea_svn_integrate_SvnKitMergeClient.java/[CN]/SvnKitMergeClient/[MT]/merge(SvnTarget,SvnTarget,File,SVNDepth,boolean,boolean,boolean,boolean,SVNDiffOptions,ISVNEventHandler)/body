{
  assertUrl(source1);
  assertUrl(source2);
  try {
    createClient(diffOptions,handler).doMerge(source1.getURL(),source1.getPegRevision(),source2.getURL(),source2.getPegRevision(),destination,depth,useAncestry,force,dryRun,recordOnly);
  }
 catch (  SVNException e) {
    throw new VcsException(e);
  }
}
