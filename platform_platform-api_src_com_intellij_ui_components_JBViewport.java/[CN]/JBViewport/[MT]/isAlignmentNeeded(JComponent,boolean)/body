{
  return (!SystemInfo.isMac || horizontal && Registry.is("mac.scroll.horizontal.gap")) && (view instanceof JList || view instanceof JTree || (!SystemInfo.isMac && Registry.is("ide.scroll.align.component")));
}
