{
  VcsRootConfiguration vcsRootConfiguration=new VcsRootConfiguration().mock(Arrays.asList(".","community")).roots(Arrays.asList("another","contrib")).unregErrors(Arrays.asList("community",".")).extraErrors(Arrays.asList("contrib","another"));
  doTest(vcsRootConfiguration);
}
