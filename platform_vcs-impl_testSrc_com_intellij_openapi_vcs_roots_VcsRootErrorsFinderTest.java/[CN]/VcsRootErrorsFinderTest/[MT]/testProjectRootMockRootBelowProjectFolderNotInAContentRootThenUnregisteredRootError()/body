{
  VcsRootConfiguration vcsRootConfiguration=new VcsRootConfiguration().mock("community").contentRoots(".").roots(PROJECT).unregErrors("community").extraErrors(PROJECT);
  doTest(vcsRootConfiguration);
}
