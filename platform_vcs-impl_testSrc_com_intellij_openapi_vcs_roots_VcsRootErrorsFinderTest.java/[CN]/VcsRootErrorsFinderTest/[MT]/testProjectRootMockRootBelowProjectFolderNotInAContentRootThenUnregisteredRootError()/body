{
  VcsRootConfiguration vcsRootConfiguration=new VcsRootConfiguration().mock(Arrays.asList("community")).contentRoots(Arrays.asList(".")).roots(Arrays.asList(PROJECT)).unregErrors(Arrays.asList("community")).extraErrors(Arrays.asList(PROJECT));
  doTest(vcsRootConfiguration);
}
