{
  VcsRootConfiguration vcsRootConfiguration=new VcsRootConfiguration().mock(Arrays.asList(".")).roots(Arrays.asList("community")).unregErrors(Arrays.asList(".")).extraErrors(Arrays.asList("community"));
  doTest(vcsRootConfiguration);
}
