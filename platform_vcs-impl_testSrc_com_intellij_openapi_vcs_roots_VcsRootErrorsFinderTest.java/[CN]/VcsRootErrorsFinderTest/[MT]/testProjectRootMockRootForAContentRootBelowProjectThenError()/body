{
  VcsRootConfiguration vcsRootConfiguration=new VcsRootConfiguration().mock(Arrays.asList("content_root")).contentRoots(Arrays.asList("content_root")).roots(Arrays.asList(PROJECT)).unregErrors(Arrays.asList("content_root")).extraErrors(Arrays.asList(PROJECT));
  doTest(vcsRootConfiguration);
}
