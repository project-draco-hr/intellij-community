{
  if (index == -1) {
    return EduNames.TESTS_FILE;
  }
  return FileUtil.getNameWithoutExtension(EduNames.TESTS_FILE) + EduNames.STEP_MARKER + index+ "."+ FileUtilRt.getExtension(EduNames.TESTS_FILE);
}
