{
  Set<String> result=ContainerUtil.newTroveSet(MavenConstants.TYPE_JAR,MavenConstants.TYPE_TEST_JAR,"maven-plugin","ejb","ejb-client","jboss-har","jboss-sar","war","ear","bundle");
  if (type == SupportedRequestType.FOR_COMPLETION) {
    result.add(MavenConstants.TYPE_POM);
  }
  for (  MavenImporter each : getSuitableImporters()) {
    each.getSupportedDependencyTypes(result,type);
  }
  return result;
}
