{
  final FileType fileType=file.getFileType();
  return ProjectRootManager.getInstance(project).getFileIndex().isInContent(file) && fileType == HtmlFileType.INSTANCE || fileType == XHtmlFileType.INSTANCE;
}
