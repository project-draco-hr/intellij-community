{
  final String name=file.getName();
  final String moduleTitle=GroovyConsoleStateService.getInstance(project).getSelectedModuleTitle(file);
  return name.startsWith(CONTENT_ID) ? StringUtil.replace(name,CONTENT_ID,moduleTitle == null ? "unknown" : moduleTitle) : String.format("%s-%s",moduleTitle,name);
}
