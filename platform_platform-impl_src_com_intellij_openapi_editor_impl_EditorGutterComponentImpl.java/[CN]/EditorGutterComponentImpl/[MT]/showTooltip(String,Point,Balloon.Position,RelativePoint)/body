{
  LineTooltipRenderer renderer=new LineTooltipRenderer(toolTip);
  TooltipController.getInstance().showTooltipByMouseMove(myEditor,showPoint,renderer,false,GUTTER_TOOLTIP_GROUP,new HintHint(this,t).setAwtTooltip(true).setPreferredPosition(ballPosition));
  return renderer;
}
