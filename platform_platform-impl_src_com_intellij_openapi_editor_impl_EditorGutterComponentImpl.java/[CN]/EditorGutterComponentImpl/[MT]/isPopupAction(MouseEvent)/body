{
  GutterIconRenderer renderer=getGutterRenderer(e);
  return renderer != null && !isNavigationBlocked(renderer,myEditor.getProject()) && renderer.getClickAction() == null && renderer.getPopupMenuActions() != null;
}
