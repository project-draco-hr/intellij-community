{
  THashSet<Map.Entry<? extends GitBranch,Hash>> set1=new THashSet<Map.Entry<? extends GitBranch,Hash>>(c1.entrySet(),new BranchesComparingStrategy());
  THashSet<Map.Entry<? extends GitBranch,Hash>> set2=new THashSet<Map.Entry<? extends GitBranch,Hash>>(c2.entrySet(),new BranchesComparingStrategy());
  return set1.equals(set2);
}
