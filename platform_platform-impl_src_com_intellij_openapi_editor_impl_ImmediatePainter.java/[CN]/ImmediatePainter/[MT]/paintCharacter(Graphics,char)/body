{
  final EditorImpl editor=myEditor;
  if (!VIM_PLUGIN_LOADED && ENABLED.asBoolean() && editor.getDocument().isWritable()&& !editor.isViewer()&& canPaintImmediately(editor,c)) {
    paintImmediately(g,editor.getCaretModel().getPrimaryCaret().getOffset(),c);
  }
}
