{
  if (elem == null) {
    return false;
  }
  return elem instanceof PsiWhiteSpace || elem.getNode().getElementType() == PyTokenTypes.BACKSLASH || (elem instanceof PsiErrorElement && elem.getTextLength() == 0);
}
