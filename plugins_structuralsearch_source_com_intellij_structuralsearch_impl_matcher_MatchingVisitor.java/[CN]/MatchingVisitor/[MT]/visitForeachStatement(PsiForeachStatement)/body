{
  final PsiForeachStatement for2=(PsiForeachStatement)element;
  result=match(for1.getIterationParameter(),for2.getIterationParameter()) && match(for1.getIteratedValue(),for2.getIteratedValue()) && compareBody(for1.getBody(),for2.getBody());
}
