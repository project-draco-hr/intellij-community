{
  PsiClassInitializer initializer2=(PsiClassInitializer)element;
  result=match(initializer.getModifierList(),initializer2.getModifierList()) && match(initializer.getBody(),initializer2.getBody());
}
