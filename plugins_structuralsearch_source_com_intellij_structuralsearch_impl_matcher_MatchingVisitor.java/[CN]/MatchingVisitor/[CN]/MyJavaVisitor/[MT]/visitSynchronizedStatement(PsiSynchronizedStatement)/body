{
  final PsiSynchronizedStatement synchronized2=(PsiSynchronizedStatement)element;
  result=match(synchronized1.getLockExpression(),synchronized2.getLockExpression()) && matchSons(synchronized1.getBody(),synchronized2.getBody());
}
