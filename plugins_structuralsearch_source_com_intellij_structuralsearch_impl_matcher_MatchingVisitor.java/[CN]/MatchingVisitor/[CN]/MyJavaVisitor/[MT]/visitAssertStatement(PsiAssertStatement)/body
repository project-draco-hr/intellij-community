{
  final PsiAssertStatement assert2=(PsiAssertStatement)element;
  result=match(assert1.getAssertCondition(),assert2.getAssertCondition()) && match(assert1.getAssertDescription(),assert2.getAssertDescription());
}
