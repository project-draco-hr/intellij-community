{
  return ContainerUtil.mapNotNull(files,new Function<VirtualFile,HgRepository>(){
    @Override public HgRepository fun(    VirtualFile file){
      return repositoryManager.getRepositoryForFile(file);
    }
  }
);
}
