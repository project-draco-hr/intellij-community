{
  return Registry.is("ide.mac.message.sheets.java.emulation.dialogs") && SystemInfo.isJavaVersionAtLeast("1.7") ? ServiceManager.getService(MacMessagesEmulation.class) : ServiceManager.getService(MacMessages.class);
}
