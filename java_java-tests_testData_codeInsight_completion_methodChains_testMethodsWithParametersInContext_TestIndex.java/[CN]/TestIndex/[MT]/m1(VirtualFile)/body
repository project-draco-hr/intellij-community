{
  PsiManager.getInstance().findFile(f).findElementAt(0);
  PsiManager.getInstance().findFile(f).findElementAt(0);
  PsiManager.getInstance().findFile(f).findElementAt(0);
  PsiManager.getInstance().findFile(f).findElementAt(0);
}
