{
  Vm vm=getVm();
  if (vm != null) {
    SuspendContext context=vm.getSuspendContextManager().getContext();
    if (context != null) {
      lastCallFrame=context.getTopFrame();
      return;
    }
  }
  lastCallFrame=null;
}
