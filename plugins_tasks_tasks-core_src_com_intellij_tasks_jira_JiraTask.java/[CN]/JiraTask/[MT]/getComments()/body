{
  return ContainerUtil.map2Array(myJiraIssue.getComments(),Comment.class,new Function<JiraComment,Comment>(){
    @Override public Comment fun(    JiraComment comment){
      return new JiraCommentAdapter(comment);
    }
  }
);
}
