{
  if (myFile.exists() && myFile.length() > 0 && readVersion(versionFile) != CURRENT_VERSION) {
    myFile.delete();
  }
  try {
    FileUtil.writeToFile(versionFile,Integer.toString(CURRENT_VERSION));
  }
 catch (  IOException e) {
    LOG.debug(e);
  }
}
