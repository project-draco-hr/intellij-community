{
  return (element instanceof PsiMethod && ((PsiMethod)element).isConstructor() && acceptClass(((PsiMethod)element).getContainingClass()) || acceptClass(element)) && element.getLanguage().isKindOf(JavaLanguage.INSTANCE);
}
