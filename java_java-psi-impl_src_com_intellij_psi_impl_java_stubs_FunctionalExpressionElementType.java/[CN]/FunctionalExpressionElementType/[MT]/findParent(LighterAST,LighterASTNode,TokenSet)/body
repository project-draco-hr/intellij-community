{
  while (node != null) {
    final IElementType type=node.getTokenType();
    if (elementType.contains(type))     return node;
    if (ElementType.JAVA_STATEMENT_BIT_SET.contains(type) || ElementType.MEMBER_BIT_SET.contains(type) || ARRAY_INITIALIZER_EXPRESSION == type)     return null;
    node=tree.getParent(node);
  }
  return null;
}
