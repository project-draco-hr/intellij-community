{
  return new DescriptorComposer(this){
    @Override protected void composeAdditionalDescription(    @NotNull final StringBuffer buf,    @NotNull final RefEntity refEntity){
      getTool().compose(buf,refEntity,this);
    }
  }
;
}
