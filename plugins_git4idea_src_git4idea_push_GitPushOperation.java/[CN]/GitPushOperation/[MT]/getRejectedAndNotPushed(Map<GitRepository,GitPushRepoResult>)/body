{
  return ContainerUtil.filter(results.keySet(),new Condition<GitRepository>(){
    @Override public boolean value(    GitRepository repository){
      return results.get(repository).getType() == GitPushRepoResult.Type.REJECTED || results.get(repository).getType() == GitPushRepoResult.Type.NOT_PUSHED;
    }
  }
);
}
