{
  UpdateMethod updateMethod=settings.getUpdateMethod();
  mySettings.setUpdateAllRootsIfPushRejected(settings.shouldUpdateAllRoots());
  if (!rebaseOverMergeDetected) {
    mySettings.setUpdateType(updateMethod);
  }
}
