{
  if (!data.isEmpty()) {
    for (    List<String> list : dat < caret > a) {
      for (      String str : list) {
        if (!str.isEmpty()) {
          System.out.println("Found!");
          return false;
        }
      }
    }
    return true;
  }
  return false;
}
