{
  int count=0;
  for (int i=start, len=min(text.length(),end); i < len; ++i) {
    if (text.charAt(i) == c) {
      count++;
    }
 else     if (stopAtOtherChar) {
      break;
    }
  }
  return count;
}
