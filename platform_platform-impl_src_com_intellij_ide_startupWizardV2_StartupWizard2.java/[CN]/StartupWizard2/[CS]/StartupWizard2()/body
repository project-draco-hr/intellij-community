{
  super(null,true,true);
  IconLoader.activate();
  getPeer().setAppIcons();
  setResizable(false);
  setTitle("Customize" + ApplicationNamesInfo.getInstance().getProductName());
  init();
  showPage(0);
}
