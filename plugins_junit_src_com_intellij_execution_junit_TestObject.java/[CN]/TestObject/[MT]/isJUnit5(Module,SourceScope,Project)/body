{
  return JUnitUtil.isJUnit5(module != null ? GlobalSearchScope.moduleRuntimeScope(module,true) : sourceScope != null ? sourceScope.getLibrariesScope() : GlobalSearchScope.allScope(project),project);
}
