{
synchronized (mySocketObject) {
    try {
      if (myDebuggerReader != null) {
        myDebuggerReader.stop();
      }
    }
  finally {
      if (mySocket != null) {
        try {
          mySocket.close();
        }
 catch (        IOException ignored) {
        }
      }
    }
  }
}
