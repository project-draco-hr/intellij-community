{
  try {
    myVcs.getFactory(myOldFilePath.getIOFile()).createAddClient().add(myOldFilePath.getIOFile(),SVNDepth.EMPTY,true,false,true,null);
  }
 catch (  VcsException e) {
    context.handleException(e,true);
  }
}
