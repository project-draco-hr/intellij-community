{
  if (!isResourceCreation(expression)) {
    return false;
  }
  final PsiVariable variable=ResourceInspection.getVariable(expression);
  return !(variable instanceof PsiResourceVariable) && !isResourceEscapingFromMethod(variable,expression);
}
