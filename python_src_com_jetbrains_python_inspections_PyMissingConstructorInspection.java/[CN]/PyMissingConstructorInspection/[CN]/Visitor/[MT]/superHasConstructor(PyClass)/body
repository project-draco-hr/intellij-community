{
  for (  PyClass s : node.iterateAncestorClasses()) {
    if (!PyNames.OBJECT.equals(s.getName()) && !PyNames.FAKE_OLD_BASE.equals(s.getName()) && !node.getName().equals(s.getName())&& s.findMethodByName(PyNames.INIT,false) != null) {
      return true;
    }
  }
  return false;
}
