{
  initFileEditor(component.getProject(),virtualFile,name,new Factory<BasicDomElementComponent>(){
    @Override public BasicDomElementComponent create(){
      return component;
    }
  }
);
  Disposer.register(myFileEditor,component);
  return myFileEditor;
}
