{
  PsiSubstitutor substitutor=inheritor.isInheritor(parentClass,true) ? TypeConversionUtil.getSuperClassSubstitutor(parentClass,inheritor,PsiSubstitutor.EMPTY) : PsiSubstitutor.EMPTY;
  return method.getSignature(substitutor);
}
