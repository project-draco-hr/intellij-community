{
synchronized (myLock) {
    myMap.put(url,map);
    myId2Key.put(url,key);
    if (myMap.size() > 10000) {
      LOG.info("Too many schema definitions registered. Something could go wrong.");
    }
  }
}
