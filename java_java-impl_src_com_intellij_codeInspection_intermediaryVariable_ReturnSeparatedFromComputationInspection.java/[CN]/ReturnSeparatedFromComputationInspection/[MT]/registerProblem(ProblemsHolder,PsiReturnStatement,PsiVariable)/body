{
  String name=variable.getName();
  PsiElement returnElement=returnStatement.getFirstChild();
  holder.registerProblem(returnElement instanceof PsiKeyword ? returnElement : returnStatement,InspectionsBundle.message("inspection.return.separated.from.computation.descriptor",name),new VariableFix(name));
}
