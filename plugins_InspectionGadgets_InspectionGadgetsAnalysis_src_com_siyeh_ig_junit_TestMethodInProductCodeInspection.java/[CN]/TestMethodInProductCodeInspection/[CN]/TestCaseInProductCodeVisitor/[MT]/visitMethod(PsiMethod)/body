{
  final PsiClass containingClass=method.getContainingClass();
  if (TestUtils.isInTestSourceContent(containingClass) || !TestUtils.isJUnit4TestMethod(method)) {
    return;
  }
  registerMethodError(method);
}
