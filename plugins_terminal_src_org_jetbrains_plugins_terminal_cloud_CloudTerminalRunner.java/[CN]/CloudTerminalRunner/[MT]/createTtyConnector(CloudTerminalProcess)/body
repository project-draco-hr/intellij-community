{
  return new ProcessTtyConnector(process,Charset.defaultCharset()){
    @Override protected void resizeImmediately(){
    }
    @Override public String getName(){
      return "Connector: " + myPipeName;
    }
    @Override public boolean isConnected(){
      return true;
    }
  }
;
}
