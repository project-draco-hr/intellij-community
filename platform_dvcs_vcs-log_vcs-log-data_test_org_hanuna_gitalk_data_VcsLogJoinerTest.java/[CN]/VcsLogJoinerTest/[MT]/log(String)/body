{
  return ContainerUtil.map(Arrays.asList(commits),new Function<String,CommitParents>(){
    @Override public CommitParents fun(    String commit){
      return CommitParser.parseCommitParents(commit);
    }
  }
);
}
