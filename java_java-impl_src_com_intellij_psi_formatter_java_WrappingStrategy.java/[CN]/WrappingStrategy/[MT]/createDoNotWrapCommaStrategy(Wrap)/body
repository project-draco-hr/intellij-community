{
  return new WrappingStrategy(wrap){
    @Override protected boolean shouldWrap(    final IElementType type){
      return type != JavaTokenType.COMMA && type != JavaTokenType.SEMICOLON;
    }
  }
;
}
