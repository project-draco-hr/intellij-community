{
  return new LanguageConsoleImpl(project,title,GroovyLanguage.INSTANCE){
    @NotNull @Override protected PsiFile createFile(    @NotNull LightVirtualFile virtualFile,    @NotNull Document document,    @NotNull Project project){
      return new GroovyCodeFragment(getProject(),virtualFile);
    }
  }
;
}
