{
  int bigSize=0;
  for (  List<String> list : map.valu < caret > es()) {
    int size=list.size();
    if (size > 10) {
      bigSize=size * 2;
      break;
    }
  }
  System.out.println(bigSize);
}
