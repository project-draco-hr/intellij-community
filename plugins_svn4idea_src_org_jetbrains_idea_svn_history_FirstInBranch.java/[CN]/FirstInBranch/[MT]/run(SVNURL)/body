{
  try {
    run(url,new Consumer<CopyData>(){
      @Override public void consume(      CopyData data){
        if (data != null) {
          myConsumer.set(data);
        }
      }
    }
);
  }
 catch (  VcsException e) {
    LOG.info(e);
    myConsumer.fail(e);
  }
}
