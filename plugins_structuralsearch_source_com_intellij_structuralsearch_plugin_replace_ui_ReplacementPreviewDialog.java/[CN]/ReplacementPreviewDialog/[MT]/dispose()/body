{
  DaemonCodeAnalyzer.getInstance(project).setHighlightingEnabled(PsiDocumentManager.getInstance(project).getPsiFile(replacement.getDocument()),true);
  EditorFactory.getInstance().releaseEditor(replacement);
  removeHilighter();
  super.dispose();
}
