{
  return StringUtil.join(authors,new Function<CourseInfo.Instructor,String>(){
    @Override public String fun(    CourseInfo.Instructor instructor){
      return instructor.getName();
    }
  }
,", ");
}
