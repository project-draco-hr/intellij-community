{
  PyMarkedFunction callee=PyCallExpressionHelper.resolveCallee(this);
  if (callee == null)   return null;
  if (!hasArgumentList()) {
    callee.getFlags().add(Flag.IMPLICIT_FIRST_ARG);
  }
  return callee;
}
