{
  int count=0;
  for (  T item : items) {
    if (item != null)     count++;
  }
  List<T> result=new ArrayList<T>(count);
  for (  T item : items) {
    if (item != null)     result.add(item);
  }
  return result;
}
