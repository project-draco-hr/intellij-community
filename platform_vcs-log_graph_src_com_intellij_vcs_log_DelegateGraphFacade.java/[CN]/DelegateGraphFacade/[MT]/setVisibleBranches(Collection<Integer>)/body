{
  boolean needUpdate;
  if (heads == null) {
    needUpdate=myHeads != null;
    myHeads=null;
  }
 else {
    needUpdate=true;
    myHeads=new HashSet<Integer>(heads);
  }
  if (needUpdate)   updateVisibleGraph();
}
