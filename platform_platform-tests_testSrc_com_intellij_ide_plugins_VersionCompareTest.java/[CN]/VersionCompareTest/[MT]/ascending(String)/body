{
  assertTrue(versions.length > 1);
  for (int i=0; i < versions.length - 1; i++) {
    assertTrue(versions[i] + " is greater then " + versions[i + 1],compareVersions(versions[i],versions[i + 1]) < 0);
  }
}
