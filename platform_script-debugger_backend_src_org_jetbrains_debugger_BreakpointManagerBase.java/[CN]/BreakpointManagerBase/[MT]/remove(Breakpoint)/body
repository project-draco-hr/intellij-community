{
  @SuppressWarnings("unchecked") T b=(T)breakpoint;
  boolean existed=breakpoints.remove(b);
  if (existed) {
    breakpointDuplicationByTarget.remove(b);
  }
  return !existed || !b.isVmRegistered() ? ActionCallback.DONE : doClearBreakpoint(b);
}
