{
  if (cell == null) {
    return null;
  }
  if (cell.getClass() == EdgeCell.class) {
    Edge edge=((EdgeCell)cell).getEdge();
    if (edge.getType() == Edge.Type.HIDE_PIECE) {
      return edge;
    }
  }
  return null;
}
