{
  ReadOnlyList<Edge> upEdges=node.getUpEdges();
  if (upEdges.size() == 1) {
    Node upNode=upEdges.get(0).getUpNode();
    if (isSimpleNode(upNode)) {
      return upNode;
    }
  }
  return null;
}
