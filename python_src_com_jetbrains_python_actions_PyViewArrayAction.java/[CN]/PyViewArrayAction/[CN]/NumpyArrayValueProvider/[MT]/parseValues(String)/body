{
  if (rawValue != null && rawValue.startsWith("[")) {
    int dimension=0;
    while (rawValue.charAt(dimension) == '[') {
      dimension+=1;
    }
    if (dimension > 2) {
    }
  }
  return null;
}
