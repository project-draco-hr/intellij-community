{
  ModuleChooserUtil.selectModule(project,ModuleChooserUtil.filterGroovyCompatibleModules(Arrays.asList(ModuleManager.getInstance(project).getModules()),APPLICABLE_MODULE),MODULE_VERSION,consumer);
}
