{
  doTest("import java.util.Optional;" + "class X {" + "  void m() {"+ "    Optional<String> o = Optional.empty();"+ "    while (!o.isPresent()){"+ "      o = Optional.of(\"\");"+ "    }"+ "    o.get();"+ "  }"+ "}");
}
