{
  List<String> result=new ArrayList<>();
  for (  String[] arr : li < caret > st) {
    if (arr.length > 2) {
      for (      String str : arr) {
        String trimmed=str.trim();
        if (!trimmed.isEmpty()) {
          result.add(trimmed);
        }
      }
    }
  }
}
