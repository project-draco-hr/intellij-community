{
  if (request.getVirtualFile() instanceof HttpVirtualFile) {
    return true;
  }
  FileViewProvider viewProvider=request.getFile().getViewProvider();
  return viewProvider.isPhysical() && !(request.getVirtualFile() instanceof LightVirtualFile) && isMyLanguage(viewProvider.getBaseLanguage());
}
