{
  return version == null ? null : new org.jetbrains.idea.svn.conflict.ConflictVersion(SVNURL.parseURIEncoded(version.myRepoUrl),version.myPathInRepo,parseRevision(version.myRevision),NodeKind.from(version.myKind));
}
