{
  if (lastParent instanceof PsiExpressionList)   return true;
  if (lastParent instanceof PsiJavaCodeReferenceElement && lastParent.getParent() == this && lastParent == getBaseClassReference()) {
    return true;
  }
  return super.processDeclarations(processor,state,lastParent,place);
}
