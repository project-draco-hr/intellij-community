{
  if (scaleFactor == 1f) {
    return this;
  }
  if (!isValid())   getRealIcon();
  Icon icon=myScaledIconsCache.getScaledIcon(scaleFactor);
  if (icon != null) {
    return icon;
  }
  return this;
}
