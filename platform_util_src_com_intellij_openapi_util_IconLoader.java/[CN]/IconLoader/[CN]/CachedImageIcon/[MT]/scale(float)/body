{
  if (scale == 1f) {
    return this;
  }
  super.scale(scale);
  if (!isValid())   getRealIcon();
  Icon icon=myScaledIconsCache.getScaledIcon(scale);
  if (icon != null) {
    return icon;
  }
  return this;
}
