{
  if (file.getIOFile().getAbsolutePath().equals(beforeFile.getIOFile().getAbsolutePath()) || file.isUnder(beforeFile,false)) {
    debug("Found subsequent deletion for file " + file);
    return true;
  }
  return false;
}
