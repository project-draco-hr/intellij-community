{
  if (GIST_ID != null) {
    try {
      GithubUtil.deleteGist(GithubUtil.getAuthData(),GIST_ID);
    }
 catch (    IOException e) {
      System.err.println(e.getMessage());
    }
    GIST=null;
    GIST_ID=null;
  }
}
