{
  final List<String> ownSlots=cls.getOwnSlots();
  return ownSlots == null || ownSlots.contains(PyNames.DICT) || (LanguageLevel.forElement(target).isPy3K() && cls.equals(declaringClass) && ownSlots.contains(target.getName()));
}
