{
  if (myPushShouldFail.value(repository)) {
    return new GitCommandResult(false,128,Collections.singletonList("Failed to push to " + remote.getName()),Collections.<String>emptyList(),null);
  }
  return super.push(repository,remote,spec,force,updateTracking,tagMode,listeners);
}
