{
  if (myPushShouldFail.value(repository)) {
    return new GitCommandResult(false,128,Collections.singletonList("Failed to push to " + target.getName()),Collections.<String>emptyList(),null);
  }
  return super.push(repository,source,target,force,updateTracking,tagMode,listeners);
}
