{
  doTest("import java.net.*;" + "import java.io.*;" + "class X {"+ "  void m(SocketInputStream ss) throws IOException {"+ "    ss./*'FileChannel' should be opened in front of a 'try' block and closed in the corresponding 'finally' block*/getChannel/**/();"+ "  }"+ "}");
}
