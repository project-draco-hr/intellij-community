{
  doTest("import java.net.*;" + "import java.io.*;" + "class X {"+ "  void m(SocketInputStream ss) throws IOException {"+ "    final Closeable channel = ss.getChannel();"+ "    try {"+ "      System.out.println();"+ "    } finally {"+ "      channel.close();"+ "    }"+ "  }"+ "}");
}
