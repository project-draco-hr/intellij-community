{
  String name=user.getName();
  String email=user.getEmail();
  String result=name;
  if (!email.isEmpty()) {
    if (!name.isEmpty()) {
      result+=" <";
    }
    result+=email;
    if (!name.isEmpty()) {
      result+=">";
    }
  }
  return result;
}
