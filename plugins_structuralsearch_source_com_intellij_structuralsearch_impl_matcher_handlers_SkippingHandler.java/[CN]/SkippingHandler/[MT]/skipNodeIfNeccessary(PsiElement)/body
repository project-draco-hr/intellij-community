{
  if (element == null) {
    return null;
  }
  final PsiElement onlyChild=getOnlyChildFromDescriptor(element);
  if (onlyChild != null) {
    return onlyChild;
  }
  return getOnlyNonLexicalChild(element);
}
