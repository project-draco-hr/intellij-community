{
switch (ref.getType()) {
case LOCAL_BRANCH:
    return Color.orange;
case REMOTE_BRANCH:
  return Color.cyan;
case TAG:
return Color.magenta;
case STASH:
return Color.red;
default :
throw new IllegalArgumentException();
}
}
