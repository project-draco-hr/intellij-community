{
  if (!hasNameMappings()) {
    return null;
  }
  for (  Map.Entry<String,String> entry : rawNameToSource.entrySet()) {
    if (entry.getValue().equals(name)) {
      return entry.getKey();
    }
  }
  return null;
}
