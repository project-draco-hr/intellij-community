{
  if (myLocation == null) {
    return false;
  }
  if (ScreenUtil.isVisible(myLocation)) {
    return true;
  }
  if (mySize == null) {
    return false;
  }
  return ScreenUtil.isVisible(new Rectangle(myLocation,mySize));
}
