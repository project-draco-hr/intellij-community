{
  int max=0;
  for (  String path : paths) {
    String[] splits=path.split("/");
    int count=0;
    for (    String split : splits) {
      if (split.equals("..")) {
        count++;
      }
    }
    if (count > max) {
      max=count;
    }
  }
  return max;
}
