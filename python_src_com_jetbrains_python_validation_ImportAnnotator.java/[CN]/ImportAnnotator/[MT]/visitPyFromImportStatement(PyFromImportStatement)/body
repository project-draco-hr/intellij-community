{
  if (node.isStarImport()) {
    if (node.getContainingElement(PyFunction.class) != null || node.getContainingElement(PyClass.class) != null) {
      getHolder().createWarningAnnotation(node,"import * only allowed at module level");
    }
  }
}
