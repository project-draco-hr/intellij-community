{
  if (isDone()) {
    return false;
  }
 else {
synchronized (LOCK) {
      if (myDone)       return false;
      if (mySlaves == null)       mySlaves=new ArrayList<SlaveFutureTask>();
      mySlaves.add(slave);
      return true;
    }
  }
}
