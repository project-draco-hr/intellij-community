{
  if (Registry.is("debugger.watches.in.variables") || getPanel().isShowing() || ApplicationManager.getApplication().isUnitTestMode()) {
    myRebuildNeeded=false;
  }
 else {
    myRebuildNeeded=true;
    return;
  }
  super.processSessionEvent(event);
}
