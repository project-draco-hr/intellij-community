{
  if (tags.length == 0) {
    return Collections.emptyList();
  }
  return ContainerUtil.findAll(tags,new Condition<XmlTag>(){
    @Override public boolean value(    XmlTag childTag){
      return isNameSuitable(name,childTag,file);
    }
  }
);
}
