{
  if (set1 == null || set1.isEmpty()) {
    return set2;
  }
 else   if (set2.isEmpty()) {
    return set1;
  }
 else {
    Set<Key> set=new HashSet<Key>();
    set.addAll(set1);
    set.addAll(set2);
    return set;
  }
}
