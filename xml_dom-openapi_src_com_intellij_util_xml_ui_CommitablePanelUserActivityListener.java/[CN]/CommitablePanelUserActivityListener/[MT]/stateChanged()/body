{
  if (myApplying)   return;
  cancel();
  cancelAllRequests();
  myAlarm.addRequest(new Runnable(){
    @Override public void run(){
      myApplying=true;
      cancel();
      try {
        applyChanges();
      }
  finally {
        myApplying=false;
      }
    }
  }
,717);
}
