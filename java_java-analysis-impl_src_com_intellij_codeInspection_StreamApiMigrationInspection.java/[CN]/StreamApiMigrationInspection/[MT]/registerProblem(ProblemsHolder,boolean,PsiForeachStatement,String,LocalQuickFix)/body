{
  PsiExpression iteratedValue=statement.getIteratedValue();
  LOG.assertTrue(iteratedValue != null);
  holder.registerProblem(statement,getRange(statement,isOnTheFly).shiftRight(-statement.getTextOffset()),"Can be replaced with '" + methodName + "' call",fixes);
}
