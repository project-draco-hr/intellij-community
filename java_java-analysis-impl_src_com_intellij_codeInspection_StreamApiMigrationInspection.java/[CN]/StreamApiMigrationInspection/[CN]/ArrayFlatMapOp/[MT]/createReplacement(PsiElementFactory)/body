{
  PsiExpression replacement=factory.createExpressionFromText("java.util.Arrays.stream(" + myExpression.getText() + ")",myExpression);
  return ".flatMap(" + compoundLambdaOrMethodReference(myVariable,replacement,"java.util.function.Function",new PsiType[]{myVariable.getType(),replacement.getType()}) + ")";
}
