{
  PsiExpression expression=myNegated ? factory.createExpressionFromText(BoolUtils.getNegatedExpressionText(myExpression),myExpression) : myExpression;
  return ".filter(" + createLambda(myVariable,expression) + ")";
}
