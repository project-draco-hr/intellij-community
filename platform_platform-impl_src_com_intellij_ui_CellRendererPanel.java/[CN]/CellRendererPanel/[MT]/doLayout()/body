{
  if (getComponentCount() != 1)   return;
  Insets insets=getInsets();
  getComponent(0).setBounds(insets.left,insets.top,getWidth() - insets.left - insets.right,getHeight() - insets.top - insets.bottom);
}
