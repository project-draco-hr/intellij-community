{
  return ContainerUtil.exists(usages,new Condition<UsageInfo>(){
    @Override public boolean value(    UsageInfo info){
      final PsiElement element=info.getElement();
      if (element == null) {
        return false;
      }
      return !belongsToFunction(element) && info.getFile() != targetFile;
    }
  }
);
}
