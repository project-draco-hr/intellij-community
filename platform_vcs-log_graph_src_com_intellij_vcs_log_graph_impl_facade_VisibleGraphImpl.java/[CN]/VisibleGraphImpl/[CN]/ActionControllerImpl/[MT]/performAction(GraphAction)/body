{
  LinearGraphController.LinearGraphAnswer answer=myGraphController.performLinearGraphAction(convert(graphAction));
  if (answer.getGraphChanges() != null)   graphWasChanged();
  return convert(answer);
}
