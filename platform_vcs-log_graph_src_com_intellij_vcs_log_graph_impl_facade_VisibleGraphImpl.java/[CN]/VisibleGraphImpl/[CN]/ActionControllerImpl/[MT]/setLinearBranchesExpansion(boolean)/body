{
  LinearGraphController.LinearGraphAnswer answer;
  if (collapse)   answer=myGraphController.performLinearGraphAction(LinearGraphActionImpl.COLLAPSE);
 else   answer=myGraphController.performLinearGraphAction(LinearGraphActionImpl.EXPAND);
  if (answer.graphWasChanged())   graphWasChanged();
}
