{
  FakeCommitParents existing=fakeCommits.get(oldHash);
  if (existing != null)   return existing;
  FakeCommitParents fake=new FakeCommitParents(newParent,new RebaseCommand(RebaseCommand.RebaseCommandKind.PICK,oldHash));
  fakeCommits.put(oldHash,fake);
  return fake;
}
