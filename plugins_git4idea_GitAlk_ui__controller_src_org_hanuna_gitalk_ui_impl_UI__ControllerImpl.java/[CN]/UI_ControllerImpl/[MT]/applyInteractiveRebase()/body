{
  if (rebaseDelegate.resultRef == null) {
    return;
  }
  getGitActionHandler().interactiveRebase(rebaseDelegate.subjectRef,rebaseDelegate.base,getCallback(),rebaseDelegate.getRebaseCommands());
  rebaseDelegate.reset();
  refresh();
}
