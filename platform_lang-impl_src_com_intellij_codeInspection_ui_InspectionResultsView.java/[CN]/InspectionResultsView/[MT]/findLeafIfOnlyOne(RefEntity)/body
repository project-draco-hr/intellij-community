{
  RefEntity current=entity;
  while (current != null) {
    final List<RefEntity> children=current.getChildren();
    if (children.size() > 1) {
      return null;
    }
    if (children.isEmpty()) {
      return current;
    }
    current=children.get(0);
  }
  return null;
}
