{
  final SearchInitializer initializer=createInitializer(targetQName,resolver,searchService,excludedParamsTypesQNames);
  return search(searchService,initializer,contextQNames,pathMaximalLength,maxResultSize,resolver,targetQName,excludedParamsTypesQNames,context);
}
