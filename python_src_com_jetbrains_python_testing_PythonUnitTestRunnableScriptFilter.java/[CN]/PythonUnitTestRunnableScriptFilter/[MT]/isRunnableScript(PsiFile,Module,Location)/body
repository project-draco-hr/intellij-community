{
  return script instanceof PyFile && PythonUnitTestUtil.getTestCaseClassesFromFile((PyFile)script).size() > 0 && !isIfNameMain(location) && TestRunnerService.getInstance(script.getProject()).getProjectConfiguration().equals(PythonTestConfigurationsModel.PYTHONS_UNITTEST_NAME);
}
