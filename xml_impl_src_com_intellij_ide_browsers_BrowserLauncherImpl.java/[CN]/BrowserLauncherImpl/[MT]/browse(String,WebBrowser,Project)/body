{
  if (Registry.is("ide.built.in.web.server.activatable",false) && BuiltInServerManager.getInstance().isOnBuiltInWebServer(Urls.parse(url,false))) {
    PropertiesComponent.getInstance().setValue("ide.built.in.web.server.active",true);
  }
  super.browse(url,browser,project);
}
