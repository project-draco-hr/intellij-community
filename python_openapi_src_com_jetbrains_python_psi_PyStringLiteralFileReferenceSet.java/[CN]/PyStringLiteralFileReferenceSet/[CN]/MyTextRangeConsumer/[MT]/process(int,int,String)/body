{
  if ("\\".equals(value) || "/".equals(value)) {
    addReference(startOffset);
  }
 else {
    if (myStartOffset == -1) {
      myStartOffset=startOffset;
    }
    myEndOffset=endOffset;
    myItem.append(value);
  }
  return true;
}
