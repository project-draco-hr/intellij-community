{
  if (myStartOffset != -1) {
    final FileReference ref=myFileReferenceSet.createFileReference(new TextRange(myStartOffset,startOffset),myIndex++,myItem.toString());
    myReferenceList.add(ref);
    myStartOffset=-1;
    myItem.setLength(0);
  }
}
