{
  if (isEnabled() != enabled) {
    super.setEnabled(enabled);
    myIsViewer=!enabled;
    EditorEx editor=myEditor;
    if (editor == null) {
      return;
    }
    releaseEditor(editor);
    initEditor();
    revalidate();
  }
}
