{
  if (myStorageData == null) {
    VirtualFile dir=getVirtualFile();
    if (dir == null || !dir.exists()) {
      return false;
    }
  }
  if (reloadData) {
    myStorageData=null;
  }
  return true;
}
