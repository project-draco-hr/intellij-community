{
  doTest("import java.math.*;" + "class X {" + "  void m(BigDecimal value) {"+ "    value./*Call to 'BigDecimal.divide()' can use 'RoundingMode' enum constant*/divide/**/(value, BigDecimal.ROUND_HALF_DOWN);"+ "    value./*Call to 'BigDecimal.divide()' can use 'RoundingMode' enum constant*/divide/**/(value, 2, 7);"+ "    value.divide(value, 2, RoundingMode.UNNECESSARY);"+ "    value./*Call to 'BigDecimal.setScale()' can use 'RoundingMode' enum constant*/setScale/**/(2, 4);"+ "    value.setScale(2, RoundingMode.CEILING);"+ "  }"+ "}");
}
