{
  String error=processLastError(resetPassword,lastError);
  return CredentialPromptDialog.askPassword(myProject,GitBundle.getString("ssh.ask.passphrase.title"),"Password for the SSH key \"" + PathUtil.getFileName(keyPath) + "\":",CredentialAttributes(GitSSHGUIHandler.class,"PASSPHRASE:" + keyPath),resetPassword,error);
}
