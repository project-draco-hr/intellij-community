{
  String error=processLastError(resetPassword,lastError);
  return CredentialPromptDialog.askPassword(myProject,GitBundle.getString("ssh.password.title"),GitBundle.message("ssh.password.message",username),CredentialAttributes(GitSSHGUIHandler.class,"PASSWORD:" + username),resetPassword,error);
}
