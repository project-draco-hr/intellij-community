{
  IElementType type=myBuilder.getTokenType();
  if (type == PyTokenTypes.INDENT) {
    myBuilder.advanceLexer();
    type=myBuilder.getTokenType();
  }
  if (type == PyDocstringTokenTypes.WELCOME) {
    myBuilder.advanceLexer();
  }
  super.parseStatement(scope);
}
