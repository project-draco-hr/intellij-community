{
  Point point=new Point(e.getX() - 8,e.getY() - 8);
  return point.distance(getSearchIconCoord()) <= 8 ? SearchAction.POPUP : hasText() && point.distance(getClearIconCoord()) <= 8 ? SearchAction.CLEAR : null;
}
