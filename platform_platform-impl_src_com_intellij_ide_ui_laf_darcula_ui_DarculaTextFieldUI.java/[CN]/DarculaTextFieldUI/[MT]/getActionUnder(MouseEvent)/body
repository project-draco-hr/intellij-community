{
  int off=JBUI.scale(8);
  Point point=new Point(e.getX() - off,e.getY() - off);
  return point.distance(getSearchIconCoord()) <= off ? SearchAction.POPUP : hasText() && point.distance(getClearIconCoord()) <= off ? SearchAction.CLEAR : null;
}
