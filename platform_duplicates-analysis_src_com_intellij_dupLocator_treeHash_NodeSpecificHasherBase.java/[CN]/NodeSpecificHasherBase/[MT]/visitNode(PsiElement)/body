{
  final Language language=node.getLanguage();
  if ((myForIndexing || mySettings.SELECTED_PROFILES.contains(language.getDisplayName())) && myDuplicatesProfile.isMyLanguage(language)) {
    myTreeHasher.hash(node,this);
  }
}
