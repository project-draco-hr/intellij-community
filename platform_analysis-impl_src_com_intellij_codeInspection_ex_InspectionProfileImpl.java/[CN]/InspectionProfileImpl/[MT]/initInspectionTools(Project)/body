{
  if (myInitialized || (ApplicationManager.getApplication().isUnitTestMode() && !INIT_INSPECTIONS)) {
    return;
  }
synchronized (myLock) {
    if (!myInitialized) {
      initialize(project);
    }
  }
}
