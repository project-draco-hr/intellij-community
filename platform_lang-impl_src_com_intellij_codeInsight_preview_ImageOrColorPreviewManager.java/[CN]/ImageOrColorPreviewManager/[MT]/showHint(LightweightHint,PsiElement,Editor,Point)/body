{
  if (element != myElement && element.isValid()) {
    hideCurrentHintIfAny();
    setCurrentHint(hint,element);
    HintManagerImpl.getInstanceImpl().showEditorHint(hint,editor,getHintPosition(hint,editor,getLogicalPosition(point),HintManager.RIGHT_UNDER),HINT_HIDE_FLAGS,0,false);
  }
}
