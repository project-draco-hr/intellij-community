{
  if (!PyUtil.inSameFile(builtin,element)) {
    registerProblem(problemElement,String.format("Shadows built-in name '%s'",name),ProblemHighlightType.GENERIC_ERROR_OR_WARNING,null,new PyRenameElementQuickFix(),new PyIgnoreBuiltinQuickFix(name));
  }
}
