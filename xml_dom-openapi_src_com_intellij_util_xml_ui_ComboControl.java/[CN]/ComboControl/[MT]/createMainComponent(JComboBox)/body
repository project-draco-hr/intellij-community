{
  return initComboBox(boundedComponent == null ? new JComboBox() : boundedComponent,new Condition<String>(){
    @Override public boolean value(    final String object){
      return isValidValue(object);
    }
  }
);
}
