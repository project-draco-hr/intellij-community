{
  int count=0;
  for (  Set<String> element : nested) {
    if (element != null) {
      for (      String str : eleme < caret > nt) {
        if (str.startsWith("xyz")) {
          count++;
        }
      }
    }
  }
}
