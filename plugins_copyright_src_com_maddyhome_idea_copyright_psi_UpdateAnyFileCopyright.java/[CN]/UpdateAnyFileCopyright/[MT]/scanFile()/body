{
  List<PsiComment> comments=SyntaxTraverser.psiTraverser(getFile()).withTraversal(TreeTraversal.LEAVES_DFS).traverse().takeWhile(Conditions.instanceOf(PsiComment.class,PsiWhiteSpace.class)).filter(PsiComment.class).toList();
  checkComments(ContainerUtil.getLastItem(comments),true,comments);
}
