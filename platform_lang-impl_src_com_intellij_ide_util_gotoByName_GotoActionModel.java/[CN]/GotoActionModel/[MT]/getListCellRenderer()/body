{
  return new GotoActionListCellRenderer(new Function<OptionDescription,String>(){
    @NotNull @Override public String fun(    @NotNull OptionDescription description){
      return getGroupName(description);
    }
  }
);
}
