{
  if (!ourIsPresent) {
    Notifications.Bus.notify(new Notification(SvnVcs.getInstance(project).getDisplayName(),"Subversion: JavaHL problem",ourProblemDescription + " Acceleration is not available.",NotificationType.ERROR),project);
  }
}
