{
  String fontName=getNonExistingFontName();
  myPreferences.register(fontName,25);
  checkState(Arrays.asList(fontName),Arrays.asList(FontPreferences.DEFAULT_FONT_NAME),FontPreferences.DEFAULT_FONT_NAME,fontName,25);
}
