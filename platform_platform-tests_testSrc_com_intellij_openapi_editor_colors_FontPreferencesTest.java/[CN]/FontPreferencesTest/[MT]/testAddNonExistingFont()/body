{
  String fontName=getNonExistingFontName();
  myPreferences.addFontFamily(fontName);
  checkState(Arrays.asList(fontName),Arrays.asList(FontPreferences.DEFAULT_FONT_NAME),FontPreferences.DEFAULT_FONT_NAME,fontName,null);
}
