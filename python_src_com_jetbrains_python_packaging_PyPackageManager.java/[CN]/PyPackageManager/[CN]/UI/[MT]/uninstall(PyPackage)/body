{
  run(new ExternalRunnable(){
    @Override public void run(    @NotNull Sdk sdk) throws PyExternalProcessException {
      PyPackageManager.getInstance(sdk).uninstall(pkg);
    }
  }
,"Uninstalling package","Package uninstalled successfully",String.format("Package '%s' uninstalled",pkg.getName()),"Uninstall package failed");
}
