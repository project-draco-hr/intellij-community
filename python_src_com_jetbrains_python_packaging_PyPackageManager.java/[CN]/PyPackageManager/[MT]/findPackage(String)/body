{
  try {
    for (    PyPackage pkg : getPackages()) {
      if (name.equals(pkg.getName())) {
        return pkg;
      }
    }
  }
 catch (  PyExternalProcessException ignored) {
  }
  return null;
}
