{
  if ((access & Opcodes.ACC_BRIDGE) != 0)   return false;
  if ((access & Opcodes.ACC_ABSTRACT) != 0)   return false;
  if ("<clinit>".equals(name) || "<init>".equals(name))   return false;
  if (myEnum && isDefaultEnumMethod(name,desc,signature,myClassName)) {
    return false;
  }
  return true;
}
