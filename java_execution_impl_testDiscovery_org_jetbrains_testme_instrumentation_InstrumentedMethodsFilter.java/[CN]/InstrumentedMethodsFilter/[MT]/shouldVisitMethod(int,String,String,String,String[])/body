{
  if ((access & Opcodes.ACC_BRIDGE) != 0)   return false;
  if ((access & Opcodes.ACC_ABSTRACT) != 0)   return false;
  if ("<clinit>".equals(name) || ((access & Opcodes.ACC_SYNTHETIC) != 0 && name.startsWith("access$")) || name.equals("<init>") && signature != null && signature.equals("()V")) {
    return false;
  }
  if (myEnum && isDefaultEnumMethod(name,desc,signature,myClassName)) {
    return false;
  }
  return true;
}
