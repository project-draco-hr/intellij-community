{
  BuildInfo latestBuild=channel.getLatestBuild();
  return latestBuild != null && latestBuild.getNumber() != null && !myUpdateSettings.getIgnoredBuildNumbers().contains(latestBuild.getNumber().asStringWithoutProductCode()) && myCurrentBuild.compareTo(latestBuild.getNumber()) < 0;
}
