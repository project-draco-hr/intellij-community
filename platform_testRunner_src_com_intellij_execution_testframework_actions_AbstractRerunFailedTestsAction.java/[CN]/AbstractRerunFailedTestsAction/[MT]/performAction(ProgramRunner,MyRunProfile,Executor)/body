{
  try {
    runner.execute(new ExecutionEnvironmentBuilder(myEnvironment).runner(runner).executor(executor).runProfile(profile).build());
  }
 catch (  ExecutionException e1) {
    LOG.error(e1);
  }
 finally {
    profile.clear();
  }
}
