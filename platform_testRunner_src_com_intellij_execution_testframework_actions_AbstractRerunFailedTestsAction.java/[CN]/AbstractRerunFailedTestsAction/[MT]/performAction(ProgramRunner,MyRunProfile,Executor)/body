{
  try {
    new ExecutionEnvironmentBuilder(myEnvironment).runner(runner).executor(executor).runProfile(profile).buildAndExecute();
  }
 catch (  ExecutionException e1) {
    LOG.error(e1);
  }
 finally {
    profile.clear();
  }
}
