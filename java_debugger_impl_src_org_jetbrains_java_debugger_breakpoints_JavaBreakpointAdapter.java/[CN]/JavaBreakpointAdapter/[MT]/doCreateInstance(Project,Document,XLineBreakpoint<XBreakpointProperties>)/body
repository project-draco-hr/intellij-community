{
  LineBreakpoint lineBreakpoint=new LineBreakpoint(project,((XLineBreakpointImpl)breakpoint).getHighlighter()){
    @Override protected void setEditorFilter(    RangeHighlighter highlighter){
    }
  }
;
  lineBreakpoint.init();
  return lineBreakpoint;
}
