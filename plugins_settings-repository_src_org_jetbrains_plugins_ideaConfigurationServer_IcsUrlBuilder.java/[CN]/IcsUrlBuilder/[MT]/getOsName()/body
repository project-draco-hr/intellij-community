{
  if (SystemInfo.isWindows) {
    return "windows";
  }
  if (SystemInfo.isOS2) {
    return "os2";
  }
  if (SystemInfo.isMac) {
    return "mac";
  }
  if (SystemInfo.isLinux) {
    return "linux";
  }
  if (SystemInfo.isOS2) {
    return "os2";
  }
  if (SystemInfo.isFreeBSD) {
    return "freebsd";
  }
  if (SystemInfo.isUnix) {
    return "unix";
  }
  return "unknown";
}
