{
  myProvider=new CompositeDataProvider(new TestDataProvider<>(DataType.ONE,1,3,5),new TestDataProvider<>(DataType.TWO,2,4));
  myProvider.advance(6);
  assertFalse(myProvider.hasData());
  assertFalse(myProvider.next());
}
