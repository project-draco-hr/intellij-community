{
  myFixture.addClass("class B {" + "  void f(A a) {" + "    a.r = () -> {};"+ "  }"+ "}");
  myFixture.addClass("public class A {" + "  public I r;" + "}");
  for (int i=0; i < JavaFunctionalExpressionSearcher.SMART_SEARCH_THRESHOLD + 1; i++) {
    myFixture.addClass("class B" + i + " { {Runnable r = () -> {};}}");
  }
  doTestOneExpression();
}
