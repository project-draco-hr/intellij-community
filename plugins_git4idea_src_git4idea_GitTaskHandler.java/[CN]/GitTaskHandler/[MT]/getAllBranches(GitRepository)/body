{
  return ContainerUtil.map(repository.getBranches().getLocalBranches(),new Function<GitLocalBranch,String>(){
    @Override public String fun(    GitLocalBranch branch){
      return branch.getName();
    }
  }
);
}
