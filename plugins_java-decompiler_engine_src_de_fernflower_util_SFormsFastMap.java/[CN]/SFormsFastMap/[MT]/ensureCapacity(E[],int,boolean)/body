{
  int minsize=size;
  if (!exact) {
    minsize=2 * arr.length / 3 + 1;
    if (size > minsize) {
      minsize=size;
    }
  }
  E[] arrnew=(E[])new Object[minsize];
  System.arraycopy(arr,0,arrnew,0,arr.length);
  return arrnew;
}
