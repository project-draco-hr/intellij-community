{
  int index=0;
  if (key < 0) {
    index=2;
    key=-key;
  }
 else   if (key >= VarExprent.STACK_BASE) {
    index=1;
    key-=VarExprent.STACK_BASE;
  }
  E[] arr=lstElements.get(index);
  if (key < arr.length) {
    return arr[key];
  }
  return null;
}
