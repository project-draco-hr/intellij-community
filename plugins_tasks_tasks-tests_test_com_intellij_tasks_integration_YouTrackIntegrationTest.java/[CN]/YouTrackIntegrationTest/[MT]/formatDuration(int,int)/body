{
  final String spentTime;
  if (hours == 0) {
    spentTime=minutes + "m";
  }
 else   if (minutes == 0) {
    spentTime=hours + "h";
  }
 else {
    spentTime=String.format("%dh%dm",hours,minutes);
  }
  return spentTime;
}
