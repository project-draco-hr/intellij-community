{
  PsiElement prevElement=original.getPrevSibling();
  if (prevElement != null) {
    if (prevElement.getNode().getElementType() != PyTokenTypes.LBRACKET)     return result.withPrefixMatcher(findPrefix(prevElement));
  }
  PsiElement parentElement=original.getParent();
  if (parentElement != null) {
    if (parentElement instanceof PyStringLiteralExpression)     return result.withPrefixMatcher(findPrefix(parentElement));
  }
  return result;
}
