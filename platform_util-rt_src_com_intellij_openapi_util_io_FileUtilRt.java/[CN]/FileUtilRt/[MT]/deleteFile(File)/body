{
  Boolean result=doIOOperation(new RetriableIOOperation<Boolean,RuntimeException>(){
    @Override public Boolean execute(    boolean lastAttempt){
      return file.delete() || !file.exists();
    }
  }
);
  return Boolean.TRUE.equals(result);
}
