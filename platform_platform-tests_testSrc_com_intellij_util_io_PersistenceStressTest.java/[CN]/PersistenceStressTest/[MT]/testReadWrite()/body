{
  myThreadPool=Executors.newFixedThreadPool(5);
  Future<Boolean> future=submit(myMap);
  Future<Boolean> submit=submit(myMap1);
  while (!future.isDone() || !submit.isDone()) {
    Thread.sleep(100);
  }
  assertTrue(future.get());
  assertTrue(submit.get());
}
