{
  return myThreadPool.submit(new Callable<Boolean>(){
    @Override public Boolean call() throws Exception {
      try {
        return doTask(map);
      }
 catch (      IOException e) {
        e.printStackTrace();
        return false;
      }
    }
  }
);
}
