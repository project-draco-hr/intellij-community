{
  if ((start < 0) || (end > nodeLength()) || (start > end)) {
    throw new IndexOutOfBoundsException();
  }
  for (int i=start; i < end; i++) {
    byte b=data[i];
    dest[destPos++]=(char)b;
  }
}
