{
  return ContainerUtil.map2List(myDeclarations,new Function<Locator,PsiElement>(){
    @Override public PsiElement fun(    Locator locator){
      return myElementDescriptor.getDeclaration(locator);
    }
  }
);
}
