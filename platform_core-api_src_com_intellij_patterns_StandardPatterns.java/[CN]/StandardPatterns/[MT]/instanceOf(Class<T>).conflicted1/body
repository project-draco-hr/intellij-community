{
  ElementPattern[] patterns=ContainerUtil.map(classes,new Function<Class<T>,ElementPattern>(){
    @Override public ElementPattern fun(    Class<T> aClass){
      return instanceOf(aClass);
    }
  }
,new ElementPattern[0]);
  return or(patterns);
}
