{
  ApplicationImpl app=(ApplicationImpl)ApplicationManager.getApplication();
  boolean save=app.isDoNotSave();
  app.doNotSave(false);
  try {
    getProject().save();
  }
  finally {
    app.doNotSave(save);
  }
}
