{
  Boolean valid=myValid;
  if (valid != null)   return valid;
synchronized (this) {
    valid=myValid;
    if (valid == null) {
      valid=calculateIsValid();
      myValid=valid;
    }
    return valid;
  }
}
