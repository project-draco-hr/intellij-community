{
  if (getSelectedTopLevelSymbols().isEmpty()) {
    CommonRefactoringUtil.showErrorMessage(RefactoringBundle.message("error.title"),RefactoringBundle.message("no.members.selected"),PyMoveModuleMembersProcessor.REFACTORING_ID,myProject);
  }
 else {
    close(OK_EXIT_CODE);
  }
}
