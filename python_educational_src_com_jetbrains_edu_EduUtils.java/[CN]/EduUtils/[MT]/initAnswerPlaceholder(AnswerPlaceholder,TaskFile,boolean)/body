{
  if (!isRestarted) {
    placeholder.setInitialState(new AnswerPlaceholder.MyInitialState(placeholder.getLine(),placeholder.getLength(),placeholder.getStart()));
  }
  placeholder.setTaskFile(file);
}
