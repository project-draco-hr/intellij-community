{
synchronized (files) {
    return new State(ContainerUtil.filter(ContainerUtil.map(files,new Function<VirtualFile,String>(){
      @Override public String fun(      VirtualFile file){
        return file.getCanonicalPath();
      }
    }
),Condition.NOT_NULL));
  }
}
