{
  String blindness=Registry.stringValue("color.blindness");
  return !StringUtil.isEmpty(blindness) ? readColor(colorElement,blindness,OS_VALUE_PREFIX,VALUE_ELEMENT) : readColor(colorElement,OS_VALUE_PREFIX,VALUE_ELEMENT);
}
