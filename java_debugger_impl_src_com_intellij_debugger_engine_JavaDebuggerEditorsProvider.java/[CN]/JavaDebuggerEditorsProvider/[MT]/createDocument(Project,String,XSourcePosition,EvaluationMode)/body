{
  text=text.trim();
  DefaultCodeFragmentFactory factory=DefaultCodeFragmentFactory.getInstance();
  JavaCodeFragment fragment=factory.createCodeFragment(new TextWithImportsImpl(CodeFragmentKind.EXPRESSION,text),null,project);
  return PsiDocumentManager.getInstance(project).getDocument(fragment);
}
