{
  for (  OutOfSourcesChecker checker : OutOfSourcesChecker.EP_NAME.getExtensions()) {
    if (checker.getFileType() == file.getFileType() && checker.isOutOfSources(project,file)) {
      return true;
    }
  }
  return false;
}
