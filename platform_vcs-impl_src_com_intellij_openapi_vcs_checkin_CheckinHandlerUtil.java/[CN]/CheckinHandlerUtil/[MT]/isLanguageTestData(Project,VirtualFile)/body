{
  for (  LanguageTestDataChecker checker : LanguageTestDataChecker.EP_NAME.getExtensions()) {
    if (checker.getFileType() == file.getFileType() && checker.isTestData(project,file)) {
      return true;
    }
  }
  return false;
}
