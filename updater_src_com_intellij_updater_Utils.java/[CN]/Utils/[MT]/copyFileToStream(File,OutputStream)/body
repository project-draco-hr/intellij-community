{
  InputStream in=new BufferedInputStream(new FileInputStream(from));
  try {
    copyStream(in,out);
  }
 catch (  Exception e) {
    Runner.printStackTrace(e);
  }
 finally {
    in.close();
  }
}
