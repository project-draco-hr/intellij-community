{
  Task[] tasks=myRepository.getActiveResponseHandler().parseIssues(RESPONSE);
  List<Task> expected=ContainerUtil.<Task>newArrayList(new GenericTask("5479650606120","Task #1",myRepository),new GenericTask("5202014833559","Task #2",myRepository));
  assertEquals(expected,Arrays.asList(tasks));
}
