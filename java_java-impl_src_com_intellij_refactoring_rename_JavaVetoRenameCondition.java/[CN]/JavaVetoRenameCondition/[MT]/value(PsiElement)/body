{
  if (element instanceof LightMethod) {
    final PsiClass containingClass=((LightMethod)element).getContainingClass();
    if (containingClass != null && containingClass.isEnum())     return true;
  }
  return element instanceof PsiJavaFile && !FileTypeUtils.isInServerPageFile(element) && !JavaProjectRootsUtil.isOutsideJavaSourceRoot((PsiFile)element)&& ((PsiJavaFile)element).getClasses().length > 0;
}
