{
  if ((elements == null && visitor.isLeftLooseMatching()) || elements == elements2) {
    return true;
  }
  return visitor.matchInAnyOrder(elements.getReferenceElements(),(elements2 != null) ? elements2.getReferenceElements() : PsiElement.EMPTY_ARRAY);
}
