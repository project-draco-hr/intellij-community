{
  List<String> result=new ArrayList<>();
  for (  String[] subArr : a < caret > rr) {
    if (subArr != null) {
      for (      String str : subArr) {
        result.add(str);
      }
    }
  }
  return result;
}
