{
  if (StringUtil.isEmpty(getSelectorPath("tasks")) || StringUtil.isEmpty(getSelectorPath("id")) || StringUtil.isEmpty(getSelectorPath("summary"))) {
    throw new Exception("Selectors 'tasks', 'id' and 'summary' are mandatory");
  }
  return doParseIssues(response,max);
}
