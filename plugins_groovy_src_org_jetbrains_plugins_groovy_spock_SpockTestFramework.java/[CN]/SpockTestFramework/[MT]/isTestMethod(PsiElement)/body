{
  if (!(element instanceof GrMethod))   return false;
  return GroovyPsiManager.isInheritorCached(((GrMethod)element).getContainingClass(),SpockUtils.SPEC_CLASS_NAME) && JUnitUtil.getTestMethod(element) != null;
}
