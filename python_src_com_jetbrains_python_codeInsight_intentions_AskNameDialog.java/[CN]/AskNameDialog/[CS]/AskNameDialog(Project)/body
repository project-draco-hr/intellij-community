{
  super(project);
  myAliasTextField.addActionListener(new ActionListener(){
    public void actionPerformed(    ActionEvent actionEvent){
      AskNameDialog.this.setOKActionEnabled(PyNames.isIdentifier(myAliasTextField.getText()));
    }
  }
);
  init();
}
