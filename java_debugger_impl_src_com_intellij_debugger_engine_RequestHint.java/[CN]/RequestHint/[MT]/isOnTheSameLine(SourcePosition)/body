{
  if (myMethodFilter == null) {
    return myPosition.getLine() == locationPosition.getLine();
  }
 else {
    return locationPosition.getLine() >= myMethodFilter.getCallingExpressionLines().getFrom() && locationPosition.getLine() <= myMethodFilter.getCallingExpressionLines().getTo();
  }
}
