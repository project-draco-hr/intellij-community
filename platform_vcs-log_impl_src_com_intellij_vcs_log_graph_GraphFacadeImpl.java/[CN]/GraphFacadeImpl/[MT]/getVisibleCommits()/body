{
  List<NodeRow> nodeRows=myGraphModel.getGraph().getNodeRows();
  return ContainerUtil.map(nodeRows,new Function<NodeRow,Integer>(){
    @Override public Integer fun(    NodeRow nodeRow){
      return getCommit(nodeRow);
    }
  }
);
}
