{
  final Set<T> result=new HashSet();
  for (  T t : set1) {
    if (!set2.contains(t)) {
      result.add(t);
    }
  }
  for (  T t : set2) {
    if (!set1.contains(t)) {
      result.add(t);
    }
  }
  return result;
}
