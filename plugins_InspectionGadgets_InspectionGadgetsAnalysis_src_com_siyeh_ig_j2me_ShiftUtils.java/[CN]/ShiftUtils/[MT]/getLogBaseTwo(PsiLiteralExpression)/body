{
  final Object value=rhs.getValue();
  int log=0;
  if (value == null) {
    return log;
  }
  int intValue=((Number)value).intValue();
  while (intValue % 2 == 0) {
    intValue>>=1;
    log++;
  }
  return log;
}
