{
  int result=start;
  while (start >= 0 && start < s.length() && " \t".indexOf(s.charAt(result)) >= 0) {
    if (backward) {
      result--;
    }
 else {
      result++;
    }
  }
  return result;
}
