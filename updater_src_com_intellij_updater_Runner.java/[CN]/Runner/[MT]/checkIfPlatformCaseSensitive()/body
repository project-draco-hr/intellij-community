{
  long requiredFreeSpace=1000000;
  String logFolder=getDir(requiredFreeSpace);
  platformCaseSensitive=new File(logFolder.toLowerCase()).exists() && new File(logFolder.toUpperCase()).exists() ? false : true;
}
