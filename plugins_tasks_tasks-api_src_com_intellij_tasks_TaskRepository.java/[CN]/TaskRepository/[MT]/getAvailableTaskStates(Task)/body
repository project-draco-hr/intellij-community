{
  return ContainerUtil.map2Set(getRepositoryType().getPossibleTaskStates(),new Function<TaskState,CustomTaskState>(){
    @Override public CustomTaskState fun(    TaskState state){
      return state.asCustomTaskState();
    }
  }
);
}
