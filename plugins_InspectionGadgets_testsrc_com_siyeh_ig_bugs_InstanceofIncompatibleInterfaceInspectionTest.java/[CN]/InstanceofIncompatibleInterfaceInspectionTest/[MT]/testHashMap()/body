{
  doTest("import java.util.HashMap;" + "import java.util.List;" + "class X {"+ "  void m() {"+ "    if(new HashMap() instanceof /*'instanceof' incompatible interface 'List'*/List/**/);"+ "  }"+ "}");
}
