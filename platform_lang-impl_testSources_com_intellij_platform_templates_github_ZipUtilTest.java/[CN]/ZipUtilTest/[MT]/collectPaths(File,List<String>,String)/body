{
  if (dir.isFile()) {
    paths.add(prefix);
    return;
  }
  File[] files=dir.listFiles();
  if (files == null) {
    return;
  }
  if (!prefix.isEmpty()) {
    prefix+='/';
  }
  for (  File file : files) {
    collectPaths(file,paths,prefix + file.getName());
  }
}
