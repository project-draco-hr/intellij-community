{
  if (isInsideTransaction() && !myWritingAllowed && !myMergeableKinds.contains(kind)) {
    LOG.error("Synchronous transactions are allowed only from user actions. " + "Please use submit*Transaction instead of invokeLater. " + "See FAQ in TransactionGuard class javadoc.");
    return AccessToken.EMPTY_ACCESS_TOKEN;
  }
  return startTransactionUnchecked();
}
