{
  this(JVMNameUtil.getJVMQualifiedName(psiMethod.getContainingClass()),psiMethod.isConstructor() ? "<init>" : psiMethod.getName(),JVMNameUtil.getJVMSignature(psiMethod),createBreakpointRequest ? calcBreakpointPosition(psiMethod) : null);
}
