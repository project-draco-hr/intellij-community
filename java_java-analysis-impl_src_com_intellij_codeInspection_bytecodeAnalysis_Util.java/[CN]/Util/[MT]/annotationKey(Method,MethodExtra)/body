{
  if ("<init>".equals(method.methodName)) {
    return canonical(method.internalClassName) + " " + simpleName(method.internalClassName)+ parameters(method,extra);
  }
 else {
    return canonical(method.internalClassName) + " " + returnType(method,extra)+ " "+ method.methodName+ parameters(method,extra);
  }
}
