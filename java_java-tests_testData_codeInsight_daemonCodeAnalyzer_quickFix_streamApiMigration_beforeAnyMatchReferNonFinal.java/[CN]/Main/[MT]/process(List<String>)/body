{
  String item="FOO";
  for (  String str : st < caret > rings) {
    String trimmed=str.trim();
    if (trimmed.equals(item)) {
      item="BAR";
      break;
    }
  }
  System.out.println(item);
}
