{
  if (!directory.isDirectory()) {
    LOG.info("not a directory: " + directory);
    return;
  }
  try {
    doOpen(directory.getAbsoluteFile(),null);
  }
 catch (  Exception e) {
    LOG.warn(e);
  }
}
