{
  return Iterables.filter(declaredNames,new Predicate<PyElement>(){
    @Override public boolean apply(    @Nullable PyElement input){
      final String name=input != null ? input.getName() : null;
      if (name != null && name.startsWith("_")) {
        return false;
      }
      return true;
    }
  }
);
}
