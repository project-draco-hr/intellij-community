{
  PsiElement targetElement=resolve();
  Assert.assertTrue(targetElement instanceof PyTargetExpression);
  Assert.assertNotNull(PsiTreeUtil.getParentOfType(targetElement,PyAssignmentStatement.class));
}
