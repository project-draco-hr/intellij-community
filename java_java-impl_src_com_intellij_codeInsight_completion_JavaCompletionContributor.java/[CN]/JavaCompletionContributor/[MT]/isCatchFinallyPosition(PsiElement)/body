{
  PsiElement leaf=PsiTreeUtil.prevVisibleLeaf(position);
  return leaf != null && leaf.textMatches("}") && leaf.getParent() instanceof PsiCodeBlock && leaf.getParent().getParent() instanceof PsiTryStatement && ((PsiTryStatement)leaf.getParent().getParent()).getResourceList() == null;
}
