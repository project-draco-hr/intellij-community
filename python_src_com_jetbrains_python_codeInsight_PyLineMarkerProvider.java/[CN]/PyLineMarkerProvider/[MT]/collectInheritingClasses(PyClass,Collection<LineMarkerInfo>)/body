{
  if (PyClassInheritorsSearch.search(element,false).findFirst() != null) {
    result.add(new LineMarkerInfo<PyClass>(element,element.getTextOffset(),OVERRIDDEN_ICON,Pass.UPDATE_OVERRIDEN_MARKERS,ourSubclassTooltipProvider,ourSubclassNavigator));
  }
}
