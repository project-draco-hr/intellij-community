{
  if (PySuperMethodsSearch.search(function).findFirst() != null) {
    return new LineMarkerInfo(element,element.getTextRange().getStartOffset(),OVERRIDING_METHOD_ICON,Pass.UPDATE_ALL,null,ourSuperMethodNavigator);
  }
  return null;
}
