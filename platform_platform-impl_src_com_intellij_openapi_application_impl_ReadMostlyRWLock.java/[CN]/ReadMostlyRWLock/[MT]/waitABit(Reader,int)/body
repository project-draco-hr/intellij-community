{
  if (iteration > SPIN_TO_WAIT_FOR_LOCK) {
    status.blocked=true;
    try {
      LockSupport.parkNanos(this,1000000);
    }
  finally {
      status.blocked=false;
    }
  }
 else {
    Thread.yield();
  }
}
