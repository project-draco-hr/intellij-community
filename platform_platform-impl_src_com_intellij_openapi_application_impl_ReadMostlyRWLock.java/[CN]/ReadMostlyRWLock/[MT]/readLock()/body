{
  checkReadThreadAccess();
  Reader status=R.get();
  for (int iter=0; ; iter++) {
    if (tryReadLock(status,true)) {
      return;
    }
    ProgressManager.checkCanceled();
    waitABit(status,iter);
  }
}
