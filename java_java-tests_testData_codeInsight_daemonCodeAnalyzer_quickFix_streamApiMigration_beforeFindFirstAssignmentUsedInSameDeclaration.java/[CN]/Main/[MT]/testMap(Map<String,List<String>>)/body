{
  int firstSize=0, other=firstSize;
  for (  List<String> list : map.valu < caret > es()) {
    if (list != null) {
      firstSize=list.size();
      break;
    }
  }
  System.out.println(firstSize);
}
