{
  metrics=FontInfo.createReferenceGraphics().getFontMetrics(font);
  lineHeight=(int)Math.ceil(metrics.getFont().createGlyphVector(metrics.getFontRenderContext(),"Ap").getVisualBounds().getHeight());
}
