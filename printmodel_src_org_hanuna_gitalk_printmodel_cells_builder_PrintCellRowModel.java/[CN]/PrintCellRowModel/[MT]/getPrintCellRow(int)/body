{
  prePrintCellRow.prepare(index);
  return new PrintCellRow(){
    @Override public int countCell(){
      return prePrintCellRow.getCountCells();
    }
    @NotNull @Override public ReadOnlyList<ShortEdge> getUpEdges(){
      return PrintCellRowModel.this.getUpEdges(index);
    }
    @NotNull @Override public ReadOnlyList<ShortEdge> getDownEdges(){
      return prePrintCellRow.downShortEdges();
    }
    @NotNull @Override public ReadOnlyList<SpecialCell> getSpecialCell(){
      return prePrintCellRow.specialCells();
    }
  }
;
}
