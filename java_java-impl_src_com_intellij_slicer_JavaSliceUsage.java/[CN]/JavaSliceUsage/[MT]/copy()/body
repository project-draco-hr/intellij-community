{
  PsiElement element=getUsageInfo().getElement();
  return getParent() == null ? createRootUsage(element,params) : new JavaSliceUsage(element,getParent(),mySubstitutor,indexNesting,syntheticField);
}
