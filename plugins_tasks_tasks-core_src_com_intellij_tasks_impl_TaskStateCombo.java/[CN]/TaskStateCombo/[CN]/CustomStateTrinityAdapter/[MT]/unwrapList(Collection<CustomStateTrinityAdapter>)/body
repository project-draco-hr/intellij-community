{
  return ContainerUtil.map(wrapped,new Function<CustomStateTrinityAdapter,CustomTaskState>(){
    @Override public CustomTaskState fun(    CustomStateTrinityAdapter adapter){
      return adapter.myState;
    }
  }
);
}
