{
  BufferedReader reader=new BufferedReader(new InputStreamReader(myStream));
  try {
    while (true) {
      String s=reader.readLine();
      if (s == null)       break;
      my_lines.add(s);
    }
    reader.close();
  }
 catch (  IOException e) {
    LOG.info(e);
  }
}
