{
  SvnTarget source=SvnTarget.fromURL(myCurrentBranchUrl);
  MergeClient client=myVcs.getFactory(myTarget).createMergeClient();
  client.merge(source,createRange(),myTarget,SVNDepth.INFINITY,mySvnConfig.MERGE_DRY_RUN,myDryRun,true,mySvnConfig.getMergeOptions(),myHandler);
}
