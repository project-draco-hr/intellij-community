{
  SvnTarget source=SvnTarget.fromURL(myCurrentBranchUrl);
  myVcs.getFactory(source).createMergeClient().merge(source,createRange(),myTarget,SVNDepth.INFINITY,mySvnConfig.MERGE_DRY_RUN,myDryRun,true,createMergeOptions(),myHandler);
}
