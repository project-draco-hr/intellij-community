{
  setNameAndTooltip(getName(),null,myInfo.isUpToDate() ? "UP-TO-DATE" : null);
  setIcon(myInfo.isRunning() ? NodeProgressAnimator.getCurrentFrame() : myInfo.isFailed() ? AllIcons.Process.State.RedExcl : myInfo.isSkipped() ? AllIcons.Process.State.YellowStr : AllIcons.Process.State.GreenOK);
}
