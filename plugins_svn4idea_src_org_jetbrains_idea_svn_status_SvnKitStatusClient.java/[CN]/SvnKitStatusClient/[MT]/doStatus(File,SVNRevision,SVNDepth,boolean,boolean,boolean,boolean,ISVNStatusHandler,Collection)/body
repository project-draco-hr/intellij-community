{
  try {
    return getStatusClient().doStatus(path,revision,depth,remote,reportAll,includeIgnored,collectParentExternals,handler,changeLists);
  }
 catch (  SVNException e) {
    throw new SvnBindException(e);
  }
}
