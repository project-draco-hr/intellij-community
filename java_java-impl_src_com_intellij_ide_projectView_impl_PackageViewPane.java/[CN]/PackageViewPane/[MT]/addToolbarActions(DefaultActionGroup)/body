{
  actionGroup.addAction(new ShowModulesAction(myProject){
    @NotNull @Override protected String getId(){
      return PackageViewPane.this.getId();
    }
  }
).setAsSecondary(true);
  actionGroup.addAction(new ShowLibraryContentsAction()).setAsSecondary(true);
}
