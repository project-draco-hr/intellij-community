{
  final Context context=new Context(document);
  context.init();
  int actual=strategy.calculateWrapPosition(createMockDocument(context.document),myProject,0,context.document.length(),context.edgeIndex,allowToBeyondMaxPreferredOffset,true);
  assertSame(context.wrapIndex,actual);
}
