{
  doTest("interface Foo<T> {" + "        void makeItSo(T command, int... values);" + "    }"+ "    class Bar implements Foo<String> {"+ "        public void makeItSo(final String command, final int... values) {"+ "        }"+ "    }");
}
