{
  String extension=FileUtilRt.getExtension(fileName);
  doTest(TestUtils.loadFile(fileName),pattern,expected,expectedWithDefaultEquivalence,patternFileType,patternFileExtension,FileTypeManager.getInstance().getFileTypeByExtension(extension),extension,true);
}
