{
  reader.beginArray();
  if (!reader.hasNext()) {
    reader.endArray();
    return Collections.emptyList();
  }
  List<T> list=new SmartList<T>();
  readListBody(reader,list);
  reader.endArray();
  return list;
}
