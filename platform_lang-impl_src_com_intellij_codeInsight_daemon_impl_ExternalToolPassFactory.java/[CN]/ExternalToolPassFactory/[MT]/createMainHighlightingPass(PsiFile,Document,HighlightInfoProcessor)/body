{
  TextRange range=file.getTextRange();
  if (range == null || !externalAnnotatorsDefined(file)) {
    return null;
  }
  return new ExternalToolPass(this,file,document,range.getStartOffset(),range.getEndOffset(),highlightInfoProcessor,true);
}
