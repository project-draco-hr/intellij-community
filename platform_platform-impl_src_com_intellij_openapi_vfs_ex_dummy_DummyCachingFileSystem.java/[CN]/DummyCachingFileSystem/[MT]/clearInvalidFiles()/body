{
  for (Iterator<String> it=myCachedFiles.keySet().iterator(); it.hasNext(); ) {
    String path=it.next();
    T t=myCachedFiles.get(path);
    if (t == null || !t.isValid()) {
      it.remove();
    }
  }
}
