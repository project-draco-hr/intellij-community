{
  if (timeoutInMilliseconds <= 0) {
    return runProcess();
  }
 else {
    startNotify();
    afterStart();
    if (waitFor(timeoutInMilliseconds)) {
      myOutput.setExitCode(getProcess().exitValue());
    }
 else {
      if (destroyOnTimeout) {
        destroyProcess();
      }
      myOutput.setTimeout();
    }
    return myOutput;
  }
}
