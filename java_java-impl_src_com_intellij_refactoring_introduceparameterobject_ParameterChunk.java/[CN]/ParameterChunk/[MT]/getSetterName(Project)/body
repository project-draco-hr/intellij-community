{
  @NonNls String setter=getSetter();
  if (setter == null) {
    setter=getField() != null ? GenerateMembersUtil.suggestSetterName(getField()) : GenerateMembersUtil.suggestSetterName(parameter.name,parameter.type,project);
  }
  return setter;
}
