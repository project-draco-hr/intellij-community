{
  @NonNls String getter=getGetter();
  if (getter == null) {
    getter=getField() != null ? GenerateMembersUtil.suggestGetterName(getField()) : GenerateMembersUtil.suggestGetterName(parameter.name,parameter.type,project);
  }
  return getter;
}
