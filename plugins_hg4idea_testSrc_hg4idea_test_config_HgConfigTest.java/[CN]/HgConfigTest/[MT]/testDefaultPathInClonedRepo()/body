{
  cd(myChildRepo);
  final String defaultPath=HgUtil.getRepositoryDefaultPath(myProject,myChildRepo);
  assertNotNull(defaultPath);
  assertEquals(myRepository.getCanonicalPath(),FileUtil.toSystemIndependentName(defaultPath));
}
