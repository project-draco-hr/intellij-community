{
  if (!chunk1.hasEqualWords && !chunk2.hasEqualWords)   return true;
  if (chunk1.isEqualIgnoreWhitespaces && chunk2.isEqualIgnoreWhitespaces)   return true;
  if (!chunk1.hasWordsInside || !chunk2.hasWordsInside)   return true;
  return false;
}
