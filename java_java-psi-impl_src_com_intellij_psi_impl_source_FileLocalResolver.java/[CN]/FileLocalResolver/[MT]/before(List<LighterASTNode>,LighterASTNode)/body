{
  return JBIterable.from(children).filter(new Condition<LighterASTNode>(){
    @Override public boolean value(    LighterASTNode node){
      return lastParent == null || node.getStartOffset() < lastParent.getStartOffset();
    }
  }
);
}
