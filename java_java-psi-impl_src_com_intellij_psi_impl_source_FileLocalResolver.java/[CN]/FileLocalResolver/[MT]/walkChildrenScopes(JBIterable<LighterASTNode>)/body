{
  return children.flatMap(new Function<LighterASTNode,Iterable<? extends LighterASTNode>>(){
    @Override public Iterable<? extends LighterASTNode> fun(    LighterASTNode child){
      return getDeclarations(child,null);
    }
  }
);
}
