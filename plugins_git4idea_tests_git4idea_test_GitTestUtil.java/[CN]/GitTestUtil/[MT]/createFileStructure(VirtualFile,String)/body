{
  for (  String path : paths) {
    cd(rootDir);
    boolean dir=path.endsWith("/");
    if (dir) {
      mkdir(path);
    }
 else {
      touch(path,"initial_content_" + Math.random());
    }
  }
}
