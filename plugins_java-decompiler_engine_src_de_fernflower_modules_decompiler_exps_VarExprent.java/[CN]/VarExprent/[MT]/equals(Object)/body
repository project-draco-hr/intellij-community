{
  if (o != null && o instanceof VarExprent) {
    VarExprent ve=(VarExprent)o;
    return index == ve.getIndex() && version == ve.getVersion() && InterpreterUtil.equalObjects(getVartype(),ve.getVartype());
  }
  return false;
}
