{
  if (r.getName() != null) {
    if (m.get(r.getName()) == null)     m.put(r.getName(),r);
  }
  for (Iterator<MatchResult> i=r.getAllSons().iterator(); i.hasNext(); ) {
    fill(i.next(),m);
  }
}
