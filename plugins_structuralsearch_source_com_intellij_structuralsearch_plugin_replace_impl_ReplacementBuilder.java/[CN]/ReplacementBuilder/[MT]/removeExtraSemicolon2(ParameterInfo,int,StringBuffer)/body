{
  if (info.statementContext) {
    int index=offset + info.startIndex;
    if (result.charAt(index - 1) == '}' && result.charAt(index) == ';') {
      result.deleteCharAt(index);
      --offset;
    }
  }
  return offset;
}
