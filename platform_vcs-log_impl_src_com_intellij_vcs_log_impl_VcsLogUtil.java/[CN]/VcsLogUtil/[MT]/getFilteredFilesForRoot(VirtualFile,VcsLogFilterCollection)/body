{
  if (filterCollection.getStructureFilter() == null)   return Collections.emptySet();
  Pair<Set<VirtualFile>,MultiMap<VirtualFile,FilePath>> rootsAndFiles=collectRoots(filterCollection.getStructureFilter().getFiles(),Collections.singleton(root));
  return new HashSet<FilePath>(rootsAndFiles.second.get(root));
}
