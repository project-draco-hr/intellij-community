{
  doTest("import java.io.*;" + "import java.net.*;" + "class X {"+ "    public void m() throws IOException {"+ "        Socket socket = new Socket(InetAddress.getLocalHost(), 1);"+ "        try {"+ "        } finally {"+ "            socket.close();"+ "        }"+ "    }"+ "}");
}
