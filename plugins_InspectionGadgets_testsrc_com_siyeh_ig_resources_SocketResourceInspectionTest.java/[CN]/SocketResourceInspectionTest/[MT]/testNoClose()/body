{
  doTest("import java.io.*;" + "import java.net.*;" + "class X {"+ "    public void m() throws IOException, UnknownHostException {"+ "        final Socket socket = new /*'Socket' should be opened in front of a 'try' block and closed in the corresponding 'finally' block*/Socket/**/(InetAddress.getLocalHost(), 1);"+ "    }"+ "}");
}
