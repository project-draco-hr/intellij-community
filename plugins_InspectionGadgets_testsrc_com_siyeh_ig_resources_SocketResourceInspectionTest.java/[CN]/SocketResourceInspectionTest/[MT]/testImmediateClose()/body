{
  doTest("import java.io.*;" + "import java.net.*;" + "class X {"+ "    public void m() throws IOException {"+ "        final Socket socket = new Socket(InetAddress.getLocalHost(), 1);"+ "        socket.close();"+ "    }"+ "}");
}
