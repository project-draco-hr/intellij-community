{
  final boolean authorized=SvnAuthenticationNotifier.passiveValidation(myProject,repositoryUrl);
  return authorized && tryExactHit(new SvnRepositoryLocation(repositoryUrl.toString()),result,logger,revisionBefore,repositoryUrl,repositoryUrl);
}
