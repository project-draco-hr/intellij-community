{
  return createRevision(date,change,new ThrowableComputable<SVNRevision,VcsException>(){
    @Override public SVNRevision compute() throws VcsException {
      return SVNRevision.create(1);
    }
  }
);
}
