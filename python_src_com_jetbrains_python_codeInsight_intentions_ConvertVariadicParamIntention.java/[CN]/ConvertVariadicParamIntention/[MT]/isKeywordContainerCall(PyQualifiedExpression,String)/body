{
  final PyExpression qualifier=callee.getQualifier();
  return qualifier != null && qualifier.getText().equals(keywordContainerName) && ArrayUtil.contains(callee.getReferencedName(),"get",PyNames.GETITEM);
}
