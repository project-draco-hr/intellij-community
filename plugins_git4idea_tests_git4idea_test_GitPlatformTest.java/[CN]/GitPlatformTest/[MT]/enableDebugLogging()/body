{
  TestLoggerFactory.enableDebugLogging(myTestRootDisposable,"#" + Executor.class.getName(),"#" + GitHandler.class.getName(),GitHandler.class.getName());
  myTestStartedIndicator=createTestStartedIndicator();
  LOG.info(myTestStartedIndicator);
}
