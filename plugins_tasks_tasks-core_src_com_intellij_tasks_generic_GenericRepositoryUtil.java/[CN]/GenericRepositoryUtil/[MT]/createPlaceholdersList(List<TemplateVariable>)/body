{
  return ContainerUtil.map2List(variables,new Function<TemplateVariable,String>(){
    @Override public String fun(    TemplateVariable variable){
      return String.format("{%s}",variable.getName());
    }
  }
);
}
