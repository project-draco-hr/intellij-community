{
  if (breakpoint == null) {
    return;
  }
  AccessToken token=WriteAction.start();
  try {
    getXBreakpointManager().removeBreakpoint(breakpoint.myXBreakpoint);
  }
  finally {
    token.finish();
  }
}
