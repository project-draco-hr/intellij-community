{
  try {
    AccessToken token=WriteAction.start();
    try {
      file.delete(VfsTestUtil.class);
    }
  finally {
      token.finish();
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
