{
  if (cell.source == null) {
    final String error=VALIDATION_ERROR_TEXT + "\n" + "\"source\" is required property:\n"+ cell;
    myErrors.add(error);
    LOG.warn(error);
    return false;
  }
  return true;
}
