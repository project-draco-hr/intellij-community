{
  final PsiNamedElement result=PsiTreeUtil.getParentOfType(element,PsiNamedElement.class,false,PyClass.class,PyFunction.class);
  if (result instanceof PyFunction && ((PyFunction)result).isTopLevel() || result instanceof PyClass && ((PyClass)result).isTopLevel()) {
    return result;
  }
  return null;
}
