{
  String name="var";
  if (myExpression != null) {
    Application application=ApplicationManager.getApplication();
    if (application != null && !application.isUnitTestMode()) {
      name=Messages.showInputDialog(project,"Enter new variable name","New variable name",Messages.getQuestionIcon());
      if (name == null)       return;
    }
    if (name.isEmpty())     return;
    PyElementGenerator elementGenerator=PyElementGenerator.getInstance(project);
    myExpression.replace(elementGenerator.createFromText(LanguageLevel.forElement(myExpression),PyAssignmentStatement.class,name + " = " + myExpression.getText()));
  }
}
