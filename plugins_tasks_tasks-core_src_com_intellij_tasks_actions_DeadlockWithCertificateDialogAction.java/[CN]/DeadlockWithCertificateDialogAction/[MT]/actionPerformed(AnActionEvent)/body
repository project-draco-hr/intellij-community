{
  ApplicationManager.getApplication().invokeLater(new Runnable(){
    @Override public void run(){
      URL location=null;
      try {
        location=new URL(SELF_SIGNED_SERVER_URL);
      }
 catch (      MalformedURLException ignored) {
      }
      Messages.showMessageDialog(e.getProject(),"This dialog may not be shown due to deadlock caused by MediaTracker and CertificateManager. " + "Fortunately it didn't happen.","Deadlocking Dialog",new ImageIcon(location));
    }
  }
);
}
