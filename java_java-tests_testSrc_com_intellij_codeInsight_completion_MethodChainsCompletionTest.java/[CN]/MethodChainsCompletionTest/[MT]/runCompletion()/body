{
  myFixture.configureByFiles(getTestCompletionFilePath());
  final LookupElement[] lookupElements=myFixture.complete(MethodsChainsCompletionContributor.COMPLETION_TYPE,MethodsChainsCompletionContributor.INVOCATIONS_THRESHOLD);
  return lookupElements == null ? LookupElement.EMPTY_ARRAY : lookupElements;
}
