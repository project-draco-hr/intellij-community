{
  while (file != null) {
    final String charset=lookupCharsetMap(file);
    if (charset != null) {
      return charset;
    }
    file=FileUtilRt.getParentFile(file);
  }
  return myProjectCharset;
}
