{
  if (myToolWindow == null)   return;
  if (file == null) {
    myToolWindow.setTitle(": (empty)");
  }
 else {
    myToolWindow.setTitle(": " + StringUtil.getShortened(EditorTabbedContainer.calcTabTitle(myProject,file),UISettings.getInstance().EDITOR_TAB_TITLE_LIMIT));
  }
}
