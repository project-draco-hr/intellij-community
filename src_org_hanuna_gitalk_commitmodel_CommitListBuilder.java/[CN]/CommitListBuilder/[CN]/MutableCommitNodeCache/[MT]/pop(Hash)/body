{
  if (hash == null) {
    return null;
  }
  MutableCommitNode commit=cache.get(hash);
  if (commit == null) {
    commit=new MutableCommitNode(hash);
  }
 else {
    cache.remove(hash);
  }
  return commit;
}
