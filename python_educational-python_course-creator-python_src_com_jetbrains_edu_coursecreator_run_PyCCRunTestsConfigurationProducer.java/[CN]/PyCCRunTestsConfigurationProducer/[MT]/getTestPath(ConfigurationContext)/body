{
  Location location=context.getLocation();
  if (location == null) {
    return null;
  }
  VirtualFile file=location.getVirtualFile();
  if (file == null) {
    return null;
  }
  VirtualFile taskDir=StudyUtils.getTaskDir(file);
  if (taskDir == null) {
    return null;
  }
  String testsPath=taskDir.findChild(EduNames.SRC) != null ? FileUtil.join(taskDir.getPath(),EduNames.SRC,EduNames.TESTS_FILE) : FileUtil.join(taskDir.getPath(),EduNames.TESTS_FILE);
  return file.getPath().equals(testsPath) ? testsPath : null;
}
