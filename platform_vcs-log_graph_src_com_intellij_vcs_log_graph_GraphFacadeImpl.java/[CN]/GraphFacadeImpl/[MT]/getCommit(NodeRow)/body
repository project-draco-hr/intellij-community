{
  List<Node> nodes=nodeRow.getNodes();
  if (nodes.size() < 1) {
    LOG.error("No nodes for nodeRow: " + nodeRow);
    return -1;
  }
 else {
    if (nodes.size() > 1 && existsNotEndNode(nodes)) {
      LOG.error("Too many nodes for nodeRow: " + nodeRow);
    }
    return nodes.get(0).getCommitIndex();
  }
}
