{
  boolean shouldReturn=false;
  for (  Window window : myTaskFile.getWindows()) {
    if (shouldReturn) {
      return window;
    }
    if (window == this) {
      shouldReturn=true;
    }
  }
  return null;
}
