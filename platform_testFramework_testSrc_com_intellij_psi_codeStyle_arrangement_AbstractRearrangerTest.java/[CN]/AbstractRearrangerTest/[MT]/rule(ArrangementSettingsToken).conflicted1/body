{
  return rule(ContainerUtil.map(conditions,new Function<ArrangementSettingsToken,ArrangementAtomMatchCondition>(){
    @Override public ArrangementAtomMatchCondition fun(    ArrangementSettingsToken it){
      return atom(it);
    }
  }
));
}
