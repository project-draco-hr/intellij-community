{
  myQueue2.execute(new Runnable(){
    public void run(){
      try {
        SwingUtilities.invokeAndWait(new Runnable(){
          public void run(){
            queue.addLast(task);
          }
        }
);
      }
 catch (      Throwable e) {
        LOG.info("Task rejected: " + task,e);
      }
    }
  }
);
}
