{
  return evaluateContext.evaluate(newValue).then(new ConsumerFunction<EvaluateResult>(){
    @Override public void consume(    EvaluateResult result){
      modifier.setValue(variable,result.value,evaluateContext);
    }
  }
);
}
