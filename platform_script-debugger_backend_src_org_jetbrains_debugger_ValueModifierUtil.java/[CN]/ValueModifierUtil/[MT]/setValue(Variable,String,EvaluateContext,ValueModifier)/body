{
  return evaluateContext.evaluate(newValue).then(new Function<EvaluateResult,Void>(){
    @Override public Void fun(    EvaluateResult result){
      modifier.setValue(variable,result.value,evaluateContext);
      return null;
    }
  }
);
}
