{
  return ContainerUtil.iterate(targetElementEvaluator.allForLanguage(language),new Condition(){
    @Override public boolean value(    Object evaluator){
      return evaluator instanceof TargetElementEvaluatorEx2;
    }
  }
);
}
