{
  if ((element instanceof PyFile) && (ResolveImportUtil.INIT_PY.equals(((PyFile)element).getName()))) {
    String path=((PyFile)element).getVirtualFile().getPath();
    if (mySeen.contains(path))     return false;
 else     mySeen.add(path);
  }
  return true;
}
