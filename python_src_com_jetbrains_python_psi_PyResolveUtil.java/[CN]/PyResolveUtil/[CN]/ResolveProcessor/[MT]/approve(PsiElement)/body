{
  if ((element instanceof PyFile) && (ResolveImportUtil.INIT_PY.equals(((PyFile)element).getName()))) {
    String fname=((PyFile)element).getUrl();
    if (mySeen.contains(fname))     return false;
 else     mySeen.add(fname);
  }
  return true;
}
