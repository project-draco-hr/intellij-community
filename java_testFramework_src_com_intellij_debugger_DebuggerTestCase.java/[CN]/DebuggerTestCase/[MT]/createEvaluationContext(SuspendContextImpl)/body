{
  try {
    return new EvaluationContextImpl(suspendContext,suspendContext.getFrameProxy(),suspendContext.getFrameProxy().thisObject());
  }
 catch (  EvaluateException e) {
    error(e);
    return null;
  }
}
