{
  Configuration.setActiveCreator(null);
  DaemonCodeAnalyzer.getInstance(searchContext.getProject()).setHighlightingEnabled(PsiDocumentManager.getInstance(searchContext.getProject()).getPsiFile(searchCriteriaEdit.getDocument()),true);
  EditorFactory.getInstance().releaseEditor(searchCriteriaEdit);
  myAlarm.cancelAllRequests();
  super.dispose();
}
