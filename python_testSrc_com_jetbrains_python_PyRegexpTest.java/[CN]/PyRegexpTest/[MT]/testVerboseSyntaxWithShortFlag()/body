{
  final PsiElement element=doTestInjectedText("import re\n" + "\n" + "re.search(\"\"\"\n"+ ".* # <caret>comment\n"+ "\"\"\", re.I | re.M | re.X)","\n.* # comment\n");
  assertEquals(element.getLanguage(),PythonVerboseRegexpLanguage.INSTANCE);
}
