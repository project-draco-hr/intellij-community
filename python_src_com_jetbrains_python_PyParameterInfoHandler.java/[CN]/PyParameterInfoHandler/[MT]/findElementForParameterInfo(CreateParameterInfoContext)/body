{
  PyArgumentList result=findArgumentList(context);
  if (result != null) {
    PyCallExpression callExpression=result.getCallExpression();
    if (callExpression == null)     return null;
    context.setItemsToShow(new Object[]{callExpression.resolveCallee()});
  }
  return result;
}
