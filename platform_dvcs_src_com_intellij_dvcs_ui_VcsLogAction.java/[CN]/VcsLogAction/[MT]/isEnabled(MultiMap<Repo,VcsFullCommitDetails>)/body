{
  if (grouped.isEmpty()) {
    return false;
  }
switch (getMode()) {
case SINGLE_COMMIT:
    return grouped.size() == 1;
case SINGLE_PER_REPO:
  return allValuesAreSingletons(grouped);
default :
return false;
}
}
