{
  final int categoryCode=getCategoryCode(category);
  return ContainerUtil.exists(errors.keySet(),new Condition<Integer>(){
    @Override public boolean value(    Integer code){
      return getCategoryCode(code) == categoryCode;
    }
  }
);
}
