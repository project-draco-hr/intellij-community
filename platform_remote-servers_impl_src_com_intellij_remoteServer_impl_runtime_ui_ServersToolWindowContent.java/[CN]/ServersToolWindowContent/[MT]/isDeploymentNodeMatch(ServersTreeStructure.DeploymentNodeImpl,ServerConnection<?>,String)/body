{
  AbstractTreeNode parent=node.getParent();
  return parent instanceof ServersTreeStructure.RemoteServerNode && isServerNodeMatch((ServersTreeStructure.RemoteServerNode)parent,connection) && node.getValue().getName().equals(deploymentName);
}
