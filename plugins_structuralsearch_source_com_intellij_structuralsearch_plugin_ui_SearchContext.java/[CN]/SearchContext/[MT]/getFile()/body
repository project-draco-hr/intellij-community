{
  if ((file == null || !file.getContainingFile().getVirtualFile().equals(currentFile)) && currentFile != null) {
    file=PsiManager.getInstance(project).findFile(currentFile);
  }
  return file;
}
