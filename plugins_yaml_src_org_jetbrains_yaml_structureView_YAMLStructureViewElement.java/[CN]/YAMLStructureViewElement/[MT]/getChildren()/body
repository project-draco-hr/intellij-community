{
  if (myElement instanceof YAMLFile || myElement instanceof YAMLDocument || myElement instanceof YAMLKeyValue && ((YAMLKeyValue)myElement).getValue() instanceof YAMLCompoundValue) {
    final List<YAMLPsiElement> list=myElement instanceof YAMLKeyValue ? ((YAMLCompoundValue)((YAMLKeyValue)myElement).getValue()).getYAMLElements() : myElement.getYAMLElements();
    final StructureViewTreeElement[] structureElements=new StructureViewTreeElement[list.size()];
    for (int i=0; i < structureElements.length; i++) {
      structureElements[i]=new YAMLStructureViewElement(list.get(i));
    }
    return structureElements;
  }
  return EMPTY_ARRAY;
}
