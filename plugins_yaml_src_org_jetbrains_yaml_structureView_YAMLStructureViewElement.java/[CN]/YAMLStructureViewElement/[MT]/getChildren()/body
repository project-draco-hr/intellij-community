{
  if (myElement instanceof YAMLFile || myElement instanceof YAMLDocument || myElement instanceof YAMLKeyValue && ((YAMLKeyValue)myElement).getValue() instanceof YAMLCompoundValue) {
    final List<YAMLPsiElement> children=myElement instanceof YAMLKeyValue ? ((YAMLCompoundValue)((YAMLKeyValue)myElement).getValue()).getYAMLElements() : myElement.getYAMLElements();
    final List<StructureViewTreeElement> structureElements=new ArrayList<StructureViewTreeElement>();
    for (    YAMLPsiElement child : children) {
      if (child instanceof YAMLDocument || child instanceof YAMLKeyValue) {
        structureElements.add(new YAMLStructureViewElement(child));
      }
    }
    return structureElements.toArray(new StructureViewTreeElement[structureElements.size()]);
  }
  return EMPTY_ARRAY;
}
