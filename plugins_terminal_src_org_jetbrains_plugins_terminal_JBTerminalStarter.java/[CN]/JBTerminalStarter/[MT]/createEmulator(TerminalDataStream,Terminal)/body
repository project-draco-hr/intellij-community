{
  return new JediEmulator(dataStream,terminal){
    @Override protected void unsupported(    char... sequenceChars){
      if (sequenceChars[0] == 7) {
        refreshAfterExecution();
      }
 else {
        super.unsupported();
      }
    }
  }
;
}
