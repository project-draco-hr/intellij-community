{
  super.visitPyDictCompExpression(node);
  LanguageLevel languageLevel=getLanguageLevel(node);
  if (!languageLevel.supportsSetLiterals()) {
    getHolder().createWarningAnnotation(node,"Python version " + languageLevel + " does not support dictionary comprehensions").registerFix(new ConvertDictCompIntention());
  }
}
