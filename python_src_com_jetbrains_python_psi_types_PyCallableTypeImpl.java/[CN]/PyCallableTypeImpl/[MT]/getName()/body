{
  return String.format("(%s) -> %s",StringUtil.join(myParameterTypes,new Function<PyType,String>(){
    @Override public String fun(    PyType type){
      return type != null ? type.getName() : "unknown";
    }
  }
,", "),myReturnType != null ? myReturnType.getName() : "unknown");
}
