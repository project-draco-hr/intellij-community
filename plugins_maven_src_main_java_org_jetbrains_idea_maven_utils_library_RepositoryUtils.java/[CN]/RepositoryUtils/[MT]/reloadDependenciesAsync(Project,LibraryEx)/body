{
  Task task=new Task.Backgroundable(project,"Maven",false){
    public void run(    @NotNull ProgressIndicator indicator){
      reloadDependencies(indicator,project,library);
    }
  }
;
  ProgressManager.getInstance().run(task);
}
