{
  this(compilerName,kind,messageText,sourcePath == null ? Collections.<String>emptyList() : Collections.singleton(sourcePath),problemBeginOffset,problemEndOffset,problemLocationOffset,locationLine,locationColumn);
}
