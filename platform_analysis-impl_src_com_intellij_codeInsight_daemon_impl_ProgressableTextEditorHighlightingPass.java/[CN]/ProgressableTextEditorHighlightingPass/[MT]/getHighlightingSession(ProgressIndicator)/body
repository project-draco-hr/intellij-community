{
  return indicator instanceof WrappedProgressIndicator ? getHighlightingSession(((WrappedProgressIndicator)indicator).getOriginalProgressIndicator()) : ((UserDataHolder)indicator).getUserData(HIGHLIGHTING_SESSION);
}
