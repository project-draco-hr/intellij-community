{
  for (int i=0; i < rows; i++) {
    for (int j=0; j < columns; j++) {
      data[i][j]="(" + (i + rOffset) + ","+ (j + cOffset)+ ")";
    }
  }
  try {
    Thread.sleep(400);
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
}
