{
  selectPath(findOption(myTreeModel.getRoot(),new DescriptorMatcher(){
    @Override public boolean matches(    @NotNull Object data){
      return StringUtil.containsIgnoreCase(data.toString(),optionName);
    }
  }
));
}
