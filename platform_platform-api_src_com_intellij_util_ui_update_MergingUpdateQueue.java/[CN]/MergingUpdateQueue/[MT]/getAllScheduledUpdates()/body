{
  return ContainerUtil.concat(myScheduledUpdates.values(),new Function<Map<Update,Update>,Collection<? extends Update>>(){
    @Override public Collection<? extends Update> fun(    Map<Update,Update> map){
      return map.keySet();
    }
  }
);
}
