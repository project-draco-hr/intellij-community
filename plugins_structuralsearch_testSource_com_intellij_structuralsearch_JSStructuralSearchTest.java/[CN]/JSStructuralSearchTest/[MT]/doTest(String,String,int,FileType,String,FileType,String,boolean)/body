{
  Language patternDialect="as".equals(patternExtension) ? JavaScriptSupportLoader.ECMA_SCRIPT_L4 : null;
  assertEquals(expectedOccurences,findMatches(source,pattern,true,patternFileType,patternDialect,sourceFileType,sourceExtension,physicalSourceFile).size());
}
