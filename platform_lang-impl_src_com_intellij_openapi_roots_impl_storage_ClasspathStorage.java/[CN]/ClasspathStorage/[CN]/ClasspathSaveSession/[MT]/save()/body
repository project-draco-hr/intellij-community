{
  AccessToken token=WriteAction.start();
  try {
    myConverter.getFileSet().commit();
  }
 catch (  IOException e) {
    throw new StateStorageException(e);
  }
 finally {
    token.finish();
  }
}
