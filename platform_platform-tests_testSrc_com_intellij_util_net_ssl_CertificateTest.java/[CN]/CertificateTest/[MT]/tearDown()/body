{
  try {
    assertTrue(myTrustManager.removeAllCertificates());
    assertEmpty(myTrustManager.getCertificates());
  }
  finally {
    try {
      myClient.close();
    }
  finally {
      super.tearDown();
    }
  }
}
