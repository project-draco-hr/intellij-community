{
  final PsiElement element=getElement();
  if (element != null) {
    GroovyPsiElementFactory factory=GroovyPsiElementFactory.getInstance(element.getProject());
    element.replace(factory.createExpressionFromText(myParamName + "." + myGetter+ "()",element));
  }
}
