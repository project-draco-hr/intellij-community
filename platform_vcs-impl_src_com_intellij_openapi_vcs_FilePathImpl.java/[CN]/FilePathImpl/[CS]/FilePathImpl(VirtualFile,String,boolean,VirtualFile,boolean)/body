{
  this(fileFromVirtual(virtualParent,child,name),isDirectory);
  myVirtualParent=virtualParent;
  if (!forDeleted) {
    if (child == null) {
      refresh();
    }
 else {
      myVirtualFile=child;
    }
  }
}
