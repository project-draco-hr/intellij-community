{
  super(name,coverageDataFileProvider,lastCoverageTimeStamp,coverageByTestEnabled,tracingEnabled,trackTestFolders,coverageRunner != null ? coverageRunner : CoverageRunner.getInstance(IDEACoverageRunner.class));
  myFilters=filters;
  mySuiteToMerge=suiteToMerge;
  myCoverageSupportProvider=coverageSupportProvider;
}
