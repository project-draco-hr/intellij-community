{
  buffer.append("\n\\s0\\box").append("\\cbpat").append(syntaxInfo.getDefaultBackground()).append("\\cb").append(syntaxInfo.getDefaultBackground());
  addFontSize(buffer,syntaxInfo.getSingleFontSize());
  buffer.append('\n');
  next.run();
  buffer.append("\\par");
}
