{
  final FutureResult<Result> future=new FutureResult<>();
  schedule(task,argument,new MavenRemoteTask.ResultProcessor<Result>(){
    @Override public void process(    @Nullable Result result){
      future.set(result);
    }
  }
,true);
  return future;
}
