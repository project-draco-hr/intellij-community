{
  final FutureResult<Result> future=new FutureResult<Result>();
  schedule(task,argument,new MavenRemoteTask.ResultProcessor<Result>(){
    @Override public void process(    Result result){
      future.set(result);
    }
  }
,true);
  return future;
}
