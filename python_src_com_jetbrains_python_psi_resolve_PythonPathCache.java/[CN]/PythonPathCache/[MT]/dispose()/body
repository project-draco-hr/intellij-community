{
  if (mySdk != null) {
synchronized (KEY) {
      final Map<Project,PythonPathCache> cacheMap=mySdk.getUserData(KEY);
      if (cacheMap != null) {
        cacheMap.remove(myProject);
      }
    }
  }
}
