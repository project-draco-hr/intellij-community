{
  FileUpdater fileUpdater=new FileUpdater(FileSystems.getDefault().getPath(parseArgs(args).outputDirectory(),configuration.packageName.replace('.',File.separatorChar),configuration.className + ".java"));
  generate(configuration,fileUpdater.builder);
  fileUpdater.update();
}
