{
  FileUpdater fileUpdater=new FileUpdater(FileSystems.getDefault().getPath(parseArgs(args).outputDirectory(),configuration.getPackageName().replace('.',File.separatorChar),configuration.getClassName() + ".java"));
  generate(configuration,fileUpdater.builder);
  fileUpdater.update();
}
