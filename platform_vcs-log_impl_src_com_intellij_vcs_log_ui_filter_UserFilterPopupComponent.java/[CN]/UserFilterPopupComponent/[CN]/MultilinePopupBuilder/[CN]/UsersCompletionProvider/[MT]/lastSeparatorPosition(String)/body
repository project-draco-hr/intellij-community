{
  int lastPosition=-1;
  for (  char separator : USERS_SEPARATORS) {
    int lio=text.lastIndexOf(separator);
    if (lio > lastPosition) {
      lastPosition=lio;
    }
  }
  return lastPosition;
}
