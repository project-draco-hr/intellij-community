{
  if (!(file.getFileSystem() instanceof LocalFileSystem))   return false;
  Map<VirtualFile,AccessStatus> statuses=getRegisteredFiles(project);
  return statuses.get(file) == AccessStatus.ALLOWED || isProjectFile(file,project);
}
