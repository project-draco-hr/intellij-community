{
  int firstSize=0;
  int other=map.size();
  if (other > 10) {
    System.out.println("Big");
  }
  for (  List<String> list : map.valu < caret > es()) {
    if (list != null) {
      firstSize=list.size();
      break;
    }
  }
  System.out.println(firstSize);
}
