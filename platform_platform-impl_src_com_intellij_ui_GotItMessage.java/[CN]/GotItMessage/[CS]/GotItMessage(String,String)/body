{
  myTitle=title;
  final String[] lines=message.split("\n");
  if (lines.length > 1) {
    myMessage="<html><div align='center'>" + message.replace("\n","<br>") + "</div></html>";
  }
 else {
    myMessage=message;
  }
}
