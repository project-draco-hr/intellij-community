{
  if (hashes.isEmpty()) {
    return Collections.emptyList();
  }
  List<String> hashArgs=new ArrayList<String>();
  for (  String hash : hashes) {
    hashArgs.add("-r");
    hashArgs.add(hash);
  }
  return hashArgs;
}
