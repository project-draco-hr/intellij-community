{
  if (preview) {
    ok=true;
    hide();
    return;
  }
  if (!matchingProcess.isEnded() && index + 1 == replacements.size()) {
    matchingProcess.resume();
    hide();
    return;
  }
  navigateNext();
}
