{
  if (typeImage == null || typeImage2 == null)   return typeImage == typeImage2;
  return typeImage2.endsWith(typeImage) && (typeImage.length() == typeImage2.length() || typeImage2.charAt(typeImage2.length() - typeImage.length() - 1) == '.');
}
