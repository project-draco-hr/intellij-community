{
  String mirror=System.getProperty("idea.maven.test.mirror","http://maven.labs.intellij.net/remote-repos/");
  return "<settings>" + content + "<mirrors>"+ "  <mirror>"+ "    <id>jb-central-proxy</id>"+ "    <url>"+ mirror+ "</url>"+ "    <mirrorOf>*</mirrorOf>"+ "  </mirror>"+ "</mirrors>"+ "</settings>";
}
