{
  final PydevConsoleRunner consoleRunner=runnerFactory.createConsoleRunner(project,sdk,consoleType,workingDirectory,environmentVariables,statements2execute);
  consoleRunner.run();
  return consoleRunner;
}
