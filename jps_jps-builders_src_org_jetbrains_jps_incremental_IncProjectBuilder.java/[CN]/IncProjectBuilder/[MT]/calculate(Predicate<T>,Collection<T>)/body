{
  int count=0;
  int item=0;
  for (  T elem : collection) {
    item++;
    if (p.apply(elem)) {
      count++;
      if (item > count) {
        return PARTIAL;
      }
    }
 else {
      if (count > 0) {
        return PARTIAL;
      }
    }
  }
  return count == 0 ? NONE : ALL;
}
