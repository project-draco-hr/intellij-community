{
  final String result=PyExpressionEvaluator.evaluateString(createFileAndExpression("my_value","x = 'hello'"," y = \" there\"","my_value = x + y + '!'"));
  Assert.assertThat("Error in string concat",result,Matchers.is("hello there!"));
}
