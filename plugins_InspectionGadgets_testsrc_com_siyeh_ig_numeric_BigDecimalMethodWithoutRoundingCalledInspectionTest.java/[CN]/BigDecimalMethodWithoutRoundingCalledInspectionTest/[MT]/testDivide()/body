{
  doTest("import java.math.BigDecimal;" + "class X {" + "  void foo(BigDecimal value) {"+ "    value./*'BigDecimal.divide()' called without a rounding mode argument*/divide/**/(value);"+ "    value.divide(value, 1);"+ "  }"+ "}");
}
