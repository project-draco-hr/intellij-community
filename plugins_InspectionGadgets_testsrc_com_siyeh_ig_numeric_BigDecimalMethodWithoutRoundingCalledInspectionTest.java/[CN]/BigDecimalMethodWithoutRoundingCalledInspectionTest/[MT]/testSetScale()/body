{
  doTest("import java.math.BigDecimal;" + "class X {" + "  void foo(BigDecimal value) {"+ "    value./*'BigDecimal.setScale()' called without a rounding mode argument*/setScale/**/(2);"+ "    value.setScale(2, 1);"+ "  }"+ "}");
}
