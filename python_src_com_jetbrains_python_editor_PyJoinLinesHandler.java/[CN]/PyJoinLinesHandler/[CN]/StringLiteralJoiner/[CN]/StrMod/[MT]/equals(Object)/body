{
  if (o instanceof StrMod) {
    final StrMod other=(StrMod)o;
    return (myOk && other.isOk() && myRaw == other.isRaw() && myPrefix.equals(other.prefix()) && myQuote.equals(other.quote()));
  }
  return false;
}
