{
  if (o != null && o instanceof FunctionExprent) {
    FunctionExprent fe=(FunctionExprent)o;
    return functype == fe.getFunctype() && InterpreterUtil.equalLists(lstOperands,fe.getLstOperands());
  }
  return false;
}
