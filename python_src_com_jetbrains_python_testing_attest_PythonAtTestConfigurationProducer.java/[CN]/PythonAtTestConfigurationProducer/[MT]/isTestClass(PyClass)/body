{
  if (pyClass == null)   return false;
  for (  PyClassRef an : pyClass.iterateAncestors()) {
    if ("TestBase".equals(an.getClassName()) && hasTestFunction(pyClass))     return true;
  }
  return false;
}
