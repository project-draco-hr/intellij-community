{
  template.parseSegments();
  defaultTemplate.parseSegments();
  return !template.equals(defaultTemplate) || !template.getVariables().equals(defaultTemplate.getVariables()) || !areOptionsEqual(template,defaultTemplate)|| !getTemplateContext(template).getDifference(defaultTemplate.getTemplateContext()).isEmpty();
}
