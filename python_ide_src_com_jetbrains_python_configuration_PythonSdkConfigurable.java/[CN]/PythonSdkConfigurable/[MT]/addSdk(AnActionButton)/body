{
  PythonSdkDetailsStep.show(myProject,myProjectSdksModel.getSdks(),new PythonSdkOptions(myProject),button.getPreferredPopupPoint(),false,new NullableConsumer<Sdk>(){
    @Override public void consume(    Sdk sdk){
      addCreatedSdk(sdk,false);
    }
  }
);
}
