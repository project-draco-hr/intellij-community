{
  PythonSdkDetailsStep.show(myProject,myProjectSdksModel.getSdks(),new PythonSdkDetailsDialog(myProject,null),button.getPreferredPopupPoint(),false,new NullableConsumer<Sdk>(){
    @Override public void consume(    Sdk sdk){
      addCreatedSdk(sdk,false);
    }
  }
);
}
