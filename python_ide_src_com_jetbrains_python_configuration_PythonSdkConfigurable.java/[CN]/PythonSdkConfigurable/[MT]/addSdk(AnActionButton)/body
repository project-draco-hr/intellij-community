{
  InterpreterPathChooser.show(myProject,myProjectSdksModel.getSdks(),button.getPreferredPopupPoint(),false,new NullableConsumer<Sdk>(){
    @Override public void consume(    Sdk sdk){
      addCreatedSdk(sdk,false,false);
    }
  }
);
}
