{
  PsiElement problemElement=descriptor.getPsiElement();
  if ((problemElement != null) && (";".equals(problemElement.getText()))) {
    if (!CodeInsightUtilBase.preparePsiElementForWrite(problemElement)) {
      return;
    }
    problemElement.delete();
  }
}
