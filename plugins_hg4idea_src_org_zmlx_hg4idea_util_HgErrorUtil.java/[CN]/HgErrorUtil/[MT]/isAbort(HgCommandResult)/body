{
  if (result == null) {
    return true;
  }
  final List<String> errorLines=result.getErrorLines();
  for (  String line : errorLines) {
    if (isAbortLine(line)) {
      return true;
    }
  }
  return false;
}
