{
  final List<ConfigurationDescriptor> names=ContainerUtil.map(collection,new Function<RunnerAndConfigurationSettings,ConfigurationDescriptor>(){
    @Override public ConfigurationDescriptor fun(    RunnerAndConfigurationSettings settings){
      return new ConfigurationDescriptor(settings.getUniqueID(),settings.getName());
    }
  }
);
  setList(names);
}
