{
  try {
    myCoverageRunner.appendCoverageArgument(new File(getCoverageFilePath()).getCanonicalPath(),getPatterns(),javaParameters,myTrackPerTestCoverage,mySampling);
  }
 catch (  IOException e) {
    LOG.info(e);
  }
}
