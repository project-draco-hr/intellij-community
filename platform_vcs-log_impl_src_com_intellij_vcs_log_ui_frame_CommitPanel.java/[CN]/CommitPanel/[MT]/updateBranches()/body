{
  if (myCommit != null) {
    myDataPanel.setBranches(myLogData.getContainingBranchesGetter().getContainingBranchesFromCache(myCommit.getRoot(),myCommit.getId()));
  }
 else {
    myDataPanel.setBranches(null);
  }
  myDataPanel.update();
}
