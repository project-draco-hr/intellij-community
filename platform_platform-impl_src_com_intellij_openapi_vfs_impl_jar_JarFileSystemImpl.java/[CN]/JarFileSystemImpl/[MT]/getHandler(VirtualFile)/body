{
  return VfsImplUtil.getHandler(entryFile,this,new Function<String,JarHandler>(){
    @Override public JarHandler fun(    String rootPath){
      return new JarHandler(extractLocalPath(rootPath));
    }
  }
);
}
