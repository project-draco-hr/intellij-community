{
  return handleChanges(context,ContainerUtil.concat(annotationsDiff.added(),annotationsDiff.removed(),toTypeCollection(paramAnnotationsDiff.added()),toTypeCollection(paramAnnotationsDiff.removed())));
}
