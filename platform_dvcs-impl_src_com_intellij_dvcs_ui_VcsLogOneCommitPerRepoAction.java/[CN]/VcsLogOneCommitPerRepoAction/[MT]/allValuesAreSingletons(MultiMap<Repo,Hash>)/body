{
  return !ContainerUtil.exists(grouped.entrySet(),new Condition<Map.Entry<Repo,Collection<Hash>>>(){
    @Override public boolean value(    Map.Entry<Repo,Collection<Hash>> entry){
      return entry.getValue().size() != 1;
    }
  }
);
}
