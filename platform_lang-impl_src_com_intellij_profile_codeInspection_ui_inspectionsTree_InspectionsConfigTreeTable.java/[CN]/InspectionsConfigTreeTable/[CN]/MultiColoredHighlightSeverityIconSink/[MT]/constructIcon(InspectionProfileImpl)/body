{
  if (myScopeToAverageSeverityMap.isEmpty()) {
    return null;
  }
  return !allScopesHasMixedSeverity() ? new MultiScopeSeverityIcon(myScopeToAverageSeverityMap,myDefaultScopeName,inspectionProfile) : ScopesAndSeveritiesTable.MIXED_FAKE_LEVEL.getIcon();
}
