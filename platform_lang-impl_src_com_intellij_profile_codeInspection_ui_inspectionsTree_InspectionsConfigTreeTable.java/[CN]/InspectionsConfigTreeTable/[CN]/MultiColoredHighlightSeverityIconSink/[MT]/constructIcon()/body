{
  if (myScopeToAverageSeverityMap.isEmpty()) {
    return null;
  }
  return !allScopesHasMixedSeverity() ? new MultiScopeSeverityIcon(myScopeToAverageSeverityMap) : ScopesAndSeveritiesTable.MIXED_FAKE_LEVEL.getIcon();
}
