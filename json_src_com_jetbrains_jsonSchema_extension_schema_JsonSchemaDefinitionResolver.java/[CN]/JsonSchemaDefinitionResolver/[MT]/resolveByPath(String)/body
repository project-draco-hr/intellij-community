{
  final Project project=myElement.getProject();
  final GlobalSearchScope filter=mySchemaId != null && myInCurrentFile ? GlobalSearchScope.fileScope(myElement.getContainingFile()) : GlobalSearchScope.allScope(project);
  return resolveInSomeSchema(referenceName,project,mySchemaId,filter);
}
