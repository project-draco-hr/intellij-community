{
  String request="git log --all --before=" + (dayStart + DAY_INTERVAL) + " --since="+ dayStart+ ""+ FORMAT+ " --full-history --sparse";
  System.out.println(request);
  return Runtime.getRuntime().exec(request);
}
