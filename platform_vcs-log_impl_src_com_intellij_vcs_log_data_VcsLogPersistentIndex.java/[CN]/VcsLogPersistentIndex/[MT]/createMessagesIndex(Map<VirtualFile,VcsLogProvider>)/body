{
  try {
    return PersistentUtil.createPersistentHashMap(EnumeratorStringDescriptor.INSTANCE,"messages",PersistentUtil.calcLogId(myProject,providers),0);
  }
 catch (  IOException e) {
    myFatalErrorsConsumer.consume(e);
    return new InMemoryMap<>();
  }
}
