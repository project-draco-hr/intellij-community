{
  final Dimension preferredSize=super.getPreferredSize();
  return myDurationWidth < 0 || isExpandableHandlerVisibleForCurrentRow() ? preferredSize : JBUI.size(preferredSize.width + myDurationWidth,preferredSize.height);
}
