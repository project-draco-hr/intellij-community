{
  myPath=path;
  myPassword=password;
  myWriteLock.lock();
  try {
    myKeyStore=loadKeyStore(path,password);
    myFactory=createFactory();
    myTrustManager=initFactoryAndGetManager();
  }
  finally {
    myWriteLock.unlock();
  }
}
