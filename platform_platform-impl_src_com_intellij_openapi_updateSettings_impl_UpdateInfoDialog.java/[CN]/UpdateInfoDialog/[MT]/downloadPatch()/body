{
  try {
    UpdateChecker.installPlatformUpdate(myPatch,myLatestBuild.getNumber(),myForceHttps);
  }
 catch (  IOException e) {
    Logger.getInstance(UpdateChecker.class).warn(e);
    if (Messages.showOkCancelDialog(IdeBundle.message("update.downloading.patch.error",e.getMessage()),IdeBundle.message("updates.error.connection.title"),IdeBundle.message("updates.download.page.button"),CommonBundle.message("button.cancel"),Messages.getErrorIcon()) == Messages.OK) {
      openDownloadPage();
    }
    return;
  }
  if (myUpdatedPlugins != null && !myUpdatedPlugins.isEmpty()) {
    new PluginUpdateInfoDialog(getContentPanel(),myUpdatedPlugins).show();
  }
}
