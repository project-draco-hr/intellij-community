{
  if (tool instanceof CustomSuppressableInspectionTool) {
    return ((CustomSuppressableInspectionTool)tool).isSuppressedFor(place);
  }
  if (tool instanceof BatchSuppressableTool) {
    return ((BatchSuppressableTool)tool).isSuppressedFor(place);
  }
  String alternativeId;
  String id;
  return SuppressionUtil.isSuppressed(place,id=tool.getID()) || (alternativeId=tool.getAlternativeID()) != null && !alternativeId.equals(id) && SuppressionUtil.isSuppressed(place,alternativeId);
}
