{
  final Project project=element.getProject();
  final PsiFile file=element.getContainingFile();
  if (!JsonSchemaFileType.INSTANCE.equals(file.getFileType()))   return false;
  return file.getVirtualFile() != null && JsonSchemaMappingsProjectConfiguration.getInstance(project).isRegisteredSchemaFile(file.getVirtualFile());
}
