{
  String moduleName=myChunk.representativeTarget().getModule().getName();
  return new CompilerMessage("",BuildMessage.Kind.ERROR,"Cannot compile Groovy files: no Groovy library is defined for module '" + moduleName + "'");
}
