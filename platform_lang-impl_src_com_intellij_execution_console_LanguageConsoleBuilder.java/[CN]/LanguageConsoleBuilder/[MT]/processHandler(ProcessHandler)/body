{
  executionEnabled=new Condition<LanguageConsole>(){
    @Override public boolean value(    LanguageConsole console){
      return !processHandler.isProcessTerminated();
    }
  }
;
  return this;
}
