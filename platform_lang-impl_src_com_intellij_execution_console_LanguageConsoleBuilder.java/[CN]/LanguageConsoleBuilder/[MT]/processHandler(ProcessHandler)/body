{
  executionEnabled=new Condition<LanguageConsoleView>(){
    @Override public boolean value(    LanguageConsoleView console){
      return !processHandler.isProcessTerminated();
    }
  }
;
  return this;
}
