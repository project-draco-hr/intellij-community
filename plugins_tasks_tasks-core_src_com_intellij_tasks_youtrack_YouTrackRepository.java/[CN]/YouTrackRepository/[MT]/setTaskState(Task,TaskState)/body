{
  String s;
switch (state) {
case IN_PROGRESS:
    s="In+Progress";
  break;
case RESOLVED:
s="fixed";
break;
default :
s=state.name();
}
doREST("/rest/issue/execute/" + task.getId() + "?command=state+"+ s,true);
}
