{
  String separator;
  if (isWindowsPath(parent)) {
    separator="\\";
  }
 else {
    separator="/";
  }
  if (parent.endsWith(separator)) {
    myPath=parent + child;
  }
 else {
    myPath=parent + separator + child;
  }
}
