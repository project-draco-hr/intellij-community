{
  String separator;
  if (win) {
    separator="\\";
  }
 else {
    separator="/";
  }
  String path;
  if (parent.endsWith(separator)) {
    path=parent + child;
  }
 else {
    path=parent + separator + child;
  }
  return path;
}
