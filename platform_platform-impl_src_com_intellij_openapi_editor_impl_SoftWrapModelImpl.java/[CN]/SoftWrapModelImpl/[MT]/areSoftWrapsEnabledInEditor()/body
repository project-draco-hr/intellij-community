{
  return !(myEditor instanceof EditorWindow) && myEditor.getSettings().isUseSoftWraps() && (!(myEditor.getDocument() instanceof DocumentImpl) || !((DocumentImpl)myEditor.getDocument()).acceptsSlashR());
}
