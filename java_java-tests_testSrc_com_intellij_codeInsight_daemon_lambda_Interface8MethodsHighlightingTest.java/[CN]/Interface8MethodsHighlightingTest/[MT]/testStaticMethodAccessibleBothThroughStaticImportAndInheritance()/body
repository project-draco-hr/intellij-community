{
  myFixture.addClass("package p; public interface Foo {" + "    static void foo() {}" + "    static void bar() {}"+ "}");
  myFixture.addClass("package p; public interface Boo {" + "    static void boo() {}" + "}");
  doTest(false,false);
}
