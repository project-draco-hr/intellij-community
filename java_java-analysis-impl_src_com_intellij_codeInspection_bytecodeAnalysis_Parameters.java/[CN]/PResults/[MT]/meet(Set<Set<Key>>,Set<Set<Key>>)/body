{
  Set<Set<Key>> sop=new HashSet<>();
  for (  Set<Key> prod1 : sop1) {
    for (    Set<Key> prod2 : sop2) {
      Set<Key> prod=new HashSet<>();
      prod.addAll(prod1);
      prod.addAll(prod2);
      sop.add(prod);
    }
  }
  return sop;
}
