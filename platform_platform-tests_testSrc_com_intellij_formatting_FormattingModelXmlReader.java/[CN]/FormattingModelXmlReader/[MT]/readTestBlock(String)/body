{
  final File dataFile=new File(PlatformTestUtil.getCommunityPath().replace(File.separatorChar,'/') + "/platform/platform-tests/testData/newGeneralFormatter",dataName + ".xml");
  final Document document=JDOMUtil.loadDocument(dataFile);
  return readBlock(document.getRootElement());
}
