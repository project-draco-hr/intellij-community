{
  return ContainerUtil.map(list,new Function<Pair<String,Key>,Pair<String,String>>(){
    @Override public Pair<String,String> fun(    Pair<String,Key> pair){
      return Pair.create(pair.first,pair.second.toString());
    }
  }
);
}
