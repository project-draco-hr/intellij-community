{
  Character mnemonic=Character.valueOf(ch);
  String shortcut=SystemInfo.isMac && SystemInfo.isJavaVersionAtLeast("1.7") ? "control alt pressed " : "alt pressed ";
  return CustomShortcutSet.fromString(shortcut + mnemonic);
}
