{
  Character quote=null;
  if (isQuoted(s)) {
    quote=s.charAt(0);
    s=removeQuotes(s);
  }
  s=removeLastSuffix(s,separator);
  if (s.length() > 0) {
    s+=separator;
  }
  s+=newElementName;
  if (quote != null) {
    s=quote.charValue() + s + quote.charValue();
  }
  return s;
}
