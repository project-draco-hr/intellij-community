{
  getSettings().ALIGN_MULTILINE_CHAINED_METHODS=true;
  doMethodTest("AA.bb()\n" + ".cc() // comment after line\n" + ".dd()\n "+ "        /* block comment on empty line */\n"+ ".ee();","AA.bb()\n" + "  .cc() // comment after line\n" + "  .dd()\n "+ "/* block comment on empty line */\n"+ "  .ee();");
}
