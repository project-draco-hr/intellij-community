{
  myWalkNodes=new TreeSet<Integer>(new Comparator<Integer>(){
    @Override public int compare(    @NotNull Integer o1,    @NotNull Integer o2){
      if (toUp)       return o2 - o1;
      return o1 - o2;
    }
  }
);
  myWalkNodes.add(startNode);
}
