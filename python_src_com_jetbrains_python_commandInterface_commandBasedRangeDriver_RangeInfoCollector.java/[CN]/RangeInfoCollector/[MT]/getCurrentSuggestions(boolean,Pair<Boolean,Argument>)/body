{
  if (argumentPair == null) {
    argumentPair=myCommand.getArgumentsInfo().getArgument(myNumOfProcessedPositionalArguments);
  }
  final List<String> suggestions=argumentPair != null ? myUnusedOptionsCollector.addUnusedOptions(argumentPair.second.getAvailableValues()) : myUnusedOptionsCollector.addUnusedOptions(null);
  if (suggestions == null) {
    return null;
  }
  return new SuggestionInfo(showAutomatically,false,suggestions);
}
