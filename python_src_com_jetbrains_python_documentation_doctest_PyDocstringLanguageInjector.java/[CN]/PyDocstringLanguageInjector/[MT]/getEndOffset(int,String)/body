{
  int end;
  int length=s.length();
  if (s.trim().endsWith("\"\"\"") || s.trim().endsWith("'''"))   length=length - 3;
 else   if (s.trim().endsWith("\"") || s.trim().endsWith("'"))   length=length - 1;
  end=start + length;
  return end;
}
