{
  FileDocumentManager.getInstance().saveAllDocuments();
  TaskDescriptor[] tasks={new MergeInitChecksTask(this),new CheckRepositorySupportsMergeInfoTask(this),finalTask};
  createContinuation().run(mapNotNull(tasks,identity()));
}
