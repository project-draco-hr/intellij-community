{
  if (value != null) {
    float alpha=myBase + myDelta * value;
    if (alpha > 0) {
      Composite old=g.getComposite();
      g.setComposite(alpha < 1 ? AlphaComposite.SrcOver.derive(alpha) : AlphaComposite.SrcOver);
      g.setColor(Gray.x80);
      paint(g,x,y,width,height);
      g.setComposite(old);
    }
  }
}
