{
  if (event.isConsumed())   return false;
  if (event.getWheelRotation() == 0)   return false;
  int modifiers=~InputEvent.SHIFT_MASK & ~InputEvent.SHIFT_DOWN_MASK & event.getModifiers();
  return modifiers == 0;
}
