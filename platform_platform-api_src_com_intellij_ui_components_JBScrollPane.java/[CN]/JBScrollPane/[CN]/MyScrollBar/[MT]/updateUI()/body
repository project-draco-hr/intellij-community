{
  ScrollBarUI ui=getUI();
  if (ui instanceof AbstractScrollBarUI)   return;
  setUI(!SystemInfo.isMac && Registry.is("ide.scroll.new.layout") ? new DefaultScrollBarUI() : ButtonlessScrollBarUI.createNormal());
}
