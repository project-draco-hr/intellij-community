{
  super.layout();
  LayoutManager layout=getLayout();
  if (layout instanceof ScrollPaneLayout) {
    myHasOverlayScrollbars=relayoutScrollbars(this,(ScrollPaneLayout)layout,myHasOverlayScrollbars);
  }
 else {
    myHasOverlayScrollbars=false;
  }
}
