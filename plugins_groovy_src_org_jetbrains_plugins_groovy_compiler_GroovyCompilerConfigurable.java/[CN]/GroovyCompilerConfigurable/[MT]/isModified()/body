{
  return !Comparing.equal(myConfig.getHeapSize(),myHeapSize.getText()) || !Comparing.equal(myConfig.getConfigScript(),getExternalizableConfigScript()) || myInvokeDynamicSupportCB.isSelected() != myConfig.isInvokeDynamic() || myExcludes.isModified();
}
