{
  testMatcher.clearContext();
  final List<MatchResult> matches=findMatches(source,pattern,true,GroovyFileType.GROOVY_FILE_TYPE,null,GroovyFileType.GROOVY_FILE_TYPE,null,false);
  assertEquals(expectedOccurences,matches.size());
}
