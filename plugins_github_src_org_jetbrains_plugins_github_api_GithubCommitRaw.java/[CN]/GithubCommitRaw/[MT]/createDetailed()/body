{
  GithubCommit commit=create();
  List<GithubFile> files=new ArrayList<GithubFile>();
  for (  GithubFileRaw raw : this.files) {
    files.add(raw.create());
  }
  return new GithubCommitDetailed(commit.getUrl(),commit.getSha(),commit.getAuthor(),commit.getCommitter(),commit.getParents(),commit.getCommit(),stats.create(),files);
}
