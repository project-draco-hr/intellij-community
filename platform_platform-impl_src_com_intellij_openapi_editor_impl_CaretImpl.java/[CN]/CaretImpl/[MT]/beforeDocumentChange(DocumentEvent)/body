{
  int startOffset=e.getOffset();
  myAfterInlayOnDeletion=e.getNewLength() == 0 && myOffset >= startOffset && myOffset <= startOffset + e.getOldLength() && myEditor.getInlayModel().hasInlineElementAt(startOffset);
  saveSelectionBeforeDocumentChange();
}
