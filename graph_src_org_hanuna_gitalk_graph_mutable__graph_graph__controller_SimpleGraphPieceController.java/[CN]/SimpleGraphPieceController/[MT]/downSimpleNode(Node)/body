{
  if (!simpleNode(node)) {
    return null;
  }
  while (node.getDownEdges().size() == 1) {
    Node nextNode=firstDownEdge(node).getDownNode();
    if (simpleNode(nextNode)) {
      node=nextNode;
    }
 else {
      return node;
    }
  }
  return node;
}
