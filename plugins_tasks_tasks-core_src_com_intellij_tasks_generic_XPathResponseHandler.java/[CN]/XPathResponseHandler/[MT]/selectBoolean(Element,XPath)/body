{
  String s=selectString(context,xPath).trim().toLowerCase();
  if (s.equals("true")) {
    return true;
  }
 else   if (s.equals("false")) {
    return false;
  }
  throw new Exception(String.format("XPath expression '%s' should match boolean value. Got '%s' instead",xPath.getXPath(),s));
}
