{
  String s=selectString(context,xPath);
  try {
    return Long.parseLong(s);
  }
 catch (  NumberFormatException e) {
    throw new Exception(String.format("XPath expression '%s' should match long value. Got '%s' instead",xPath.getXPath(),s));
  }
}
