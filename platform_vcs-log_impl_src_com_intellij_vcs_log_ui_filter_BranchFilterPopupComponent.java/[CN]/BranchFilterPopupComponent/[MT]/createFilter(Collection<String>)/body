{
  return VcsLogBranchFilterImpl.fromTextPresentation(values,ContainerUtil.map2Set(myUi.getDataPack().getRefs().getBranches(),new Function<VcsRef,String>(){
    @Override public String fun(    VcsRef vcsRef){
      return vcsRef.getName();
    }
  }
));
}
