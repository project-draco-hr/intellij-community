{
  try {
    int intValue=ThreeStateCheckBox.State.DONT_CARE.equals(value) ? myUndefinedValue : ThreeStateCheckBox.State.SELECTED.equals(value) ? myTrueValue : myFalseValue;
    field.setInt(getSettings(settings),intValue);
  }
 catch (  IllegalAccessException ignored) {
  }
}
