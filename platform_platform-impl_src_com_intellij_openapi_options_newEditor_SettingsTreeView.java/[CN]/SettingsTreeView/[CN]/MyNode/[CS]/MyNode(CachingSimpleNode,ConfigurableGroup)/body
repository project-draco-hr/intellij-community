{
  super(parent);
  myComposite=group;
  myConfigurable=null;
  String name=group.getDisplayName();
  myDisplayName=name != null ? name.replace("\n"," ") : "{ " + group.getClass().getSimpleName() + " }";
}
