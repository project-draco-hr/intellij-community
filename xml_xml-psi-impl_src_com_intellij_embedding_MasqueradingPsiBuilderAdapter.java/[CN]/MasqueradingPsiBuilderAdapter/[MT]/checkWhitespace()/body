{
  while (myLexPosition < myShrunkSequence.size() && ((PsiBuilderImpl)myDelegate).whitespaceOrComment(myShrunkSequence.get(myLexPosition).elementType)) {
    myLexPosition++;
  }
  synchronizePositions();
}
