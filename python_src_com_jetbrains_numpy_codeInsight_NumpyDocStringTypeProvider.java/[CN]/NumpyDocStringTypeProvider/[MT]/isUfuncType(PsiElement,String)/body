{
  for (  String typeName : NumPyDocString.getNumpyUnionType(typeString)) {
    if (anchor instanceof PyFunction && NumpyUfuncs.isUFunc(((PyFunction)anchor).getName()) && (typeName.equals("array_like") || typeName.equals("ndarray"))) {
      return true;
    }
  }
  return false;
}
