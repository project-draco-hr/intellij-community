{
  final InspectionConfigTreeNode node=findNodeByKey(name,myRoot);
  if (node != null) {
    TreeUtil.showRowCentered(myTreeTable.getTree(),myTreeTable.getTree().getRowForPath(new TreePath(node.getPath())) - 1,true);
    TreeUtil.selectNode(myTreeTable.getTree(),node);
  }
}
