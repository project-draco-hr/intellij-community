{
  init("line1\n" + "line2",TestFileType.TEXT);
  setEditorVisibleSize(1000,1000);
  mouse().alt().clickAt(1,1).dragTo(0,2).release();
  executeAction("EditorEscape");
  checkResultByText("line1\n" + "li<caret>ne2");
}
