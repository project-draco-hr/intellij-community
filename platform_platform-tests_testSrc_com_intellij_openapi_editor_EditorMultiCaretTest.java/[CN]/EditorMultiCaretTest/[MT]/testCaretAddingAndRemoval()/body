{
  initText("some <selection>t<caret>ext</selection>\n" + "another line");
  mouse().alt().shift().clickAt(1,1);
  checkResultByText("some <selection>t<caret>ext</selection>\n" + "a<caret>nother line");
  mouse().alt().shift().clickAt(0,8);
  checkResultByText("some <selection>t<caret>ext</selection>\n" + "a<caret>nother line");
  mouse().alt().shift().clickAt(0,6);
  checkResultByText("some text\n" + "a<caret>nother line");
  mouse().alt().shift().clickAt(1,1);
  checkResultByText("some text\n" + "a<caret>nother line");
  mouse().alt().shift().clickAt(0,30);
  checkResultByText("some text<caret>\n" + "a<caret>nother line");
  mouse().clickAt(0,0);
  checkResultByText("<caret>some text\n" + "another line");
}
