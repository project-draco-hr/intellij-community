{
  myOwnDetails.setDetailsModeEnabled(true);
  boolean noScroll=ConfigurableWrapper.isNoScroll(myConfigurable) || ConfigurableWrapper.cast(MasterDetails.class,myConfigurable) != null;
  wrapper.setContent(myComponent,getContext().getErrors().get(myConfigurable),!noScroll);
}
