{
  final GuavaLambda lambda=GuavaLambda.findFor(evaluator.evaluateType(expression));
  return lambda == null ? expression : (PsiExpression)expression.replace(JavaPsiFacade.getElementFactory(expression.getProject()).createExpressionFromText(expression.getText() + "::" + lambda.getSamName(),null));
}
