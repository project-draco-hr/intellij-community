{
  Component parent=getParent();
  if (parent instanceof JBScrollPane && (((JBScrollPane)parent).getVerticalScrollBar().getValueIsAdjusting() || ((JBScrollPane)parent).getHorizontalScrollBar().getValueIsAdjusting())) {
    return;
  }
  super.setViewPosition(p);
}
