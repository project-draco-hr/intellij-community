{
  super(content,layout);
  myContent=content;
  new ClickListener(){
    @Override public boolean onClick(    @NotNull MouseEvent event,    int clickCount){
      if (!myDisposed) {
        showPopup();
      }
      return true;
    }
  }
.installOn(this);
}
