{
  Status result=null;
  if (status != null) {
    result=new Status(status.getURL(),status.getFile(),status.getKind(),status.getRevision(),status.getCommittedRevision(),status.getContentsStatus(),status.getPropertiesStatus(),status.getRemoteContentsStatus(),status.getRemotePropertiesStatus(),status.isLocked(),status.isCopied(),status.isSwitched(),status.getCopyFromURL(),status.getRemoteLock(),status.getLocalLock(),status.getChangelistName(),TreeConflictDescription.create(status.getTreeConflict()));
  }
  return result;
}
