{
  final File compilerOutput=getCompilerOutputPath(testCaseName);
  assertEquals(Main.compile(new String[]{"-g:vars","-d",compilerOutput.getAbsolutePath(),String.format("%s/%s/%s",getTestDataPath(),testCaseName,fileToCompile)}),0);
  return compilerOutput;
}
