{
  PsiElement mk=node.getFirstChild();
  if (mk != null) {
    mk=mk.getNextSibling();
    if (mk != null) {
      Annotation ann=getHolder().createInfoAnnotation(mk,null);
      ann.setTextAttributes(PyHighlighter.PY_DECORATOR);
    }
  }
}
