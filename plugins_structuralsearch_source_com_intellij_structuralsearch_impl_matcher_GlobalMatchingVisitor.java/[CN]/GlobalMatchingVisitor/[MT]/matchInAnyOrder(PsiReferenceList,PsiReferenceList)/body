{
  if ((elements == null && isLeftLooseMatching()) || elements == elements2) {
    return true;
  }
  return matchInAnyOrder(elements.getReferenceElements(),(elements2 != null) ? elements2.getReferenceElements() : PsiElement.EMPTY_ARRAY);
}
