{
  if (!nodes.hasNext()) {
    return nodes.hasNext() == nodes2.hasNext();
  }
  myLastHandler=matchContext.getPattern().getHandler(nodes.current());
  return myLastHandler.matchSequentially(nodes,nodes2,matchContext);
}
