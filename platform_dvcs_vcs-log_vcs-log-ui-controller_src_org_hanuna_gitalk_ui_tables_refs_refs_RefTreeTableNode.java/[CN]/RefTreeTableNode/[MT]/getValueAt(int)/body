{
switch (column) {
case 0:
    return isSelectNode();
case 1:
  if (isRefNode()) {
    return ref;
  }
 else {
    return text;
  }
default :
throw new IllegalArgumentException("bad column number: " + column);
}
}
