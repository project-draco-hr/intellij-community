{
  PsiElement element=getKey().getNextSibling();
  while (element != null && !(element instanceof YAMLCompoundValue || YAMLTokenTypes.SCALAR_VALUES.contains(element.getNode().getElementType()))) {
    element=element.getNextSibling();
  }
  return element;
}
