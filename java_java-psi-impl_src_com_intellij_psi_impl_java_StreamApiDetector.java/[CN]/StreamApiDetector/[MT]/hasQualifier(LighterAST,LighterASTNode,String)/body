{
  LighterASTNode node=tree.getChildren(methodExpr).get(0);
  return node.getTokenType() == REFERENCE_EXPRESSION && refName.equals(JavaLightTreeUtil.getNameIdentifierText(tree,node)) && !hasContextVariable(tree,node,refName);
}
