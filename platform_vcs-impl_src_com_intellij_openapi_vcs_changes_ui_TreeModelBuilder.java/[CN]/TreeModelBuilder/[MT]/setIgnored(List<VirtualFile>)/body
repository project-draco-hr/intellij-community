{
  if (ContainerUtil.isEmpty(ignoredFiles))   return this;
  return insertSpecificNodeToModel(ignoredFiles,new ChangesBrowserIgnoredFilesNode(myProject,ignoredFiles.size(),0,ignoredFiles.size() > UNVERSIONED_MAX_SIZE));
}
