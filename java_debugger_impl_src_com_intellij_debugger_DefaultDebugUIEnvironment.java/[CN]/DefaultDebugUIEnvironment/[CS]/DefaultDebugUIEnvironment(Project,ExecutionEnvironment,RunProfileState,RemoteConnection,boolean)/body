{
  myProject=project;
  myExecutionEnvironment=environment;
  myModelEnvironment=new DefaultDebugEnvironment(project,environment.getExecutor(),RunnerRegistry.getInstance().findRunnerById(environment.getRunnerId()),myExecutionEnvironment.getRunProfile(),state,remoteConnection,pollConnection);
}
