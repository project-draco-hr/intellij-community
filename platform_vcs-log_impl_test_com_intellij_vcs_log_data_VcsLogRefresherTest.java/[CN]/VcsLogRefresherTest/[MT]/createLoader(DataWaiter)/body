{
  myDataHolder=new VcsLogDataHolder(myProject,myProject,myLogProviders,ServiceManager.getService(myProject,VcsLogSettings.class),dataWaiter);
  return new VcsLogRefresherImpl(myProject,myDataHolder.getHashMap(),myLogProviders,myDataHolder.getUserRegistry(),myTopDetailsCache,dataWaiter,FAILING_EXCEPTION_HANDLER,RECENT_COMMITS_COUNT);
}
