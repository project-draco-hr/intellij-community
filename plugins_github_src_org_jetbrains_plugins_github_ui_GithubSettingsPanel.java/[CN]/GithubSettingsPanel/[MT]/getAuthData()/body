{
  GithubAuthData authData=myCredentialsPanel.getAuthData();
  if (authData.getBasicAuth() != null && StringUtil.isEmptyOrSpaces(authData.getBasicAuth().getLogin()) || authData.getTokenAuth() != null && StringUtil.isEmptyOrSpaces(authData.getTokenAuth().getToken())) {
    return GithubAuthData.createAnonymous(myCredentialsPanel.getHost());
  }
  return authData;
}
