{
  return !Comparing.equal(mySettings.getHost(),getHost()) || !Comparing.equal(mySettings.getLogin(),getLogin()) || !Comparing.equal(mySettings.getAuthType(),getAuthType())|| myCredentialsModified;
}
