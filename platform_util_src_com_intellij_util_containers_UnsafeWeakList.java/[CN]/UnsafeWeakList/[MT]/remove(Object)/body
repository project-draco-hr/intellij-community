{
  processQueue();
  for (int i=0; i < myList.size(); i++) {
    T t=SoftReference.dereference(myList.get(i));
    if (t != null && t.equals(o)) {
      nullizeAt(i);
      return true;
    }
  }
  return false;
}
