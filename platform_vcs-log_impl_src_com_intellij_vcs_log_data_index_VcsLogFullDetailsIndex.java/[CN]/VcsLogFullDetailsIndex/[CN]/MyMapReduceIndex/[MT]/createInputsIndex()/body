{
  IndexExtension<Integer,Void,VcsFullCommitDetails> extension=getExtension();
  return new PersistentHashMap<>(PersistentUtil.getStorageFile("index-inputs-" + myName,myLogId,extension.getVersion()),EnumeratorIntegerDescriptor.INSTANCE,new InputIndexDataExternalizer<>(extension.getKeyDescriptor(),myID));
}
