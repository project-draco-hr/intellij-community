{
  IndexExtension<Integer,T,VcsFullCommitDetails> extension=getExtension();
  return new PersistentHashMap<>(getInputsStorageFile(extension.getVersion()),EnumeratorIntegerDescriptor.INSTANCE,new InputIndexDataExternalizer<>(extension.getKeyDescriptor(),myID));
}
