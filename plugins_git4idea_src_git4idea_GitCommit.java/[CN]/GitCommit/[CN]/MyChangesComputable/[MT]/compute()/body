{
  if (myChanges == null) {
    myChanges=GitChangesParser.parse(myData.project,myData.root,myData.changesOutput,myData.hash.asString(),new Date(myData.time),ContainerUtil.map(myData.parents,new Function<Hash,String>(){
      @Override public String fun(      Hash hash){
        return hash.asString();
      }
    }
));
    myData=null;
  }
  return myChanges;
}
