{
  List<Integer> rowsList=Ints.asList(myUi.getTable().getSelectedRows());
  myDataManager.getCommitDetailsGetter().loadCommitsData(getTable().getModel().convertToHashesAndRoots(rowsList),consumer,indicator);
}
