{
  LanguageLevelProjectExtension.getInstance(myFixture.getProject()).setLanguageLevel(myLanguageLevel);
  try {
    myFixture.tearDown();
  }
  finally {
    myFixture=null;
    super.tearDown();
  }
}
