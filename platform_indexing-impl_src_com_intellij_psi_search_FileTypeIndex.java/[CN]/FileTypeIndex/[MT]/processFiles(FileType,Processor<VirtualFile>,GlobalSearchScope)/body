{
  return FileBasedIndex.getInstance().processValues(NAME,fileType,null,new FileBasedIndex.ValueProcessor<Void>(){
    @Override public boolean process(    VirtualFile file,    Void value){
      return processor.process(file);
    }
  }
,scope);
}
