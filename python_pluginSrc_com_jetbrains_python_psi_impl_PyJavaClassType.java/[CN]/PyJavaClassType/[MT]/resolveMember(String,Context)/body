{
  final PsiMethod[] methods=myClass.findMethodsByName(name,true);
  if (methods.length > 0) {
    return new Maybe<PsiElement>(methods[0]);
  }
  final PsiField field=myClass.findFieldByName(name,true);
  if (field != null)   return new Maybe<PsiElement>(field);
  return UNRESOLVED;
}
