{
  String firstStr="";
  OUTER:   for (  List<String> list : map.valu < caret > es()) {
    if (list != null) {
      for (      String str : list) {
        if (!str.isEmpty()) {
          firstStr=str;
          break;
        }
      }
    }
  }
  System.out.println(firstStr);
}
