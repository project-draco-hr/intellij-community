{
  runPythonTest(new IPythonTask(){
    @Override public void testing() throws Exception {
      waitForReady();
      addTextToEditor("sys?");
      PsiFile psi=PsiDocumentManager.getInstance(getProject()).getPsiFile(getConsoleView().getLanguageConsole().getConsoleEditor().getDocument());
      assertFalse(PsiTreeUtil.hasErrorElements(psi));
    }
  }
);
}
