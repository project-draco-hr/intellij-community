{
  List<Action> actions=ContainerUtil.skipNulls(ContainerUtil.list(myBottomActions.rightAction4,myBottomActions.rightAction3,myBottomActions.resolveAction,myBottomActions.cancelAction));
  if (myBottomActions.resolveAction != null) {
    myBottomActions.resolveAction.putValue(DialogWrapper.DEFAULT_ACTION,true);
  }
  return actions.toArray(new Action[actions.size()]);
}
