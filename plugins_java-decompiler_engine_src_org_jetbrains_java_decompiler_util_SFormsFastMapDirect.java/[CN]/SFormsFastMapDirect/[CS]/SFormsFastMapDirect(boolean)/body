{
  if (initialize) {
    for (int i=2; i >= 0; i--) {
      @SuppressWarnings("unchecked") FastSparseSet<Integer>[] empty=new FastSparseSet[0];
      elements[i]=empty;
      next[i]=new int[0];
    }
  }
}
