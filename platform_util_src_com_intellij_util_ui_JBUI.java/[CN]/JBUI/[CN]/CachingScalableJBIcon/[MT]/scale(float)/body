{
  if (scale == 1f)   return this;
  scale=scaleVal(scale,Scale.ARBITRARY);
  if (myScaledIconCache == null || myScaledIconCache.getScale() != scale) {
    myScaledIconCache=copy();
    myScaledIconCache.setScale(scale);
  }
  return myScaledIconCache;
}
