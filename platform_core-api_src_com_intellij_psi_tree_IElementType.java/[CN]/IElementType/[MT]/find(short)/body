{
  ourRegistryReadLock.lock();
  try {
    if (idx == 0)     return ourRegistry[0];
    if (idx >= ourCounter)     return null;
    return ourRegistry[idx - FIRST_TOKEN_INDEX];
  }
  finally {
    ourRegistryReadLock.unlock();
  }
}
