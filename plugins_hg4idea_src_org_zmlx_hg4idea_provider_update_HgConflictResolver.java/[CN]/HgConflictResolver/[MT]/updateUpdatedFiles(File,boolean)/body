{
  if (updatedFiles != null) {
    updatedFiles.getGroupById(FileGroup.UPDATED_ID).remove(file.getAbsolutePath());
    updatedFiles.getGroupById(unresolved ? FileGroup.MERGED_WITH_CONFLICT_ID : FileGroup.MERGED_ID).add(file.getPath(),HgVcs.VCS_NAME,null);
  }
}
