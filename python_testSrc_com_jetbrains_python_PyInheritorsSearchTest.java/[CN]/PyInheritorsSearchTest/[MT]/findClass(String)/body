{
  final Collection<PyClass> classes=StubIndex.getInstance().get(PyClassNameIndex.KEY,name,myProject,ProjectScope.getProjectScope(myProject));
  assertEquals(1,classes.size());
  return classes.iterator().next();
}
