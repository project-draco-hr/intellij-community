{
  return buildFileStub(file,bytes,new Function<String,PsiJavaFileStub>(){
    @Override public PsiJavaFileStub fun(    String packageName){
      return new PsiJavaFileStubImpl(packageName,true);
    }
  }
);
}
