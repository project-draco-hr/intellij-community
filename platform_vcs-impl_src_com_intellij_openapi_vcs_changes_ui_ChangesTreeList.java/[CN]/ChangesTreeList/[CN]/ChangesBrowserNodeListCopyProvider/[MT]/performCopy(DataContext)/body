{
  CopyPasteManager.getInstance().setContents(new StringSelection(StringUtil.join(myList.getSelectedValues(),new Function<Object,String>(){
    @Override public String fun(    Object object){
      return ChangesBrowserNode.create(myProject,object).getTextPresentation();
    }
  }
,"\n")));
}
