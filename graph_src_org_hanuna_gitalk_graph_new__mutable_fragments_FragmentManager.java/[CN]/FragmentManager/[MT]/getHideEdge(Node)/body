{
  for (  Edge edge : node.getDownEdges()) {
    if (edge.getType() == Edge.Type.HIDE_FRAGMENT) {
      return edge;
    }
  }
  for (  Edge edge : node.getUpEdges()) {
    if (edge.getType() == Edge.Type.HIDE_FRAGMENT) {
      return edge;
    }
  }
  return null;
}
