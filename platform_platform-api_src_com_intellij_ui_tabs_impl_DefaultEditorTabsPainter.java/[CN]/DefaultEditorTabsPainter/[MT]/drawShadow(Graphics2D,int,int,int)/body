{
  int shadowTop=shadowBottom - ACTIVE_TAB_SHADOW_HEIGHT;
  g.setPaint(UIUtil.getGradientPaint(x,shadowTop,Gray.TRANSPARENT,x,shadowBottom,Gray._0.withAlpha(30)));
  g.fillRect(x,shadowTop,w,ACTIVE_TAB_SHADOW_HEIGHT);
}
