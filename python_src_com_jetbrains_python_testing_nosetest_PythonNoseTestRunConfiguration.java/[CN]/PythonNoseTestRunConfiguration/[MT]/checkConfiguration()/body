{
  super.checkConfiguration();
  if (!VFSTestFrameworkListener.getInstance().isNoseTestInstalled(getSdkHome()))   throw new RuntimeConfigurationWarning("No nosetest runner found in selected interpreter");
}
