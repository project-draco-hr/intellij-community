{
  super.checkConfiguration();
  if (!PyTestFrameworksUtil.isNoseTestInstalled(getProject(),getSdkHome()))   throw new RuntimeConfigurationWarning("No nosetest runner found in selected interpreter");
}
