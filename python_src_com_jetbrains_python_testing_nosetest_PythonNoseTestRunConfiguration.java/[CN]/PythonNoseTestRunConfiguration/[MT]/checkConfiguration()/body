{
  super.checkConfiguration();
  if (!PyTestFrameworksUtil.isNoseTestInstalled(getProject(),getSdkHome()))   throw new RuntimeConfigurationError("No nosetest runner found in selected interpreter");
}
