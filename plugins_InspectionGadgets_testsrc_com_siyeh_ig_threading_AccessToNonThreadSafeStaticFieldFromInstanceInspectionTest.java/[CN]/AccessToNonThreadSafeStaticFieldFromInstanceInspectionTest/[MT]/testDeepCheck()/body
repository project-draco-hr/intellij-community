{
  doTest("import java.util.Date;" + "import java.text.DateFormat;" + "import java.text.SimpleDateFormat;"+ "class C {"+ "    private static final DateFormat df = new SimpleDateFormat(\"yyyy-MM-dd\");"+ "    private static final Date d = new Date();"+ "    private static final String s1 = df.format(d);"+ "    String m() {"+ "      return /*Access to non thread-safe static field 'df' of type 'java.text.SimpleDateFormat'*/df/**/.format(d);"+ "    }"+ "}");
}
