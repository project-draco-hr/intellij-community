{
  RefsModel emptyModel=new RefsModel(ContainerUtil.newHashMap(),ContainerUtil.<Integer>newHashSet(),VcsLogHashMapImpl.EMPTY,ContainerUtil.newHashMap());
  return new DataPack(emptyModel,EmptyPermanentGraph.getInstance(),Collections.<VirtualFile,VcsLogProvider>emptyMap(),false);
}
