{
  int min=Integer.MAX_VALUE;
  int minIndex=start;
  for (int i=start; i < end; i++) {
    int value=index2usage[i];
    if (value < min) {
      min=value;
      minIndex=i;
    }
  }
  return minIndex;
}
