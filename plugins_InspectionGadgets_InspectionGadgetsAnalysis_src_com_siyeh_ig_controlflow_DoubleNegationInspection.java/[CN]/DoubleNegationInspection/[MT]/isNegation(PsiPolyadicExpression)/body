{
  for (  PsiExpression operand : expression.getOperands()) {
    if (TypeUtils.hasFloatingPointType(operand))     return false;
  }
  return JavaTokenType.NE.equals(expression.getOperationTokenType());
}
