{
  Notification notification=new Notification(GITHUB_NOTIFICATION_GROUP,title,"<a href='" + url + "'>"+ message+ "</a>",NotificationType.INFORMATION,NotificationListener.URL_OPENING_LISTENER);
  Notificator.getInstance(project).notify(notification);
  LOG.info(title + "; " + message+ "; "+ url);
}
