{
  return new Condition<PsiExpression>(){
    @Override public boolean value(    PsiExpression expression){
      return PsiType.BOOLEAN.equals(expression.getType()) || PsiType.BOOLEAN.equals(PsiPrimitiveType.getUnboxedType(expression.getType()));
    }
  }
;
}
