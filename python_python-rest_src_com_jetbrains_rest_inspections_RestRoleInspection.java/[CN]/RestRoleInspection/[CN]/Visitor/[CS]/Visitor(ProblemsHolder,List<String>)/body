{
  super(holder);
  myIgnoredRoles=ImmutableSet.copyOf(ignoredRoles);
  Project project=holder.getProject();
  String dir=ReSTService.getInstance(project).getWorkdir();
  if (!dir.isEmpty())   fillSphinxRoles(dir,project);
}
