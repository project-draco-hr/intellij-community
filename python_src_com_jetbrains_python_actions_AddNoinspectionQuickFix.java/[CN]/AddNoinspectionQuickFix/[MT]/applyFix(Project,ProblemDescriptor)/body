{
  if (myInspectionElement == null)   return;
  final PsiComment noInspection=PyElementGenerator.getInstance(project).createFromText(LanguageLevel.getDefault(),PsiComment.class,"#noinspection " + myInspectionName);
  myInspectionElement.getParent().addBefore(noInspection,myInspectionElement);
}
