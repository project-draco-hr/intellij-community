{
  if (is(LBRACKET)) {
    advance();
    PsiBuilder.Marker sectionName=mark();
    if (is(SECTION_NAME)) {
      doneAdvance(sectionName,SECTION_NAME);
    }
 else {
      sectionName.drop();
      return false;
    }
    if (is(RBRACKET)) {
      advance();
    }
 else {
      error("] expected.");
      skipLine();
    }
    return true;
  }
  return false;
}
