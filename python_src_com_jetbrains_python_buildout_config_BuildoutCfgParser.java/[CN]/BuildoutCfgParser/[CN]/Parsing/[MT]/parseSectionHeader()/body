{
  if (is(LBRACKET)) {
    advance();
    PsiBuilder.Marker sectionName=mark();
    if (is(SECTION_NAME)) {
      doneAdvance(sectionName,SECTION_NAME);
    }
 else {
      sectionName.drop();
      error("Section name expected.");
      return false;
    }
    if (is(RBRACKET)) {
      advance();
    }
 else {
      error("] expected.");
      skipLine();
    }
    return true;
  }
  return false;
}
