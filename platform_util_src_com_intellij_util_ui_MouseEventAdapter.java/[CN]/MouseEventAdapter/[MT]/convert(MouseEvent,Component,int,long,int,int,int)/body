{
  return event instanceof MouseWheelEvent ? convert((MouseWheelEvent)event,source,id,when,modifiers,x,y) : new MouseEvent(source,id,when,modifiers,x,y,event.getClickCount(),event.isPopupTrigger(),event.getButton());
}
