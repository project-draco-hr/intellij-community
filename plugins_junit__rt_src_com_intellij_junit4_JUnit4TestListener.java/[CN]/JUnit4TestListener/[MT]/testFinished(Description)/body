{
  final long duration=System.currentTimeMillis() - myCurrentTestStart;
  myPrintStream.println("\n##teamcity[testFinished name=\'" + escapeName(JUnit4ReflectionUtil.getMethodName(description)) + (duration > 0 ? "\' duration=\'" + Long.toString(duration) : "")+ "\']");
}
