{
  if (commandName != null) {
    for (    JBProtocolCommand command : EP_NAME.getExtensions()) {
      if (command.getCommandName().equals(commandName)) {
        return command;
      }
    }
  }
  return null;
}
