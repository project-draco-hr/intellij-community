{
  return myDelegate == null ? myCurrentX - getFoldRegionWidthInPixels(myFoldRegion) + getXForVisualColumnInsideFoldRegion(myFoldRegion,column - myCurrentVisualColumn + getFoldRegionWidthInColumns(myFoldRegion)) : myDelegate.visualColumnToX(column);
}
