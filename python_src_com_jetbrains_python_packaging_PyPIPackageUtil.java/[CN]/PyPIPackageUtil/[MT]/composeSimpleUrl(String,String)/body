{
  String suffix="";
  final String repository=StringUtil.trimEnd(rep,"/");
  if (!repository.endsWith("+simple")) {
    suffix="/+simple";
  }
  suffix+="/" + packageName;
  return repository + suffix;
}
