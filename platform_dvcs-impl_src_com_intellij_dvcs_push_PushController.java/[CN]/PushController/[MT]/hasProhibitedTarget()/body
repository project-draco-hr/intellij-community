{
  for (  MyRepoModel model : myView2Model.values()) {
    PushTarget target=model.getTarget();
    if (model.isSelected() && target != null && !model.getSupport().isForcePushAllowed(model.getRepository(),target)) {
      return target;
    }
  }
  return null;
}
