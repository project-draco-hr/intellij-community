{
  return ContainerUtil.filter(instructions,new Condition<Instruction>(){
    @Override public boolean value(    Instruction instruction){
      return visitor.isRedundantInstanceof(instruction);
    }
  }
);
}
