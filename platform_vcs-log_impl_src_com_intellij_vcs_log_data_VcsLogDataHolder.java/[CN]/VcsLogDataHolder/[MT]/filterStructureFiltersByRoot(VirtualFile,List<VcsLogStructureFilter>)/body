{
  return ContainerUtil.filter(structureFilters,new Condition<VcsLogStructureFilter>(){
    @Override public boolean value(    VcsLogStructureFilter filter){
      return !filter.getFiles(root).isEmpty();
    }
  }
);
}
