{
  for (int i=0, length=name.length(); i < length; i++) {
    int c=name.codePointAt(i);
    if (!(c >= 'a' && c <= 'z' || c >= 'A' && c <= 'Z' || c >= '0' && c <= '9')) {
      return false;
    }
  }
  return true;
}
