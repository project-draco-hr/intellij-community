{
  return EditorColorsManager.getInstance().getGlobalScheme().getColor(hovered ? Background.HOVERED : selected ? Background.CURRENT : light && !navigationCrumb ? Background.INACTIVE : Background.DEFAULT);
}
