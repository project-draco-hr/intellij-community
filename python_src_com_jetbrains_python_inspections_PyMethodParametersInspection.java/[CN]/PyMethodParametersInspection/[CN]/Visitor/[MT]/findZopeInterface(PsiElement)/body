{
  PsiElement ret;
synchronized (this) {
    if (myPossibleZopeRef == null) {
      myPossibleZopeRef=new Ref<PsiElement>();
      ret=ResolveImportUtil.resolveInRoots(foothold,"zope.interface.Interface");
      myPossibleZopeRef.set(ret);
    }
 else     ret=myPossibleZopeRef.get();
  }
  return ret;
}
