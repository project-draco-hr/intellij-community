{
  WindowWrapper.Mode mode=hints.getMode();
  if (mode == null)   mode=(KeyboardFocusManager.getCurrentKeyboardFocusManager().getFocusedWindow() instanceof JDialog) ? WindowWrapper.Mode.MODAL : WindowWrapper.Mode.FRAME;
  return mode;
}
