{
  if (!contentRoot.isValid()) {
    return;
  }
  final ProjectFileIndex index=ProjectRootManager.getInstance(project).getFileIndex();
  collectFolderCoverage(contentRoot,dataManager,annotator,data,suite.isTrackTestFolders(),index,suite.getCoverageEngine(),Sets.<VirtualFile>newHashSet());
}
