{
  return map2Usages(ContainerUtil.mapNotNull(myFailedConversions.entrySet(),new Function<Map.Entry<Pair<SmartPsiElementPointer<PsiExpression>,PsiType>,TypeMigrationUsageInfo>,Pair<SmartPsiElementPointer<PsiExpression>,PsiType>>(){
    @Override public Pair<SmartPsiElementPointer<PsiExpression>,PsiType> fun(    Map.Entry<Pair<SmartPsiElementPointer<PsiExpression>,PsiType>,TypeMigrationUsageInfo> entry){
      return entry.getValue().equals(root) ? entry.getKey() : null;
    }
  }
));
}
