{
  return ContainerUtil.find(getExceptionFields(),new Condition<SectionField>(){
    @Override public boolean value(    SectionField field){
      return exceptionType.equals(field.getType());
    }
  }
);
}
