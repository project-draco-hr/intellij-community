{
  return kCLASS == builder.getTokenType() || kINTERFACE == builder.getTokenType() || kENUM == builder.getTokenType() || kTRAIT == builder.getTokenType() || ParserUtils.lookAhead(builder,mAT,kINTERFACE);
}
