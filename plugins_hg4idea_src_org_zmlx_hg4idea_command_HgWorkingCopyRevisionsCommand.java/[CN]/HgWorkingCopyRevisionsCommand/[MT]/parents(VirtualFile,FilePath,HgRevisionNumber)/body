{
  final List<HgRevisionNumber> revisions=getRevisions(repo,"parents",file,revision,true);
switch (revisions.size()) {
case 1:
    return Couple.newOne(revisions.get(0),null);
case 2:
  return Couple.newOne(revisions.get(0),revisions.get(1));
default :
return Couple.newOne(null,null);
}
}
