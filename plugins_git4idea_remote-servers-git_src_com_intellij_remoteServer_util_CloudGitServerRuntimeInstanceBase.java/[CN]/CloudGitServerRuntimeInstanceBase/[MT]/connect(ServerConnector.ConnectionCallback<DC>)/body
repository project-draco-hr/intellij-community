{
  myAgentTaskExecutor.execute(new Computable(){
    @Override public Object compute(){
      doConnect(myConfiguration,new CloudAgentLogger(){
        @Override public void debugEx(        Exception e){
          LOG.debug(e);
        }
        @Override public void debug(        String message){
          LOG.debug(message);
        }
      }
);
      return null;
    }
  }
,new CallbackWrapper(){
    @Override public void onSuccess(    Object result){
      callback.connected(CloudGitServerRuntimeInstanceBase.this);
    }
    @Override public void onError(    String message){
      callback.errorOccurred(message);
    }
  }
);
}
