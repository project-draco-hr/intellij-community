{
  super("Next Diff","Move to the next difference",AllIcons.Actions.MoveDown);
  setEnabledInModalContext(true);
  setShortcutSet(ActionManager.getInstance().getAction(IdeActions.ACTION_NEXT_DIFF).getShortcutSet());
}
