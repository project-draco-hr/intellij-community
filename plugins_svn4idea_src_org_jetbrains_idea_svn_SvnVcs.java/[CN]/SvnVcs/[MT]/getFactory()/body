{
  WorkingCopyFormat format=getWorkingCopyFormat(new File(getProject().getBaseDir().getPath()));
  return WorkingCopyFormat.ONE_DOT_EIGHT.equals(format) || myConfiguration.myUseAcceleration.equals(SvnConfiguration.UseAcceleration.commandLine) ? cmdClientFactory : svnKitClientFactory;
}
