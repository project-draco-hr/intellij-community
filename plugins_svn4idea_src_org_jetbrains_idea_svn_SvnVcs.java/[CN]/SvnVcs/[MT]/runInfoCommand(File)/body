{
  SVNInfo result=null;
  try {
    result=SvnConfiguration.UseAcceleration.commandLine.equals(myConfiguration.myUseAcceleration) ? getInfoCommandLine(file) : getInfoSvnKit(file);
  }
 catch (  SVNException e) {
    handleInfoException(e);
  }
  return result;
}
