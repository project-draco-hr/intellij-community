{
  SVNInfo result=null;
  try {
    result=SvnConfiguration.UseAcceleration.commandLine.equals(myConfiguration.myUseAcceleration) ? getInfoCommandLine(file,SVNRevision.UNDEFINED) : getInfoSvnKit(file);
  }
 catch (  SVNException e) {
    handleInfoException(e);
  }
  return result;
}
