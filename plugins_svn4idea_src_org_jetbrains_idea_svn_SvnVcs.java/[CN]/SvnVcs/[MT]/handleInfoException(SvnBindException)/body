{
  if (!myLogExceptions || SvnUtil.isUnversionedOrNotFound(e) || e.contains(SVNErrorCode.WC_UNSUPPORTED_FORMAT)|| e.contains(SVNErrorCode.WC_UPGRADE_REQUIRED)) {
    LOG.debug(e);
  }
 else {
    LOG.error(e);
  }
}
