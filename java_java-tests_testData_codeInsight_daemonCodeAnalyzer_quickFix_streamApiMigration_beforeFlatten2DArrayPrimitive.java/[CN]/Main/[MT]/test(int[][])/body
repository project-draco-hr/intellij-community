{
  List<Integer> result=new ArrayList<>();
  for (  int[] subArr : a < caret > rr) {
    if (subArr != null) {
      for (      int str : subArr) {
        result.add(str);
      }
    }
  }
  return result;
}
