{
  if (type == PromptType.In)   return promptNumber == null ? type + "[ ]:" : String.format(type + " [%d]:",promptNumber);
 else   if (type == PromptType.Out)   return promptNumber == null ? type + "[ ]:" : String.format(type + "[%d]:",promptNumber);
  return "";
}
