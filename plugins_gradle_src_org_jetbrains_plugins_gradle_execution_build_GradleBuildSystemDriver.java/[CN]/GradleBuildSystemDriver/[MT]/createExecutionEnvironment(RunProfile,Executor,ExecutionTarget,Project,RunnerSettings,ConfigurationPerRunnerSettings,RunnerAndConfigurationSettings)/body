{
  if (runProfile instanceof ApplicationConfiguration) {
    return new GradleApplicationEnvironmentBuilder().build(runProfile,executor,executionTarget,project,runnerSettings,configurationSettings,settings);
  }
  return null;
}
