{
  myProject=project;
  setLayout(new BoxLayout(this,BoxLayout.Y_AXIS));
  final String[] text=cell.getText();
  if (text != null)   add(createContainer(outputPrompt(promptNumber),new JTextArea(StringUtil.join(text,"\n"))));
}
