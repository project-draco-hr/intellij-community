{
  myProgress=1;
switch (getState()) {
case COLLAPSING:
    setState(State.COLLAPSED);
  break;
case EXPANDING:
setState(State.TEMPORARY_EXPANDED);
break;
default :
}
revalidate();
if (getState() == State.COLLAPSED) {
getParent().repaint();
}
 else {
repaint();
}
}
