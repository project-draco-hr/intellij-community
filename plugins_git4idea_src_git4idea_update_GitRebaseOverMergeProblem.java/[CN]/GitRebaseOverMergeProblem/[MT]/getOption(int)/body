{
  return ObjectUtils.assertNotNull(ContainerUtil.find(values(),new Condition<Decision>(){
    @Override public boolean value(    Decision decision){
      return decision.ordinal() == index;
    }
  }
));
}
