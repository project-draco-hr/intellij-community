{
  if (qualifiers.isEmpty()) {
    return resolved.asMethod() != null;
  }
  for (  PyExpression qualifier : qualifiers) {
    if (isQualifiedByInstance(resolved,qualifier,context)) {
      return true;
    }
  }
  return false;
}
