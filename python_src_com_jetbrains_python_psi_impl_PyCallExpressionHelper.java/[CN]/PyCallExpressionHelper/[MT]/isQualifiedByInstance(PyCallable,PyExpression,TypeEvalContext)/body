{
  if (isQualifiedByClass(resolved,qualifier,context)) {
    return false;
  }
  PyType qtype=context.getType(qualifier);
  if (qtype != null) {
    if (qtype instanceof PyModuleType)     return false;
  }
  return true;
}
