{
  LogEntry entry=new LogEntry.Builder().setRevision(revision).setAuthor(AUTHOR).setDate(new Date(System.currentTimeMillis())).setMessage("").build();
  return new SvnChangeList(null,myLocation,entry,ROOT.toDecodedString());
}
