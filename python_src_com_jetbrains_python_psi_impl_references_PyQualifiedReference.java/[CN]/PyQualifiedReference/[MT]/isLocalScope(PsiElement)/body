{
  if (element instanceof PyParameter) {
    return true;
  }
  if (element instanceof PyTargetExpression) {
    return ((PyTargetExpression)element).getQualifier() == null && PsiTreeUtil.getParentOfType(element,ScopeOwner.class) instanceof PyFunction;
  }
  return false;
}
