{
  String pkgName=owner.getPackageName();
  PackageSymbol pkg=StringUtil.isEmpty(pkgName) ? mySymbols.myRootPackage : mySymbols.enterPackage(myNameEnvironment.fromString(pkgName,true));
  for (  PsiClass psiClass : owner.getClasses()) {
    enter(psiClass,pkg);
  }
}
