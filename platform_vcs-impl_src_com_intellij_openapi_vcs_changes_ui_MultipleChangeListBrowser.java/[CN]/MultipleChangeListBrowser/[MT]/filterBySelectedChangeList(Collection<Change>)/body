{
  return ContainerUtil.findAll(changes,new Condition<Change>(){
    @Override public boolean value(    @NotNull Change change){
      return Comparing.equal(getList(change),mySelectedChangeList);
    }
  }
);
}
