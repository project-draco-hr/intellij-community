{
  ProcessHandler processHandler=myDescriptor.getProcessHandler();
  return !ExecutorRegistry.getInstance().isStarting(myEnvironment.getProject(),myExecutor.getId(),myRunner.getRunnerId()) && !(processHandler != null && processHandler.isProcessTerminating());
}
