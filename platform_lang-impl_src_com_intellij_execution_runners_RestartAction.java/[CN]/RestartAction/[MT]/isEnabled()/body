{
  ProcessHandler processHandler=myDescriptor.getProcessHandler();
  return !ExecutorRegistry.getInstance().isStarting(myEnvironment.getProject(),myExecutor.getId(),getRunnerId()) && !(processHandler != null && processHandler.isProcessTerminating());
}
