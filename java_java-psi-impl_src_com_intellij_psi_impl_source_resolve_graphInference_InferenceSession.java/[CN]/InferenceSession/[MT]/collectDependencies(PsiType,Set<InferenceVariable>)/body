{
  return collectDependencies(type,dependencies,new Function<PsiClassType,InferenceVariable>(){
    @Override public InferenceVariable fun(    PsiClassType classType){
      return getInferenceVariable(classType);
    }
  }
);
}
