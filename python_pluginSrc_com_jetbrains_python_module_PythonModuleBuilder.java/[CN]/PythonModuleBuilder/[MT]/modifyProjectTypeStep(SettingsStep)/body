{
  return new SdkSettingsStep(settingsStep,this,new Condition<SdkTypeId>(){
    @Override public boolean value(    SdkTypeId id){
      return PythonSdkType.getInstance() == id;
    }
  }
){
    @Override protected void OnSdkSelected(    Sdk sdk){
      setSdk(sdk);
    }
  }
;
}
