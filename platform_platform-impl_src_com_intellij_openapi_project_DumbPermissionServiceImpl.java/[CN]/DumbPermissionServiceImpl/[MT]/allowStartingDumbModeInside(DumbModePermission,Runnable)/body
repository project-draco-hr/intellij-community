{
  DumbModePermission prev=myPermission.get();
  myPermission.set(permission);
  try {
    runnable.run();
  }
  finally {
    if (prev == null) {
      myPermission.remove();
    }
 else {
      myPermission.set(prev);
    }
  }
}
