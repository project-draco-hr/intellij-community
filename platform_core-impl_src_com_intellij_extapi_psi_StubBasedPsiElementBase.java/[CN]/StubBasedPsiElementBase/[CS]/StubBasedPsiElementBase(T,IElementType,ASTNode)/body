{
  if (stub != null) {
    LOG.assertTrue(nodeType != null && node == null);
    myStub=stub;
    myElementType=nodeType;
  }
 else {
    LOG.assertTrue(nodeType == null && node != null);
    myNode=node;
    myElementType=node.getElementType();
  }
}
