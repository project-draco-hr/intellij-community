{
  if (isVersioned(f)) {
    myVcs.created(f.getPath(),f.isDirectory());
  }
  if (f.isDirectory()) {
    Collection<VirtualFile> children=isVersioned(f) ? IdeaGateway.loadAndIterateChildren(f) : IdeaGateway.iterateDBChildren(f);
    for (    VirtualFile each : children) {
      createRecursively(each);
    }
  }
}
