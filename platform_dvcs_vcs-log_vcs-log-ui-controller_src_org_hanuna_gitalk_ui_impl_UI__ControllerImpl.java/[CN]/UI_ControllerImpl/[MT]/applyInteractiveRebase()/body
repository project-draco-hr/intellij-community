{
  if (rebaseDelegate.resultRef == null) {
    return;
  }
  getVcsLogActionHandler().interactiveRebase(rebaseDelegate.subjectRef,rebaseDelegate.branchBase,getCallback(),rebaseDelegate.getRebaseCommands());
  cancelInteractiveRebase();
}
