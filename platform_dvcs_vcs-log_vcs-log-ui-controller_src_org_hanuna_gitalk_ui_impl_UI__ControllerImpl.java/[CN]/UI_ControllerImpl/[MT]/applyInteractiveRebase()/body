{
  if (rebaseDelegate.resultRef == null) {
    return;
  }
  getGitActionHandler().interactiveRebase(rebaseDelegate.subjectRef,rebaseDelegate.branchBase,getCallback(),rebaseDelegate.getRebaseCommands());
  cancelInteractiveRebase();
}
