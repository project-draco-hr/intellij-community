{
  if (codeBlock == null) {
    PsiCodeBlock search;
    search=(PsiCodeBlock)MatcherImplUtil.createTreeFromText(options.getMatchOptions().getSearchPattern(),PatternTreeContext.Block,options.getMatchOptions().getFileType(),project)[0].getParent();
    codeBlock=search;
  }
  return codeBlock;
}
