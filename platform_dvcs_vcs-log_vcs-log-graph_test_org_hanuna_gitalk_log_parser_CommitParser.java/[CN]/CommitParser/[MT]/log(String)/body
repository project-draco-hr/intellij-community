{
  return ContainerUtil.map(Arrays.asList(commits),new Function<String,TimeCommitParents>(){
    @Override public TimeCommitParents fun(    String commit){
      return parseTimestampParentHashes(commit);
    }
  }
);
}
