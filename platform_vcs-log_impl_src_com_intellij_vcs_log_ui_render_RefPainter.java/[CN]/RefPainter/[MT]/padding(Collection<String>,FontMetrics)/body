{
  if (refs.isEmpty())   return 0;
  int p=2 * LABEL_PADDING;
  for (  String ref : refs) {
    HorizontalDimensions horizontalDimensions=getHorizontalDimensions(ref,p,fontMetrics);
    p+=horizontalDimensions.width + LABEL_PADDING;
  }
  return p;
}
