{
  for (int i=0, n=url.length(); i < n; i++) {
    char c=url.charAt(i);
    if (c == ':') {
      return parseUrl(url);
    }
 else     if (c == '/' || c == '\\') {
      return newLocalFileUrl(url.toString());
    }
  }
  return newLocalFileUrl(url.toString());
}
