{
  Url asUrl=parseUrl(url);
  if (asUrl == null) {
    return null;
  }
  try {
    return asUrl.toJavaUriWithoutParameters();
  }
 catch (  Exception e) {
    LOG.info("Can't parse " + url,e);
    return null;
  }
}
