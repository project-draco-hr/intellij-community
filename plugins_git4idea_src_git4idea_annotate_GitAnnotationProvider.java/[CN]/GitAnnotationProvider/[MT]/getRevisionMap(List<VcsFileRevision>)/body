{
  return ContainerUtil.map2Map(revisions,new Function<VcsFileRevision,Pair<VcsRevisionNumber,VcsFileRevision>>(){
    @Override public Pair<VcsRevisionNumber,VcsFileRevision> fun(    VcsFileRevision revision){
      return Pair.create(revision.getRevisionNumber(),revision);
    }
  }
);
}
