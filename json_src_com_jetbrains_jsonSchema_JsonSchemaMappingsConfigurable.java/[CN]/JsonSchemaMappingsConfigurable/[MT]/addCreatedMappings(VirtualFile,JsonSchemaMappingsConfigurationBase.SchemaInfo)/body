{
  final MyNode node=new MyNode(new JsonSchemaConfigurable(myProject,FileUtil.toSystemDependentName(schemaFile.getPath()),info,myTreeUpdater),info.isApplicationLevel());
  addNode(node,myRoot);
  selectNodeInTree(node,true);
}
