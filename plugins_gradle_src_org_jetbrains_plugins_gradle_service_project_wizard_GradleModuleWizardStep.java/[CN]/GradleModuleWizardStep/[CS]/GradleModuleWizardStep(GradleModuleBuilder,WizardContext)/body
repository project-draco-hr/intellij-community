{
  myProjectOrNull=context.getProject();
  myBuilder=builder;
  myContext=context;
  myParentProjectForm=new GradleParentProjectForm(context,new NullableConsumer<ProjectData>(){
    @Override public void consume(    @Nullable ProjectData parentProject){
      updateComponents();
    }
  }
);
  initComponents();
  loadSettings();
}
