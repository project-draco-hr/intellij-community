{
  try {
synchronized (myLock) {
      if (myConnection != null) {
        myConnection.close();
      }
    }
    myChannel.close();
  }
 catch (  IOException e) {
    Logger.getInstance(ProcessProxy.class).warn(e);
  }
}
