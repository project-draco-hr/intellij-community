{
  try {
synchronized (myLock) {
      if (myWriter != null) {
        myWriter.close();
      }
    }
    mySocket.close();
  }
 catch (  IOException e) {
    Logger.getInstance(ProcessProxy.class).warn(e);
  }
}
