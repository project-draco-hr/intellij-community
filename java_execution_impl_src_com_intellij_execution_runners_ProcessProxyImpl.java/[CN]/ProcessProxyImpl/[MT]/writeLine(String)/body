{
  try {
    ByteBuffer out=ByteBuffer.wrap((s + '\n').getBytes("US-ASCII"));
synchronized (myLock) {
      myConnection.write(out);
    }
  }
 catch (  IOException e) {
    Logger.getInstance(ProcessProxy.class).warn(e);
  }
}
