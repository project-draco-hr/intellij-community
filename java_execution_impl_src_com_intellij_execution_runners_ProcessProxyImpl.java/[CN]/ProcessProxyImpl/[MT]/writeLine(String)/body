{
  try {
synchronized (myLock) {
      myWriter.write(s);
      myWriter.write('\n');
      myWriter.flush();
    }
  }
 catch (  IOException e) {
    Logger.getInstance(ProcessProxy.class).warn(e);
  }
}
