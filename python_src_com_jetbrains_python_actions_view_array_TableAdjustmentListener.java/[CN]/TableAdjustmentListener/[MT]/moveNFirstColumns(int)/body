{
  for (int j=0; j < n; j++) {
    myTable.moveColumn(0,myTable.getColumnCount() - 1);
    myTable.getColumnModel().getColumn(myTable.getColumnCount() - 1).setHeaderValue(new Integer(myTable.getColumnModel().getColumn(myTable.getColumnCount() - 2).getHeaderValue().toString()) + 1);
  }
}
