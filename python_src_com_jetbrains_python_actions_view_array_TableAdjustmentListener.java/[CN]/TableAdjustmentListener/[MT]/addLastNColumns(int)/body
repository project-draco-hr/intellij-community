{
  DefaultTableModel dtm=(DefaultTableModel)myTable.getModel();
  int index=Integer.parseInt(myTable.getColumnModel().getColumn(mySize - 1).getHeaderValue().toString());
  for (int j=0; j < n; j++) {
    dtm.addColumn(index + j + 1,new Object[dtm.getRowCount()]);
  }
}
