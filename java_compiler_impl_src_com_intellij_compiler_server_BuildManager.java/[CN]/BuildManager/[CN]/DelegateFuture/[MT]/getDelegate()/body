{
  TaskFuture<? extends T> delegate=myDelegate;
  while (delegate == null) {
    try {
      wait();
    }
 catch (    InterruptedException ignored) {
    }
    delegate=myDelegate;
  }
  return delegate;
}
