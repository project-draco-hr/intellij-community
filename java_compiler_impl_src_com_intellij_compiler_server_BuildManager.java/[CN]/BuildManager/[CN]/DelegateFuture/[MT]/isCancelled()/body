{
  final TaskFuture<? extends T> delegate;
synchronized (this) {
    delegate=myDelegate;
    if (delegate == null) {
      return myRequestedCancelState != null;
    }
  }
  return delegate.isCancelled();
}
