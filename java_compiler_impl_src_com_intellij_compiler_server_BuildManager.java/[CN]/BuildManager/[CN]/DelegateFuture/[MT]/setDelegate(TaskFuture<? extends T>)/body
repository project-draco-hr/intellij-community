{
  if (myDelegate == null) {
    try {
      myDelegate=delegate;
      if (myRequestedCancelState != null) {
        myDelegate.cancel(myRequestedCancelState);
      }
    }
  finally {
      notifyAll();
    }
    return true;
  }
  return false;
}
