{
  myRegisteredExceptionBreakpoints.remove(properties);
  if (myDebugger.isConnected()) {
    final ExceptionBreakpointCommand command=ExceptionBreakpointCommand.removeExceptionBreakpointCommand(myDebugger,properties.getException());
    myDebugger.execute(command);
  }
}
