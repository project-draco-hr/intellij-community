{
  myRegisteredExceptionBreakpoints.add(properties);
  if (myDebugger.isConnected()) {
    final ExceptionBreakpointCommand command=ExceptionBreakpointCommand.addExceptionBreakpointCommand(myDebugger,properties.getException());
    myDebugger.execute(command);
  }
}
