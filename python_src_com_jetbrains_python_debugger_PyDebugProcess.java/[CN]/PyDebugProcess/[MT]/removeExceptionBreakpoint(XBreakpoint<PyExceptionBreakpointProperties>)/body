{
  myRegisteredExceptionBreakpoints.remove(breakpoint.getProperties().getException());
  if (myDebugger.isConnected()) {
    final ExceptionBreakpointCommand command=ExceptionBreakpointCommand.removeExceptionBreakpointCommand(myDebugger,breakpoint.getProperties().getException());
    myDebugger.execute(command);
  }
}
