{
  myRegisteredBreakpoints.put(position,breakpoint);
  if (isConnected()) {
    myDebugger.setBreakpointWithFuncName(breakpoint.getType().getId(),position.getFile(),position.getLine(),breakpoint.getCondition(),breakpoint.getLogExpression(),getFunctionName(breakpoint));
  }
}
