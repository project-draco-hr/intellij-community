{
  doTest("import java.sql.*;" + "class X {" + "    public void m(Driver driver) throws SQLException {"+ "        Connection connection = connection = driver.connect(\"foo\", null);\n"+ "        try {"+ "            System.out.println(connection);"+ "        } finally {"+ "            connection.close();"+ "        }"+ "    }"+ "}");
}
