{
  doTest("import java.sql.*;" + "class X {" + "  void m(PreparedStatement s) throws SQLException {"+ "    ResultSet r = s./*'ResultSet' should be opened in front of a 'try' block and closed in the corresponding 'finally' block*/executeQuery/**/();"+ "  }"+ "}");
}
