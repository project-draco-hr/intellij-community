{
  doTest("import java.sql.*;" + "class X {" + "  void m(Driver driver) throws SQLException {"+ "    /*'Connection' should be opened in front of a 'try' block and closed in the corresponding 'finally' block*/driver.connect(\"jdbc\", null)/**/;"+ "  }"+ "}");
}
