{
  doTest("import java.sql.*;" + "class X {" + "  void m(Driver driver) {"+ "    try (Connnection connection = driver.connect(\"asdf\", null);"+ "      PreparedStatement statement = connection.prepareStatement(\"SELECT *\");"+ "      ResultSet resultSet = statement.executeQuery()) {"+ "      while (resultSet.next()) {"+ "      }"+ "    }"+ "  }"+ "}");
}
