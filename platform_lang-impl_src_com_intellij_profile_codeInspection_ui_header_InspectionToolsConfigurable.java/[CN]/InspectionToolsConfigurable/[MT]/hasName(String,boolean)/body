{
  for (  Map.Entry<Profile,SingleInspectionProfilePanel> e : myPanels.entrySet()) {
    if (name.equals(e.getKey().getName()) && shared == e.getValue().isProfileShared()) {
      return true;
    }
  }
  return false;
}
