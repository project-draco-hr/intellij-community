{
  List<Ref> refs=new ArrayList<Ref>();
  if (trackedCommitHashes.contains(hash)) {
    for (    Ref ref : allRefs) {
      if (ref.getCommitHash().equals(hash)) {
        refs.add(ref);
      }
    }
  }
  return refs;
}
