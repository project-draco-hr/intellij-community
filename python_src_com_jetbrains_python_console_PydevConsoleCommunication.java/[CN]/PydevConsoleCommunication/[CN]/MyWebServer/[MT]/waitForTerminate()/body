{
  if (listener != null) {
    return new WaitFor(10000){
      @Override protected boolean condition(){
        return !listener.isAlive();
      }
    }
.isConditionRealized();
  }
  return true;
}
