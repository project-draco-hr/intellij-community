{
  String targetName=node.getName();
  if (targetName != null && targetName.equals("None")) {
    getHolder().createErrorAnnotation(node,(_op == Operation.Delete) ? "deleting None" : "assignment to None");
  }
}
