{
  int idx=newIndex;
  while (idx < 0 || (idx < newOrder.length && newOrder[idx] != null)) {
    idx++;
  }
  if (idx >= newOrder.length) {
    idx=newIndex - 1;
    while (idx >= 0 && (idx >= newOrder.length || newOrder[idx] != null)) {
      idx--;
    }
  }
  return idx == -1 ? -1 : idx;
}
