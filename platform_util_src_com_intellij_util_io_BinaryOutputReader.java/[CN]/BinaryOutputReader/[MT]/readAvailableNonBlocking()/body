{
  byte[] buffer=myBuffer;
  boolean read=false;
  int n;
  while (myStream.available() > 0 && (n=myStream.read(buffer)) >= 0) {
    if (n > 0) {
      read=true;
      onBinaryAvailable(buffer,n);
    }
  }
  return read;
}
