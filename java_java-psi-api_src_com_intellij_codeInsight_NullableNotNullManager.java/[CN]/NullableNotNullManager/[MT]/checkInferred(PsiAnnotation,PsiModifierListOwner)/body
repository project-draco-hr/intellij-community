{
  if (owner instanceof PsiMethod && AnnotationUtil.isInferredAnnotation(annotation) && AnnotationUtil.NOT_NULL.equals(annotation.getQualifiedName())&& PsiUtil.canBeOverriden((PsiMethod)owner)) {
    return null;
  }
  return annotation;
}
