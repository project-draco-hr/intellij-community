{
  return ContainerUtil.exists(myRequiredKwOnlyParams,new Condition<PyParameter>(){
    @Override public boolean value(    PyParameter parameter){
      return name != null && name.equals(parameter.getName());
    }
  }
);
}
