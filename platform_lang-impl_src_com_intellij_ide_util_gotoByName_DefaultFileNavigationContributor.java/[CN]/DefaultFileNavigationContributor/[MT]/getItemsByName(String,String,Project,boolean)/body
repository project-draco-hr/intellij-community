{
  final boolean includeDirs=pattern.endsWith("/") || pattern.endsWith("\\");
  return FilenameIndex.getFilesByName(project,name,includeNonProjectItems ? ProjectScope.getAllScope(project) : ProjectScope.getProjectScope(project),includeDirs);
}
