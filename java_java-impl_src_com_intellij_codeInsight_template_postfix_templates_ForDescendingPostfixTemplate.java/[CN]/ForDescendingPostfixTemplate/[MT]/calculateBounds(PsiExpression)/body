{
  String bound=getExpressionBound(expression);
  if (bound == null) {
    return null;
  }
  return JavaPostfixTemplatesUtils.isNumber(expression.getType()) ? Pair.create(bound,"0") : Pair.create(bound + " - 1","0");
}
