{
  return JavaPostfixTemplatesUtils.isNumber(expr.getType()) ? "for ($type$ $index$ = $bound$; $index$ > 0; $index$--) {\n$END$\n}" : "for ($type$ $index$ = $bound$ - 1; $index$ >= 0; $index$--) {\n$END$\n}";
}
