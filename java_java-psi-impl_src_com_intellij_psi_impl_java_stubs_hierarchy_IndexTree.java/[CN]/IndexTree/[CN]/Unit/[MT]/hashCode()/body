{
  int hash=myUnitType * 31 + Arrays.hashCode(myPackageName);
  for (  ClassDecl decl : myDecls) {
    int name=decl.myName;
    if (name != 0) {
      return hash * 31 + name;
    }
  }
  return hash;
}
