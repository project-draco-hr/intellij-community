{
  ASTNode slash=next.getTreePrev();
  if (slash instanceof PsiWhiteSpace && slash.getText().contains("\\"))   statement.deleteChildInternal(slash);
 else {
    slash=next.getTreeNext();
    if (slash instanceof PsiWhiteSpace && slash.getText().contains("\\"))     statement.deleteChildInternal(slash);
  }
}
