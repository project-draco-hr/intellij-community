{
  myLexerState=myLexer.getState();
  Pair<String,ConsoleViewContentType> res=Pair.create(myLexer.getTokenText(),new ConsoleViewContentType("",convertAttributes(myPyHighlighter.getTokenHighlights(myLexer.getTokenType()))));
  myLexer.advance();
  return res;
}
