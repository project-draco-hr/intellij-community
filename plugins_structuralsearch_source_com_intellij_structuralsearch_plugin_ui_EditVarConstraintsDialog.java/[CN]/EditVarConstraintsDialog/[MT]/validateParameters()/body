{
  return validateRegExp(regexp) && validateRegExp(regexprForExprType) && validateIntOccurence(minoccurs)&& validateScript(customScriptCode.getTextField())&& (maxoccursUnlimited.isSelected() || validateIntOccurence(maxoccurs));
}
