{
  return validateRegExp(regexp) && validateRegExp(regexprForExprType) && (minoccursUnlimited.isSelected() || validateIntOccurence(minoccurs))&& (maxoccursUnlimited.isSelected() || validateIntOccurence(maxoccurs));
}
