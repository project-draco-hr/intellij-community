{
  executeOnPooledThread(new Runnable(){
    @Override public void run(){
      try {
        deleteUserPreferences(builder);
      }
 catch (      IOException e) {
        LOG.debug(e);
      }
    }
  }
);
}
