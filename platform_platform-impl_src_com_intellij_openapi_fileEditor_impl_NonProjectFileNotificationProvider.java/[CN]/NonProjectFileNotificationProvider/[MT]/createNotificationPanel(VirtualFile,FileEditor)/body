{
  NonProjectFileWritingAccessProvider.AccessStatus status=NonProjectFileWritingAccessProvider.getAccessStatus(myProject,file);
  if (status != NonProjectFileWritingAccessProvider.AccessStatus.REQUESTED)   return null;
  return new NonProjectFileNotificationPanel(myProject,file);
}
