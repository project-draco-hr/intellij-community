{
  try {
    InputStream sourceStream=getClass().getResourceAsStream(MAP_PROPERTIES);
    try {
      myProperties.load(sourceStream);
    }
  finally {
      sourceStream.close();
    }
  }
 catch (  IOException e) {
    LOG.error(e);
  }
}
