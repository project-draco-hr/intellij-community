{
  PsiExpression previousExpression=null;
  for (  PsiExpression expression : expressions) {
    if (previousExpression != null && (isOne(expression) || EquivalenceChecker.expressionsAreEquivalent(previousExpression,expression))) {
      return true;
    }
    previousExpression=expression;
  }
  return false;
}
