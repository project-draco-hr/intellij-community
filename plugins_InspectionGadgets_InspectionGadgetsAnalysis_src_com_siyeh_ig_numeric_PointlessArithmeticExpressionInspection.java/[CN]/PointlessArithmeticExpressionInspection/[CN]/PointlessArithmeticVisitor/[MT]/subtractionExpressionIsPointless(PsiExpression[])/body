{
  PsiExpression previousExpression=null;
  for (  PsiExpression expression : expressions) {
    if (previousExpression != null && (isZero(expression) || EquivalenceChecker.expressionsAreEquivalent(previousExpression,expression))) {
      return true;
    }
    previousExpression=expression;
  }
  return false;
}
