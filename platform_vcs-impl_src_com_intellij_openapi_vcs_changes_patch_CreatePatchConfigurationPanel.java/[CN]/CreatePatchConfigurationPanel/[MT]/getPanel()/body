{
  return new ComplexFocusedComponentWrapper(!myIncludeBaseRevisionTextCheckBox.isVisible() || myChanges.isEmpty() ? myMainPanel : myPanelWithSelectedFiles){
    @Override public JComponent getPreferredFocusedSimpleComponent(){
      return getPreferredFocusComponent();
    }
  }
;
}
