{
  return !Comparing.equal(getExternalizableJarPath(),mySettings.greclipsePath) || !Comparing.equal(myCmdLineParams.getText(),mySettings.cmdLineParams) || !Comparing.equal(myGenerateDebugInfo.isSelected(),mySettings.debugInfo);
}
