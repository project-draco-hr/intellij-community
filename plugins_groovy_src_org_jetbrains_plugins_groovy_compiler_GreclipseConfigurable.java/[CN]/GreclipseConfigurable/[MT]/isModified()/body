{
  return !Comparing.equal(myJarPath.getText(),mySettings.greclipsePath) || !Comparing.equal(myCmdLineParams.getText(),mySettings.cmdLineParams) || !Comparing.equal(myGenerateDebugInfo.isSelected(),mySettings.debugInfo);
}
