{
  return new TreeConflictDescription(conflict.getPath(),conflict.getNodeKind(),conflict.getConflictAction(),conflict.getConflictReason(),conflict.getOperation(),conflict.getSourceLeftVersion(),conflict.getSourceRightVersion());
}
