{
  Integer delegateIndex=myDelegateGraph.getNodeIndexById(nodeId);
  if (delegateIndex == null)   return null;
  if (myDelegateNodesVisibility.get(delegateIndex))   return myNodesMap.getShortIndex(delegateIndex);
 else   return null;
}
