{
  if ((elements == null && myMatchingVisitor.isLeftLooseMatching()) || elements == elements2) {
    return true;
  }
  return myMatchingVisitor.matchInAnyOrder(elements.getReferenceElements(),(elements2 != null) ? elements2.getReferenceElements() : PsiElement.EMPTY_ARRAY);
}
