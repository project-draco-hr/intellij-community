{
  List<GraphEdge> result=ContainerUtil.newArrayList();
  if (nodeIndex != 0 && mySimpleNodes.get(nodeIndex - 1))   result.add(new GraphEdge(nodeIndex - 1,nodeIndex,USUAL));
  for (int i=myNodeToEdgeIndex.get(nodeIndex); i < myNodeToEdgeIndex.get(nodeIndex + 1); i++) {
    int node=myLongEdges.get(i);
    if (node < 0)     result.add(new GraphEdge(nodeIndex,null,node,GraphEdgeType.NOT_LOAD_COMMIT));
 else     if (nodeIndex < node)     result.add(new GraphEdge(nodeIndex,node,USUAL));
 else     result.add(new GraphEdge(node,nodeIndex,USUAL));
  }
  if (mySimpleNodes.get(nodeIndex))   result.add(new GraphEdge(nodeIndex,nodeIndex + 1,USUAL));
  return result;
}
