{
  GitPushNativeResult nr=new GitPushNativeResult(nativeType,from);
  return GitPushRepoResult.addUpdateResult(convertFromNative(nr,Collections.<GitPushNativeResult>emptyList(),commits,from(from),to(to)),updateResult);
}
