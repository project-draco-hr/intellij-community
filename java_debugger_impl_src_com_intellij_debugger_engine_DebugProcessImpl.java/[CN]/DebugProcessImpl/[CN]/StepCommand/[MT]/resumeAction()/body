{
  SuspendContextImpl context=getSuspendContext();
  if (context != null && Registry.is("debugger.step.resumes.one.thread") && context.getSuspendPolicy() == EventRequest.SUSPEND_ALL) {
    getSuspendManager().resumeThread(context,myContextThread);
  }
 else {
    super.resumeAction();
  }
}
