{
  final byte[] forcedContent=getUserData(ourForcedContentKey);
  return VfsUtilCore.inputStreamSkippingBOM(forcedContent == null ? ourPersistence.getInputStream(this) : new DataInputStream(new UnsyncByteArrayInputStream(forcedContent)),this);
}
