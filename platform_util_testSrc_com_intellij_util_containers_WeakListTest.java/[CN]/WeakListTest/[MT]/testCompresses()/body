{
  for (int i=0; i < 20; i++) {
    addElement(new Object());
  }
  assertEquals(20,myWeakList.listSize());
  String obj="xxx";
  addElement(obj);
  myHolder.clear();
  gc();
  myWeakList.remove(new Object());
  assertEquals(1,myWeakList.listSize());
}
