{
  int index=typeString.indexOf(", optional");
  if (index >= 0) {
    return typeString.substring(0,index);
  }
  index=typeString.indexOf(" of ");
  if (index >= 0) {
    return typeString.substring(0,index);
  }
  return typeString;
}
