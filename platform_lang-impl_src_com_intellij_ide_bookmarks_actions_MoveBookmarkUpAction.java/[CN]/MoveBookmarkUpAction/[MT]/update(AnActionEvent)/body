{
  e.getPresentation().setEnabled(!UISettings.getInstance().SORT_BOOKMARKS && BookmarksAction.notFiltered(myList) && BookmarksAction.getSelectedBookmarks(myList).size() == 1 && myList.getSelectedIndex() > 0);
}
