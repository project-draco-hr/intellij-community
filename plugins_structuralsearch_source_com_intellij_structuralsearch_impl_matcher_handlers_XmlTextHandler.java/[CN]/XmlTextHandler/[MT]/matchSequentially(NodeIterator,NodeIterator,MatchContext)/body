{
  final PsiElement psiElement=nodes.current();
  return GlobalMatchingVisitor.continueMatchingSequentially(new SsrFilteringNodeIterator(new ArrayBackedNodeIterator(psiElement.getChildren())),nodes2,context);
}
