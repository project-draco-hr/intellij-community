{
  final PsiElement psiElement=nodes.current();
  return GlobalMatchingVisitor.continueMatchingSequentially(new FilteringNodeIterator(new ArrayBackedNodeIterator(psiElement.getChildren())),nodes2,context);
}
