{
  final PsiElement psiElement=nodes.current();
  return MatchingVisitor.continueMatchingSequentially(new FilteringNodeIterator(new ArrayBackedNodeIterator(psiElement.getChildren())),nodes2,context);
}
