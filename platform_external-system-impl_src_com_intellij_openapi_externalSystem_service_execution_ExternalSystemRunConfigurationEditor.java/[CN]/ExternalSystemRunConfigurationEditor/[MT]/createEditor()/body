{
  PaintAwarePanel result=new PaintAwarePanel(new GridBagLayout());
  myControl.fillUi(result,0);
  result.add(new Box.Filler(new Dimension(0,0),new Dimension(0,200),new Dimension(0,0)),ExternalSystemUiUtil.getFillLineConstraints(0));
  ExternalSystemUiUtil.fillBottom(result);
  return result;
}
