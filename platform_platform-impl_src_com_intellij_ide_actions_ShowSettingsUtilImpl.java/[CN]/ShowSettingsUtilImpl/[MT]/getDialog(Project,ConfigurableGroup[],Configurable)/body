{
  return Registry.is("ide.perProjectModality") ? new OptionsEditorDialog(getProject(project),filterEmptyGroups(groups),toSelect,true) : new OptionsEditorDialog(getProject(project),filterEmptyGroups(groups),toSelect);
}
