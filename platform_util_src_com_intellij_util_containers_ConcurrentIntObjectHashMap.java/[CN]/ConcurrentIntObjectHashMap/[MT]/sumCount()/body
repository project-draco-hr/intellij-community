{
  ConcurrentHashMap.CounterCell[] as=counterCells;
  long sum=baseCount;
  if (as != null) {
    for (    ConcurrentHashMap.CounterCell a : as) {
      if (a != null) {
        sum+=a.value;
      }
    }
  }
  return sum;
}
