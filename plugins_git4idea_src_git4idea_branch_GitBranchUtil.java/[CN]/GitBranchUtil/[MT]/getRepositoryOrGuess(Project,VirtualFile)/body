{
  if (project.isDisposed())   return null;
  return DvcsUtil.guessRepositoryForFile(project,GitUtil.getRepositoryManager(project),GitVcs.getInstance(project),file,GitVcsSettings.getInstance(project).getRecentRootPath());
}
