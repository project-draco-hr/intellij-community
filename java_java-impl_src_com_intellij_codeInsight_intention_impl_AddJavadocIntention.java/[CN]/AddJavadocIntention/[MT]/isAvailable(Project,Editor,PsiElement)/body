{
  if (!(element instanceof PsiIdentifier)) {
    return false;
  }
  final PsiElement docCommentOwner=element.getParent();
  return docCommentOwner instanceof PsiDocCommentOwner && !(docCommentOwner instanceof PsiAnonymousClass) && ((PsiDocCommentOwner)docCommentOwner).getDocComment() == null;
}
