{
  foo(1,new Function<Integer,Boolean>(){
    public boolean apply(    Integer i){
      if (i > 0) {
        System.out.println(i);
        System.out.println(i);
        return true;
      }
      return false;
    }
  }
);
}
