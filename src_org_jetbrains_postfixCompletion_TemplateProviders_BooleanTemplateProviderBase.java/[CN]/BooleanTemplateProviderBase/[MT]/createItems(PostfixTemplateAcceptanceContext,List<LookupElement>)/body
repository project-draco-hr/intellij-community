{
  for (  PrefixExpressionContext expression : context.expressions)   if (isBooleanExpression(expression) && createBooleanItems(expression,consumer))   return;
  if (context.isForceMode)   for (  PrefixExpressionContext expression : context.expressions)   if (createBooleanItems(expression,consumer))   return;
}
