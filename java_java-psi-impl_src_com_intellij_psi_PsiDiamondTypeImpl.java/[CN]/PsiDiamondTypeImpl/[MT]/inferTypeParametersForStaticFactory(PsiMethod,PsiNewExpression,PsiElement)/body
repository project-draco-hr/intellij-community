{
  final PsiExpressionList argumentList=expression.getArgumentList();
  if (argumentList != null) {
    final MethodCandidateInfo staticFactoryCandidateInfo=new MethodCandidateInfo(staticFactoryMethod,PsiSubstitutor.EMPTY,false,false,argumentList,parent,argumentList.getExpressionTypes(),null){
      @Override protected PsiElement getParent(){
        return parent;
      }
    }
;
    return staticFactoryCandidateInfo.getSubstitutor();
  }
 else {
    return PsiSubstitutor.EMPTY;
  }
}
