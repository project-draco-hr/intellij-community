{
  return ApplicationManager.getApplication().runReadAction(new Computable<ModifiableModuleModel>(){
    @Override public ModifiableModuleModel compute(){
      return ModuleManager.getInstance(myProject).getModifiableModel();
    }
  }
);
}
