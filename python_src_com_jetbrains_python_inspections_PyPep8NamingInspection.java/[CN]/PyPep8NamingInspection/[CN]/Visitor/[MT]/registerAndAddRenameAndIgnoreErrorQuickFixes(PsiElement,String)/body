{
  if (getHolder() != null && getHolder().isOnTheFly())   registerProblem(node,errorCodesDescription.get(errorCode),new PyRenameElementQuickFix(),new IgnoreErrorFix(errorCode));
 else   registerProblem(node,errorCodesDescription.get(errorCode),new IgnoreErrorFix(errorCode));
}
