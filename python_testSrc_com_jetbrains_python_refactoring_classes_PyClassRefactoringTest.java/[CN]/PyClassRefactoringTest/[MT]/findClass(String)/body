{
  final Project project=myFixture.getProject();
  final Collection<PyClass> classes=StubIndex.getInstance().get(PyClassNameIndex.KEY,name,project,ProjectScope.getProjectScope(project));
  assertEquals(1,classes.size());
  return classes.iterator().next();
}
