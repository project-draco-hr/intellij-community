{
  String extension=FileUtil.getExtension(fileName);
  String source=TestUtils.loadFile(fileName);
  String expected=TestUtils.loadFile(expectedFileName);
  doTest(source,what,by,expected,patternFileType,patternFileExtension,FileTypeManager.getInstance().getFileTypeByExtension(extension),extension);
}
