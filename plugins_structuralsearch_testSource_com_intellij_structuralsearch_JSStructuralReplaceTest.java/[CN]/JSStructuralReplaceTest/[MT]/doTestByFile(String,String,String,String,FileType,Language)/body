{
  String extension=FileUtilRt.getExtension(fileName);
  String source=TestUtils.loadFile(fileName);
  String expected=TestUtils.loadFile(expectedFileName);
  doTest(source,what,by,expected,patternFileType,patternFileDialect,FileTypeManager.getInstance().getFileTypeByExtension(extension),null);
}
