{
  doTest("import org.apache.logging.log4j.*;" + "class Logging {" + "  private static final Logger LOG = LogManager.getLogger();"+ "  void m(int i) {"+ "    LOG./*Non-constant string concatenation as argument to 'info()' logging call*/info/**/(\"hello? \" + i);"+ "    LOG./*Non-constant string concatenation as argument to 'fatal()' logging call*/fatal/**/(\"you got me \" + i);"+ "  }"+ "}");
}
