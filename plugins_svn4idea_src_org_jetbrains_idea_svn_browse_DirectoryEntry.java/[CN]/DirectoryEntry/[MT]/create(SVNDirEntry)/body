{
  return new DirectoryEntry(entry.getURL(),entry.getRepositoryRoot(),entry.getName(),entry.getKind(),new CommitInfo.Builder(entry.getRevision(),entry.getDate(),entry.getAuthor()).build(),entry.getRelativePath());
}
