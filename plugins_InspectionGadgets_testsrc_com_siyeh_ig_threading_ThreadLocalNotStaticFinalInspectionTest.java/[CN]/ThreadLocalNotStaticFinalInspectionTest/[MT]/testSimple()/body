{
  doTest("class A {" + "  private ThreadLocal /*ThreadLocal 'tl' is not declared 'static final'*/tl/**/ = new ThreadLocal() {" + "    @Override "+ "    protected Object initialValue() {"+ "      return Boolean.TRUE;"+ "    }"+ "  };"+ "}");
}
