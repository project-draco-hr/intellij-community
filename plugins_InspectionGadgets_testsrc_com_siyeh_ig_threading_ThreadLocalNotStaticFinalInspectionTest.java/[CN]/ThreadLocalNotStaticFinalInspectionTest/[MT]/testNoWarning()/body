{
  doTest("class A {" + "  private static final ThreadLocal tl = new ThreadLocal() {" + "    @Override "+ "    protected Object initialValue() {"+ "      return Boolean.TRUE;"+ "    }"+ "  };"+ "}");
}
