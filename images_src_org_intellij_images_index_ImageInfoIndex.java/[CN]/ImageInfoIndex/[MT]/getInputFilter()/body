{
  return new DefaultFileTypeSpecificInputFilter(ImageFileTypeManager.getInstance().getImageFileType()){
    @Override public boolean acceptInput(    @NotNull final VirtualFile file){
      return file.isInLocalFileSystem() && file.getLength() / 1024 < ourMaxImageSize;
    }
  }
;
}
