{
  String result=containingFilePath;
  for (int i=0; i < endElement; i++) {
    String arg=evaluate(args[i],result,visited);
    if (arg == null) {
      return null;
    }
    result=arg;
  }
  return result;
}
