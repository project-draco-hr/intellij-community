{
  final String annotation=PyTypingTypeProvider.getTypeCommentValue(comment.getText());
  return annotation != null && (findAssignmentTypeCommentTarget(comment) != null || findForLoopTypeCommentTarget(comment) != null || findWithStatementTypeCommentTarget(comment) != null);
}
