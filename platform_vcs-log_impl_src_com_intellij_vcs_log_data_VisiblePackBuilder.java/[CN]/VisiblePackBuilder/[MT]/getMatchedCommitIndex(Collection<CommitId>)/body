{
  if (commits == null) {
    return null;
  }
  return ContainerUtil.map2Set(commits,new Function<CommitId,Integer>(){
    @Override public Integer fun(    CommitId commitId){
      return myHashMap.getCommitIndex(commitId.getHash(),commitId.getRoot());
    }
  }
);
}
