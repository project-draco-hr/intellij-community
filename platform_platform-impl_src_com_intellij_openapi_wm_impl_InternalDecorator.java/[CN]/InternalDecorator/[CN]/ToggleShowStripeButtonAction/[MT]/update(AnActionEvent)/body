{
  e.getPresentation().setText(myInfo.isShowStripeButton() ? ActionsBundle.message("action.ToggleShowStripeButton.text") : ActionsBundle.message("action.ToggleShowStripeButton.text2"));
  e.getPresentation().setEnabledAndVisible(!myInfo.isFloating() && !myInfo.isWindowed());
}
