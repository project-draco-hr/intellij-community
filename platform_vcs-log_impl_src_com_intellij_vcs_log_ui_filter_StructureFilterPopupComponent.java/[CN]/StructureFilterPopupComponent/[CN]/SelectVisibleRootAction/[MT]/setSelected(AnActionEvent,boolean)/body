{
  if (!isEnabled()) {
    setVisibleOnly(myRoot);
  }
 else {
    if ((e.getModifiers() & InputEvent.CTRL_MASK) != 0) {
      setVisibleOnly(myRoot);
    }
 else {
      setVisible(myRoot,state);
    }
  }
}
