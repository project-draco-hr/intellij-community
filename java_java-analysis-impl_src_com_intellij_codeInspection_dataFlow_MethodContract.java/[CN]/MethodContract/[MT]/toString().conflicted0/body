{
  return StringUtil.join(arguments,new Function<ValueConstraint,String>(){
    @Override public String fun(    ValueConstraint constraint){
      return constraint.toString();
    }
  }
,", ") + " -> " + returnValue;
}
