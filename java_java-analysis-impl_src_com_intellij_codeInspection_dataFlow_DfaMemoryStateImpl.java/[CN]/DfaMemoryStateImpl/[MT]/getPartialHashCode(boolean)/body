{
  int hash=((getNonTrivialEqClasses().hashCode() * 31 + getDistinctClassPairs().hashCode()) * 31 + myStack.hashCode()) * 31 + myVariableStates.hashCode();
  if (unknowns) {
    hash=hash * 31 + myUnknownVariables.hashCode();
  }
  return hash;
}
