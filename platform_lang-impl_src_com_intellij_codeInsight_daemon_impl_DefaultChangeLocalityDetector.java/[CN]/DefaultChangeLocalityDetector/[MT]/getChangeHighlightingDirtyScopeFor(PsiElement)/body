{
  if (changedElement instanceof PsiWhiteSpace || changedElement instanceof PsiComment && !changedElement.getText().contains(SuppressionUtilCore.SUPPRESS_INSPECTIONS_TAG_NAME)) {
    return changedElement;
  }
  return null;
}
