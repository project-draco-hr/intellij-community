{
  try {
    return new TemplateResource[]{new TemplateResource("IntelliJ Default",readFile(DEFAULT),true)};
  }
 catch (  IOException e) {
    throw new TemplateResourceException("Error loading default templates",e);
  }
}
