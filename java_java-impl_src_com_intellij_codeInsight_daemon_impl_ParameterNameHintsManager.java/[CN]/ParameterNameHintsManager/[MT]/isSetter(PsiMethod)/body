{
  String methodName=method.getName();
  if (method.getParameterList().getParametersCount() == 1 && methodName.startsWith("set") && methodName.length() > 3 && Character.isUpperCase(methodName.charAt(3))) {
    return true;
  }
  return false;
}
