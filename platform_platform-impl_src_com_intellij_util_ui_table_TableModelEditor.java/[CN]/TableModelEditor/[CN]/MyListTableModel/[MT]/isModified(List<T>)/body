{
  if (items.size() == oldItems.size()) {
    for (int i=0, size=items.size(); i < size; i++) {
      if (!items.get(i).equals(oldItems.get(i))) {
        return true;
      }
    }
  }
 else {
    return true;
  }
  return false;
}
