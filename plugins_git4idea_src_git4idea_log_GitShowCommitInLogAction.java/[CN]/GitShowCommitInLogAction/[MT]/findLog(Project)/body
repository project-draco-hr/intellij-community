{
  VcsLogManager manager=VcsLogManager.getInstance(project);
  if (manager != null) {
    VcsLogUiImpl ui=manager.getMainLogUi();
    if (ui != null) {
      return ui.getVcsLog();
    }
  }
  return null;
}
