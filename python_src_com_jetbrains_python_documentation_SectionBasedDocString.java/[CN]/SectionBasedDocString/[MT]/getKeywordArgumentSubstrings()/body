{
  return ContainerUtil.mapNotNull(getKeywordArgumentFields(),new Function<SectionField,Substring>(){
    @Override public Substring fun(    SectionField field){
      return field.getNameAsSubstring();
    }
  }
);
}
