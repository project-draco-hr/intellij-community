{
  return ContainerUtil.mapNotNull(mySections,new Function<Section,Section>(){
    @Override public Section fun(    Section section){
      return section.getNormalizedTitle().equals(title) ? section : null;
    }
  }
);
}
