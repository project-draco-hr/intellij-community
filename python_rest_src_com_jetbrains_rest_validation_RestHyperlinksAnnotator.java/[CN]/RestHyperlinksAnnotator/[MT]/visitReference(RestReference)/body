{
  if (node.getText().matches("`[^`]*<[^`]+>`_(_)?"))   return;
  if (node.resolve() == null)   getHolder().createWarningAnnotation(node,RestBundle.message("ANN.unknown.target",node.getReferenceText()));
}
