{
  return ContainerUtil.map(plugins,new Function<IdeaPluginDescriptor,PluginId>(){
    @Override public PluginId fun(    IdeaPluginDescriptor descriptor){
      return descriptor.getPluginId();
    }
  }
);
}
