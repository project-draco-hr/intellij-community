{
  return ContainerUtil.map(expression.getStringNodes(),new Function<ASTNode,StringNodeInfo>(){
    @Override public StringNodeInfo fun(    ASTNode node){
      return new StringNodeInfo(node);
    }
  }
);
}
