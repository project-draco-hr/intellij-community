{
  if (element instanceof CommandLineFile) {
    final Command command=((CommandLineFile)element).findRealCommand();
    if (command != null) {
      return command.getHelp(false);
    }
  }
  return null;
}
