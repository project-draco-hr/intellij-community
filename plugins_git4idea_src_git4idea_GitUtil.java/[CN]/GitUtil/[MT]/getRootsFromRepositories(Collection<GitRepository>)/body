{
  return ContainerUtil.map(repositories,new Function<GitRepository,VirtualFile>(){
    @Override public VirtualFile fun(    @NotNull GitRepository repository){
      return repository.getRoot();
    }
  }
);
}
