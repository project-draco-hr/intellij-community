{
  doTestFieldType("myField","Type",myJavaFacade.getElementFactory().createTypeFromText("Ancestor",null).createArrayType().createArrayType(),myJavaFacade.getElementFactory().createTypeFromText("Subject",null).createArrayType().createArrayType());
}
