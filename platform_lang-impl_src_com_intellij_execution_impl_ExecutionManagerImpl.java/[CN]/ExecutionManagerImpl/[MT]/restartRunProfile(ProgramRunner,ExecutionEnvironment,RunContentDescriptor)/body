{
  restartRunProfile(environment.getProject(),environment.getDataContext(),runner,environment.getRunProfile(),environment.getExecutor(),environment.getExecutionTarget(),environment.getRunnerAndConfigurationSettings(),currentDescriptor);
}
