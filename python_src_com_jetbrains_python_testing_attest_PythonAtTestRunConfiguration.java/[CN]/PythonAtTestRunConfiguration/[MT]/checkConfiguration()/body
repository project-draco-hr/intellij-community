{
  super.checkConfiguration();
  if (!VFSTestFrameworkListener.getInstance().isAtTestInstalled(getSdkHome()))   throw new RuntimeConfigurationWarning("No attest runner found in selected interpreter");
}
