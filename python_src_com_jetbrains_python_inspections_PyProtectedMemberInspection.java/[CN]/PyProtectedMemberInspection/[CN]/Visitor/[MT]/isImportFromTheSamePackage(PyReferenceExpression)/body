{
  PsiDirectory directory=importSource.getContainingFile().getContainingDirectory();
  if (PyUtil.isPackage(directory,true,importSource.getContainingFile()) && directory.getName().equals(importSource.getName())) {
    return true;
  }
  return false;
}
