{
  schemeToInfo.forEachValue(new TObjectProcedure<ExternalInfo>(){
    @Override public boolean execute(    ExternalInfo info){
      scheduleDelete(info);
      return true;
    }
  }
);
  myCurrentScheme=null;
  mySchemes.clear();
  schemeToInfo.clear();
}
