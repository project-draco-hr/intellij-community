{
  final List<String> pythonPath=Lists.newArrayList(PythonCommandLineState.getAddedPaths(mySdk));
  if (addProjectRoot)   pythonPath.addAll(PythonCommandLineState.collectPythonPath(myModule));
  return pythonPath;
}
