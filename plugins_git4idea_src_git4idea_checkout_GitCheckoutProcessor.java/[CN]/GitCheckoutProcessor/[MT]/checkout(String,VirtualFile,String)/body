{
  IdeFrame frame=IdeFocusManager.getGlobalInstance().getLastFocusedFrame();
  return GitCheckoutProvider.doClone(frame == null || frame.getProject() == null ? ProjectManager.getInstance().getDefaultProject() : frame.getProject(),ServiceManager.getService(Git.class),directoryName,parentDirectory.getPath(),url);
}
