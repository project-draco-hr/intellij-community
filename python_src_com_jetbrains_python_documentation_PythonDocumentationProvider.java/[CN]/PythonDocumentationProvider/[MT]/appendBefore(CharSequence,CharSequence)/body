{
  StringBuilder result=new StringBuilder(toAdd);
  for (int i=0; i < text.length(); i++) {
    char c=text.charAt(i);
    result.append(c);
    if (c == '\n') {
      result.append(toAdd);
    }
  }
  return result;
}
