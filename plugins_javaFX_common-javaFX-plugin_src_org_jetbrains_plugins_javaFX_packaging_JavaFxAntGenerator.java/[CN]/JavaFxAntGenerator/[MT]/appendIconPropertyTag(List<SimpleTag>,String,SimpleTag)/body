{
  final SimpleTag condition=new SimpleTag("condition",Couple.of("property","app.icon.path"),Couple.of("value","${basedir}/" + iconPath));
  condition.add(osFamily);
  tagsCollector.add(condition);
}
