{
  String desc="Remote changes need to be merged before pushing.";
  if (myRebaseOverMergeProblemDetected) {
    desc+="<br/><br/>In this case <b>merge is highly recommended</b>, because there are non-pushed merge commits. " + "<br/>Rebasing them can lead to problems.";
  }
  return desc;
}
