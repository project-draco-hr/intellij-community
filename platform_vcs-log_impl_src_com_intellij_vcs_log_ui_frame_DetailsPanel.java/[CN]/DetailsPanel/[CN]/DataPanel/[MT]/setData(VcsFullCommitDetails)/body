{
  if (commit == null) {
    myMainText=null;
  }
 else {
    String header=(myMultiRoot ? "[" + commit.getRoot().getName() + "] " : "") + commit.getId().toShortString() + " "+ getAuthorText(commit);
    String body=getMessageText(commit);
    myMainText=header + "<br/>" + body;
  }
  update();
}
