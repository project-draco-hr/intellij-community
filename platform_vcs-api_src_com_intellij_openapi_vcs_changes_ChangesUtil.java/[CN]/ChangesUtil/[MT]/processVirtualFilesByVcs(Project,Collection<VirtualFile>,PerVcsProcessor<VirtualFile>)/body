{
  processItemsByVcs(files,new VcsSeparator<VirtualFile>(){
    @Override public AbstractVcs getVcsFor(    @NotNull VirtualFile item){
      return getVcsForFile(item,project);
    }
  }
,processor);
}
