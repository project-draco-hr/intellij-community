{
  processItemsByVcs(files,new VcsSeparator<FilePath>(){
    @Override public AbstractVcs getVcsFor(    @NotNull FilePath item){
      return getVcsForFile(item.getIOFile(),project);
    }
  }
,processor);
}
