{
  for (  Import anImport : Translator.getDefaultImports(info.type,myNameEnvironment))   handleImport(anImport,name,symbols);
  for (  Import anImport : info.imports)   handleImport(anImport,name,symbols);
}
