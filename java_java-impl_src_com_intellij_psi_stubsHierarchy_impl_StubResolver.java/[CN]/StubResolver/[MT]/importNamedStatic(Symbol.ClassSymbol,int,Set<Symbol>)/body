{
  new Object(){
    Set<Symbol> processed=new HashSet<Symbol>();
    void importFrom(    Symbol.ClassSymbol cs) throws IncompleteHierarchyException {
      if (cs == null || !processed.add(cs))       return;
      for (      Symbol.ClassSymbol c : cs.getSuperClasses(myConnector))       importFrom(c);
      processMembers(cs.getMembers(),name,symbols,true);
    }
  }
.importFrom(tsym);
}
