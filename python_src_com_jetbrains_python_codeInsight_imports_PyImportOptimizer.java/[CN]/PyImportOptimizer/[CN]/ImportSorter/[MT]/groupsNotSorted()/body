{
  final Ordering<PyImportStatementBase> importOrdering=Ordering.from(AddImportHelper.IMPORT_BY_NAME_COMPARATOR);
  return SORT_IMPORTS && (!importOrdering.isOrdered(myBuiltinImports) || !importOrdering.isOrdered(myProjectImports) || !importOrdering.isOrdered(myThirdPartyImports));
}
