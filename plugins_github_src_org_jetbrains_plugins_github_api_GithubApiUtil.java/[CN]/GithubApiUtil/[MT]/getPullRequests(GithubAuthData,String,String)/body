{
  String path="/repos/" + user + "/"+ repo+ "/pulls?per_page=100";
  PagedRequest<GithubPullRequest> request=new PagedRequest<GithubPullRequest>(path,GithubPullRequest.class,GithubPullRequestRaw[].class);
  return request.getAll(auth);
}
