{
  String path="/repos/" + owner + "/"+ name+ "/forks?"+ PER_PAGE;
  PagedRequest<GithubRepo> requestWatched=new PagedRequest<>(path,GithubRepo.class,GithubRepoRaw[].class,ACCEPT_V3_JSON);
  return requestWatched.getAll(connection);
}
