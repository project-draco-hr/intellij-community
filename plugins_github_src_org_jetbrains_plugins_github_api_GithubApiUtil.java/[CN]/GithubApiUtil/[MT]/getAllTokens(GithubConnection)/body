{
  try {
    String path="/authorizations";
    PagedRequest<GithubAuthorization> request=new PagedRequest<>(path,GithubAuthorization.class,GithubAuthorizationRaw[].class,ACCEPT_V3_JSON);
    return request.getAll(connection);
  }
 catch (  GithubConfusingException e) {
    e.setDetails("Can't get available tokens");
    throw e;
  }
}
