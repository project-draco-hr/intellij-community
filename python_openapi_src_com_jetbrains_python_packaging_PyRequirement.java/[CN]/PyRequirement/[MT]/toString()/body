{
  return myName + StringUtil.join(myVersionSpecs,new Function<PyRequirementVersionSpec,String>(){
    @Override public String fun(    PyRequirementVersionSpec spec){
      return spec.toString();
    }
  }
,",");
}
