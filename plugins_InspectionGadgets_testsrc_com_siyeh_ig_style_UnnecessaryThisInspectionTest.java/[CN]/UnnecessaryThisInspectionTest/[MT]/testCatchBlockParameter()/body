{
  doTest("class A {" + "    private Throwable throwable = null;" + "    public void method() {"+ "        try {"+ "        } catch (Throwable throwable) {"+ "            this.throwable = throwable;"+ "            throwable.printStackTrace();"+ "        }"+ "    }"+ "}");
}
