{
  Color rootIndicatorColor=VcsLogColorManagerImpl.getIndicatorColor(myUi.getColorManager().getRootColor(myRoot));
  myReferencePainter.paint(myGroup.getName(),g,0,(getHeight() - myReferencePainter.getHeight(this)) / 2,myGroup.getBgColor(),rootIndicatorColor);
}
