{
  return ContainerUtil.filter(myElements,new Condition<PipelineElement>(){
    @Override public boolean value(    PipelineElement element){
      return methodName.equals(element.getMethodName());
    }
  }
);
}
