{
  final PyReferenceExpression importReference=psi.getImportReference();
  final PyTargetExpression asName=psi.getAsName();
  return new PyImportElementStubImpl(importReference != null ? importReference.getText() : "",asName != null ? asName.getText() : "",parentStub);
}
