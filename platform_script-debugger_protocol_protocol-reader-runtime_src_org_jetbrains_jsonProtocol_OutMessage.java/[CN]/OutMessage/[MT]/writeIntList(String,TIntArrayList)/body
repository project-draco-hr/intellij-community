{
  try {
    beginArguments();
    writer.name(name);
    writer.beginArray();
    for (int i=0; i < value.size(); i++) {
      writer.value(value.getQuick(i));
    }
    writer.endArray();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
