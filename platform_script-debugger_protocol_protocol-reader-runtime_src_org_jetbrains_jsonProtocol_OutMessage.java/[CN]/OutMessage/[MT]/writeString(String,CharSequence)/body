{
  if (value != null) {
    try {
      prepareWriteRaw(this,name);
      JsonUtil.escape(value,stringWriter.getBuffer());
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
}
