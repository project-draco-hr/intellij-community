{
  if (value != null) {
    try {
      beginArguments();
      prepareWriteRaw(this,name);
      JsonUtil.escape(value,buffer);
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
}
