{
  try {
    beginArguments();
    prepareWriteRaw(this,name);
    if (!value.finalized) {
      value.close();
    }
    buffer.writeBytes(value.buffer);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
