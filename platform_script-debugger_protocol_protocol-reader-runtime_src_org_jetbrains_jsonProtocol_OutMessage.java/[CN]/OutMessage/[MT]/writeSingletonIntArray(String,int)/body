{
  try {
    beginArguments();
    writer.name(name);
    writer.beginArray();
    writer.value(value);
    writer.endArray();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
