{
  try {
    beginArguments();
    writer.name(name);
    writer.beginArray();
    for (    int v : value) {
      writer.value(v);
    }
    writer.endArray();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
