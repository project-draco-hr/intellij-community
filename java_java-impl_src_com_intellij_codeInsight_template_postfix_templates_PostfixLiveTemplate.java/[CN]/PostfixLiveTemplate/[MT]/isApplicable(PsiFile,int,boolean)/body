{
  PostfixTemplatesSettings settings=PostfixTemplatesSettings.getInstance();
  return !wrapping && file != null && settings != null && settings.isPostfixTemplatesEnabled() && PsiUtilCore.getLanguageAtOffset(file,offset) == JavaLanguage.INSTANCE;
}
