{
  return new LinkedHashSet<Fact>(ContainerUtil.filter(getFacts(state),new Condition<Fact>(){
    @Override public boolean value(    Fact another){
      return !fact.invalidatesFact(another);
    }
  }
));
}
