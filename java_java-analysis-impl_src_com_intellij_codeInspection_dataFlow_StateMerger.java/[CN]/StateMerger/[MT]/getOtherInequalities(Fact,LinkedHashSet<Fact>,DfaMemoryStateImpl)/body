{
  Set<DfaConstValue> otherInequalities=ContainerUtil.newLinkedHashSet();
  for (  Fact candidate : memberFacts) {
    if (candidate.myType == FactType.equality && !candidate.myPositive && candidate.myVar == removedFact.myVar && !state.areEquivalent((DfaValue)candidate.myArg,(DfaValue)removedFact.myArg) && candidate.myArg instanceof DfaConstValue) {
      otherInequalities.add((DfaConstValue)candidate.myArg);
    }
  }
  return otherInequalities;
}
