{
  if (myConversions.get(expression) != null) {
    return;
  }
  if (obj instanceof TypeConversionDescriptor) {
    ((TypeConversionDescriptor)obj).setRoot(myCurrentRoot);
  }
  myConversions.put(expression,obj);
}
