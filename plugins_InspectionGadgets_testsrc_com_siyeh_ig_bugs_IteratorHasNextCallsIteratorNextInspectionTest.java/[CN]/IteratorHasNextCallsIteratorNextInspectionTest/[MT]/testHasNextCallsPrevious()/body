{
  doTest("import java.util.*;" + "abstract class MyIterator<T> implements ListIterator<T> {" + "    private ListIterator<T> iterator;"+ "    public MyIterator(ListIterator<T> iterator) {"+ "        this.iterator = iterator;"+ "    }"+ "    public boolean /*'Iterator.hasNext()' contains call to 'next()'*/hasNext/**/() {"+ "        return previous() != null;"+ "    }"+ "    public T next() {"+ "        return iterator.next();"+ "    }"+ "    public T previous() {"+ "        return iterator.previous();"+ "    }"+ "}");
}
