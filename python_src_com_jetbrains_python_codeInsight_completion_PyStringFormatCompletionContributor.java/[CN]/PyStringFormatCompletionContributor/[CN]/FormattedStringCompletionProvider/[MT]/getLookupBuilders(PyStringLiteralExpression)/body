{
  final Map<String,PyStringFormatParser.SubstitutionChunk> chunks=PyStringFormatParser.getKeywordSubstitutions(PyStringFormatParser.filterSubstitutions(PyStringFormatParser.parseNewStyleFormat(literalExpression.getStringValue())));
  final List<LookupElement> keys=new ArrayList<LookupElement>();
  for (  String chunk : chunks.keySet()) {
    keys.add(createLookUpElement(chunk));
  }
  return keys;
}
