{
  IElementType tokenType=iterator.getTokenType();
  if (tokenType == null) {
    throw new AssertionError("Null token for iterator " + iterator + " at "+ iterator.getStart()+ " in "+ iterator.getDocument()+ "; atEnd="+ iterator.atEnd());
  }
  return getBraceMatcher(fileType,tokenType);
}
