{
  int a1=random() ? 1 : 0;
  int a2=random() ? 1 : 0;
  int a3=random() ? 1 : 0;
  int a4=random() ? 1 : 0;
  int a5=random() ? 1 : 0;
  int a6=random() ? 1 : 0;
  int a7=random() ? 1 : 0;
  int a8=random() ? 1 : 0;
  int a9=random() ? 1 : 0;
  int a10=random() ? 1 : 0;
  int a11=random() ? 1 : 0;
  int a12=random() ? 1 : 0;
  int a13=random() ? 1 : 0;
  int a14=random() ? 1 : 0;
  int a15=random() ? 1 : 0;
}
