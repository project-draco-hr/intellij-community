{
  int thickness=getThickness();
  Alignment alignment=Alignment.get(c);
  return alignment == Alignment.LEFT || alignment == Alignment.RIGHT ? new Dimension(thickness,thickness * 3) : new Dimension(thickness * 3,thickness);
}
