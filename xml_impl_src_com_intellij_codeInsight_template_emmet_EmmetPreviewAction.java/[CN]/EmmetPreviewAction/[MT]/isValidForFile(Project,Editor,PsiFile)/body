{
  int offset=editor.getCaretModel().getOffset();
  return super.isValidForFile(project,editor,file) && file instanceof XmlFile && ZenCodingTemplate.findApplicableDefaultGenerator(CustomTemplateCallback.getContext(file,offset),false) != null;
}
