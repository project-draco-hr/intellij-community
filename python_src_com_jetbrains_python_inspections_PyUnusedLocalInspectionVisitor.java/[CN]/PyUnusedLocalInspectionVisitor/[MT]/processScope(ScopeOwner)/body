{
  if ((owner.getContainingFile() instanceof PyExpressionCodeFragment || PydevConsoleRunner.isInPydevConsole(owner)) || callsLocals(owner)) {
    return;
  }
  if (!(owner instanceof PyClass)) {
    collectAllWrites(owner);
  }
  collectUsedReads(owner);
}
