{
  values.sort();
  double median=values.get(values.size() / 2) / 1000000;
  return String.format("typing delay, ms: min: %5.1f | max: %5.1f | avg: %5.1f | median: %4.1f",getMin(),getMax(),getMean(),median);
}
