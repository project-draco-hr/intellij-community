{
  Lock result=null;
  if (lock != null) {
    result=new Lock.Builder().setOwner(lock.getOwner()).setComment(lock.getComment()).setCreationDate(lock.getCreationDate()).setExpirationDate(lock.getExpirationDate()).build();
  }
  return result;
}
