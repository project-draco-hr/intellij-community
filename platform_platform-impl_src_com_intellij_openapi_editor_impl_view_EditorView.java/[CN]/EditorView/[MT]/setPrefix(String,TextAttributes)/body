{
  myPrefixLayout=prefixText == null || prefixText.isEmpty() ? null : new LineLayout(this,prefixText,attributes.getFontType(),myFontRenderContext,0);
  myPrefixAttributes=attributes;
  mySizeManager.invalidateCachedWidth();
  invalidateLines(0,0);
}
