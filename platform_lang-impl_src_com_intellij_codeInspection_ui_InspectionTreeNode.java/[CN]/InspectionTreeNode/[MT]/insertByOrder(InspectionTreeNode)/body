{
  if (getIndex(child) != -1) {
    return;
  }
  final int i=TreeUtil.indexedBinarySearch(this,child,InspectionResultsViewComparator.getInstance());
  if (i >= 0) {
    return;
  }
  insert(child,-i - 1);
}
