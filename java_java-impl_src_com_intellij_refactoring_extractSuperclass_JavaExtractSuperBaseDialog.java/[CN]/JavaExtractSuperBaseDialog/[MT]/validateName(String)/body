{
  return PsiNameHelper.getInstance(myProject).isIdentifier(name) ? name.equals(mySourceClass.getName()) ? "Different name expected" : null : RefactoringMessageUtil.getIncorrectIdentifierMessage(name);
}
