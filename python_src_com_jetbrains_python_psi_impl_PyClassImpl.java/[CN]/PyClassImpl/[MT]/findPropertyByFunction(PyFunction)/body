{
  if (myPropertyCache == null) {
    myPropertyCache=initializePropertyCache();
  }
  for (  Property property : myPropertyCache.values()) {
    if (property.getGetter().value() == function || property.getSetter().value() == function || property.getDeleter().value() == function) {
      return property;
    }
  }
  return null;
}
