{
  List<String> slots=getOwnSlots();
  if (slots != null) {
    return slots;
  }
  for (  PyClass cls : getAncestorClasses()) {
    slots=((PyClassImpl)cls).getOwnSlots();
    if (slots != null) {
      return slots;
    }
  }
  return null;
}
