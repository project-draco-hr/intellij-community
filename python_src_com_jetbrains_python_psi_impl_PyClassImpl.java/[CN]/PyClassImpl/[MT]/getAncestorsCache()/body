{
  if (myAncestorsCache != null) {
    return myAncestorsCache;
  }
synchronized (myAncestorsCacheLock) {
    if (myAncestorsCache == null) {
      myAncestorsCache=new TypeEvalContextBasedCache<>(CachedValuesManager.getManager(getProject()),new CachedAncestorsProvider());
    }
    return myAncestorsCache;
  }
}
