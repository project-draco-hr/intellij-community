{
switch (direction) {
case READ:
    return myGetter;
case WRITE:
  return mySetter;
case DELETE:
return myDeleter;
}
throw new IllegalArgumentException("Unknown direction " + PyUtil.nvl(direction));
}
