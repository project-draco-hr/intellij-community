{
  if (PyNames.NONE.equals(ref.getName()))   return null;
  PsiElement something=ref.getReference().resolve();
  if (something instanceof PyFunction) {
    return (PyFunction)something;
  }
  return null;
}
