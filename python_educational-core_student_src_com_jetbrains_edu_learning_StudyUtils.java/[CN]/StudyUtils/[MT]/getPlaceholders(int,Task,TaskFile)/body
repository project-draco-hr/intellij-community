{
  TaskFile prevTaskFile=index == -1 ? taskFile.getTask().getTaskFile(taskFile.name) : task.getAdditionalSteps().get(index).getTaskFiles().get(taskFile.name);
  if (prevTaskFile == null) {
    return Collections.emptyList();
  }
  return prevTaskFile.getAnswerPlaceholders();
}
