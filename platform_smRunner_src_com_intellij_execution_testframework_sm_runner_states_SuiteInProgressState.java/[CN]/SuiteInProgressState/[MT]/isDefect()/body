{
  if (myDefectFound) {
    return true;
  }
  final List<? extends SMTestProxy> children=mySuiteProxy.getChildren();
  for (  SMTestProxy child : children) {
    if (child.isDefect()) {
      myDefectFound=true;
      return true;
    }
  }
  return false;
}
