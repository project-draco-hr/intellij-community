{
  if (myBuilder.getTokenType() == token) {
    myBuilder.advanceLexer();
    return true;
  }
  PsiBuilder.Marker mark=myBuilder.mark();
  myBuilder.advanceLexer();
  mark.error(message);
  return false;
}
