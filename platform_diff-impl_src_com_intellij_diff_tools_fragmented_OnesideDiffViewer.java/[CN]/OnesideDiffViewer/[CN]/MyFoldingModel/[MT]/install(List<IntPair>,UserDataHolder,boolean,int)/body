{
  Iterator<int[]> it=map(changedLines,new Function<IntPair,int[]>(){
    @Override public int[] fun(    IntPair line){
      return new int[]{line.val1,line.val2};
    }
  }
);
  install(it,context,defaultExpanded,range);
}
