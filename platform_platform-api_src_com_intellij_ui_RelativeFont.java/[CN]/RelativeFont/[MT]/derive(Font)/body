{
  if (font != null) {
    if (-1 != myStyle && myStyle != font.getStyle()) {
      return mySize != 0 ? font.deriveFont(myStyle,mySize + font.getSize2D()) : font.deriveFont(myStyle);
    }
    if (mySize != 0) {
      return font.deriveFont(mySize + font.getSize2D());
    }
  }
  return font;
}
