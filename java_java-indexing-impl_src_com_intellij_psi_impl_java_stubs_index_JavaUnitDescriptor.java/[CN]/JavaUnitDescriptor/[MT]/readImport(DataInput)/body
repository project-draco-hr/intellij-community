{
  int[] fullname=readIntArray(in);
  int flags=in.readByte();
  return new IndexTree.Import(fullname,BitUtil.isSet(flags,IS_STATIC),BitUtil.isSet(flags,IS_ON_DEMAND),BitUtil.isSet(flags,HAS_ALIAS) ? in.readInt() : 0);
}
