{
  for (  String s : list) {
    final RunnerAndConfigurationSettings settings=myRunManager.findConfigurationByName(s);
    if (settings != null) {
      result.add(settings);
    }
  }
  return result;
}
