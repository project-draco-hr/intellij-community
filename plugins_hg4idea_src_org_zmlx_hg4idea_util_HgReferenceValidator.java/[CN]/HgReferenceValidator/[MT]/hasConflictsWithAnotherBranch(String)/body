{
  Collection<String> branches=myRepository.getBranches().keySet();
  String currentBranch=myRepository.getCurrentBranch();
  myErrorText=currentBranch.equals(name) || branches.contains(name) ? String.format("A branch with the \'%s\' name already exists",name) : null;
  return myErrorText != null;
}
