{
  final DirectoryProjectGenerator generator=settings.getProjectGenerator();
  return AbstractNewProjectStep.doGenerateProject(project,settings.getProjectLocation(),generator,new Function<VirtualFile,Object>(){
    @Override public Object fun(    VirtualFile file){
      return computeProjectSettings(generator,(ProjectSpecificSettingsStep)settings);
    }
  }
);
}
