{
  try {
    return PersistentUtil.createPersistentStringEnumerator(LOG_KIND,project.getName() + "." + project.getBaseDir().getPath().hashCode(),VERSION);
  }
 catch (  IOException e) {
    throw new RuntimeException("Can not create persistent storage for vcs roots.",e);
  }
}
