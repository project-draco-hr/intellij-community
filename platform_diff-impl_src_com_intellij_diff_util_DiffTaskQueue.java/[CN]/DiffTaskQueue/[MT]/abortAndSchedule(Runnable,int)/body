{
synchronized (LOCK) {
    if (myDisposed)     return;
    abort();
    myAlarm.addRequest(new Runnable(){
      @Override public void run(){
synchronized (LOCK) {
          if (myDisposed)           return;
        }
        task.run();
      }
    }
,millis);
  }
}
