{
  Element infoElement;
  element.getChildren().add(infoElement=new Element(config instanceof SearchConfiguration ? SAVE_TAG_NAME : SAVE_TAG_NAME2));
  config.writeExternal(infoElement);
  return infoElement;
}
