{
  super(controller,permanentGraphInfo);
  long start=System.currentTimeMillis();
  final BekIntMap bekIntMap=controller.getBekIntMap();
  myCompiledGraph=compileGraph(getDelegateLinearGraphController().getCompiledGraph(),new BekGraphLayout(permanentGraphInfo.getPermanentGraphLayout(),bekIntMap),new BekTimestampGetter(timestampGetter,bekIntMap));
  long end=System.currentTimeMillis();
  System.err.println(((double)end - start) / 1000);
}
