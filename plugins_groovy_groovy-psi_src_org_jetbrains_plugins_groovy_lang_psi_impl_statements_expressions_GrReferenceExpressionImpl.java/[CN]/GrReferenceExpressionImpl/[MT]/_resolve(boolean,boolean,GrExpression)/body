{
  final GroovyResolverProcessor processor=GroovyResolverProcessorBuilder.builder().setIncomplete(incomplete).setAllVariants(allVariants).setUpToArgument(upToArgument).build(this);
  GrReferenceResolveRunnerKt.resolveReferenceExpression(this,processor);
  return processor.getCandidatesArray();
}
