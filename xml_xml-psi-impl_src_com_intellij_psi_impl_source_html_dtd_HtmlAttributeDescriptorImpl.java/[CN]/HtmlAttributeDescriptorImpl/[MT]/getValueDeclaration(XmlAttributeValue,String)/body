{
  String s=myCaseSensitive ? value : value.toLowerCase();
  return delegate instanceof BasicXmlAttributeDescriptor ? ((BasicXmlAttributeDescriptor)delegate).getValueDeclaration(attributeValue,s) : super.getValueDeclaration(attributeValue,value);
}
