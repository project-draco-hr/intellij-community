{
  String s=myCaseSensitive ? value : value.toLowerCase();
  return delegate instanceof XmlEnumerationDescriptor ? ((XmlEnumerationDescriptor)delegate).getValueDeclaration(attributeValue,s) : super.getValueDeclaration(attributeValue,value);
}
