{
  final PsiType type=element instanceof PsiLiteralExpression ? ((PsiLiteralExpression)element).getType() : null;
  return type != null && type.equalsToText(JAVA_LANG_STRING) && element.getText().startsWith("\"") && element.getText().endsWith("\"");
}
