{
  checkResultByText(expectedDocumentContents);
  assertEquals("Clipboard contents mismatch",expectedClipboardContents,CopyPasteManager.getInstance().getContents().getTransferData(DataFlavor.stringFlavor).toString());
}
