{
  this.kind=kind;
  this.name=name;
  if (value instanceof Integer && ((Integer)value) == 0 && kind == VAR_KIND.BRACE_STYLE) {
    this.value=1;
  }
 else {
    this.value=value;
  }
  state=STATE.INIT;
  this.serializer=serializer;
}
