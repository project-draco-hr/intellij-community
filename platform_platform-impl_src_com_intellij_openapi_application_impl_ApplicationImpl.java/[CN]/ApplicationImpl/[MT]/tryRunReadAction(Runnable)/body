{
  boolean mustAcquire=!isReadAccessAllowed();
  if (mustAcquire) {
    assertNoPsiLock();
    if (!myLock.tryReadLock())     return false;
  }
  try {
    action.run();
  }
  finally {
    if (mustAcquire) {
      endRead();
    }
  }
  return true;
}
