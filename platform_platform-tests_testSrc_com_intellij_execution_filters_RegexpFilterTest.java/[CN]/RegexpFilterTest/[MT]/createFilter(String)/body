{
  myFilter=new RegexpFilter(null,string){
    @Override protected HyperlinkInfo createOpenFileHyperlink(    String filePath,    int line,    int column){
      return createOpenFile(filePath,line,column);
    }
  }
;
}
