{
  myPersistentEnumerator=PersistentUtil.createPersistentEnumerator(new MyCommitIdKeyDescriptor(project),LOG_KIND,PersistentUtil.calcLogId(project,logProviders),VERSION);
  Disposer.register(project,this);
}
