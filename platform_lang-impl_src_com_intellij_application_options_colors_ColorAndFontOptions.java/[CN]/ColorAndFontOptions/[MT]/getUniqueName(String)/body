{
  String name;
  if (mySchemes.containsKey(preferredName)) {
    for (int i=1; ; i++) {
      name=preferredName + " (" + i+ ")";
      if (!mySchemes.containsKey(name)) {
        break;
      }
    }
  }
 else {
    name=preferredName;
  }
  return name;
}
