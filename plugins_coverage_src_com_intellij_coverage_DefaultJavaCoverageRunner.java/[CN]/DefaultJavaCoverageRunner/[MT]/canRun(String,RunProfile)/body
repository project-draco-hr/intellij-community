{
  return executorId.equals(CoverageExecutor.EXECUTOR_ID) && profile instanceof ModuleBasedConfiguration && !(profile instanceof RunConfigurationWithSuppressedDefaultRunAction)&& CoverageEnabledConfiguration.isApplicableTo((RunConfigurationBase)profile);
}
