{
  if (MakoLanguage._isDisabledFor(node)) {
    return;
  }
  if (node.getExpression() instanceof PyStringLiteralExpression && EpydocUtil.isVariableDocString((PyStringLiteralExpression)node.getExpression())) {
    annotateDocStringStmt((PyStringLiteralExpression)node.getExpression());
  }
}
