{
  String result="";
  if (password == null) {
    return result;
  }
  for (int i=0; i < password.length(); i++) {
    result+=Integer.toHexString(password.charAt(i) ^ 0xdfaa);
  }
  return result;
}
