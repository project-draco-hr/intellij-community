{
  int index=url.indexOf('@');
  if (index != -1) {
    return url.substring(index + 1);
  }
  index=url.indexOf("://");
  if (index != -1) {
    return url.substring(index + 3);
  }
  return url;
}
