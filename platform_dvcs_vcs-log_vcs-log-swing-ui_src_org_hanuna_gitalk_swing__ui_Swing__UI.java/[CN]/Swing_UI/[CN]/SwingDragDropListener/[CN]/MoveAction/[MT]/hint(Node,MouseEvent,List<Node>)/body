{
  List<Ref> refsAbove=getLocalRefsAbove(commit);
  showHint(e,GitLogIcons.MOVE,"Move " + renderCommits(commitsBeingDragged) + (refsAbove.size() == 1 ? " in branch " + refsAbove.get(0).getName() : ""));
}
