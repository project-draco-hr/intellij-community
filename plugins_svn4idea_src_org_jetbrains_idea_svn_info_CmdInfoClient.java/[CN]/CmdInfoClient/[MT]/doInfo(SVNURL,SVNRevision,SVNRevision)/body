{
  CommandExecutor command=execute(myVcs,SvnTarget.fromURL(url),SvnCommandName.info,buildParameters(url.toString(),pegRevision,revision,Depth.EMPTY),null);
  return parseResult(null,command.getOutput());
}
