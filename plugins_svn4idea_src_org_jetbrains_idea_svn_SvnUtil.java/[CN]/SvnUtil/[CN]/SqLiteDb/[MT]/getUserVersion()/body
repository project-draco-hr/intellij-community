{
  int result=0;
  ISqlJetOptions options=dbHandle.getOptions();
  if (options != null) {
    try {
      result=options.getUserVersion();
    }
 catch (    SqlJetException ignore) {
    }
  }
  return result;
}
