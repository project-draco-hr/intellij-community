{
  myThreadToUse=threadToUse;
  myExecutorService=threadToUse == ThreadToUse.POOLED_THREAD ? JobScheduler.getScheduler() : AppExecutorUtil.createBoundedScheduledExecutorService(1);
  if (parentDisposable != null) {
    Disposer.register(parentDisposable,this);
  }
}
