{
synchronized (LOCK) {
    myTask=new Runnable(){
      @Override public void run(){
        try {
          task.run();
        }
 catch (        Exception e) {
          LOG.error("Exception in task " + task,e);
        }
      }
    }
;
    myModalityState=modalityState;
    myDelay=delayMillis;
  }
}
