{
  return previousRevision == null ? null : SvnRepositoryContentRevision.create(myVcs,previousRevision.getFullPath(),VcsUtil.getFilePath(previousRevision.getFile().getIOFile(),isDir),previousRevision.getRevisionNumber().getRevision().getNumber());
}
