{
  return ChainsSearcher.search(searchService,targetQName,contextVarsQNames,maxResultSize,maxChainSize,createNotDeprecatedMethodsResolver(JavaPsiFacade.getInstance(context.getProject()),context.getResolveScope()),context.getExcludedQNames(),context.getContextMethodName());
}
