{
  final int endIndex=StringUtil.endsWithIgnoreCase(text,"l") || StringUtil.endsWithIgnoreCase(text,"j") ? text.length() - 1 : text.length();
  return text.substring(beginIndex,endIndex).replaceAll("_","");
}
