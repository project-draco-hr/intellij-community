{
  final String qualifiedName=containingClass.getQualifiedName();
  return JUnitCommonClassNames.ORG_JUNIT_JUPITER_API_ASSERTIONS.equals(qualifiedName) || checkTestNG && "org.testng.Assert".equals(qualifiedName);
}
