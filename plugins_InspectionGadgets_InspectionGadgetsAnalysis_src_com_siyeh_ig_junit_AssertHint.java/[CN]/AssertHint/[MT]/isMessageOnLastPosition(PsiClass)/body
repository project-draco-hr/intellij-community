{
  final String qualifiedName=containingClass.getQualifiedName();
  return JUnitCommonClassNames.ORG_JUNIT_JUPITER_API_ASSERTIONS.equals(qualifiedName) || "org.testng.Assert".equals(qualifiedName);
}
