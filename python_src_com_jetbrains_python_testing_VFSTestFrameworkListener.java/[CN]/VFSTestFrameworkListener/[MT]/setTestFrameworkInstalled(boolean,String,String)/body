{
  if (name.equals(PyNames.NOSE_TEST))   setNoseTestInstalled(installed,sdkHome);
 else   if (name.equals(PyNames.PY_TEST))   setPyTestInstalled(installed,sdkHome);
 else   if (name.equals(PyNames.AT_TEST))   setAtTestInstalled(installed,sdkHome);
}
