{
  if (name.equals(PyNames.NOSE_TEST))   noseTestInstalled(installed,sdkHome);
 else   if (name.equals(PyNames.PY_TEST))   pyTestInstalled(installed,sdkHome);
 else   if (name.equals(PyNames.AT_TEST))   atTestInstalled(installed,sdkHome);
}
