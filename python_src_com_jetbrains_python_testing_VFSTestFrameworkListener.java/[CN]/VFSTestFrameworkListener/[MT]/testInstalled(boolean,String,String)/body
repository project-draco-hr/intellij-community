{
  if (name.equals("nose"))   noseTestInstalled(installed,sdkHome);
 else   if (name.equals("pytest"))   pyTestInstalled(installed,sdkHome);
 else   if (name.equals("attest"))   atTestInstalled(installed,sdkHome);
}
