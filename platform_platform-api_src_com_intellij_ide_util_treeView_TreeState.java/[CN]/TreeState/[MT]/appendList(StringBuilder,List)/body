{
  if (list.isEmpty()) {
    sb.append("{}");
  }
 else {
    String prefix="{";
    for (    Object object : list) {
      append(sb,prefix,object);
      prefix=", ";
    }
    sb.append("}");
  }
}
