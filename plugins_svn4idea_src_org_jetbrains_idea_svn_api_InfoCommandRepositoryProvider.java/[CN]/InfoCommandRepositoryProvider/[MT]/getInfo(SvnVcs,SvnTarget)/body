{
  SVNInfo result=null;
  try {
    result=target.isFile() ? vcs.getInfo(target.getFile()) : vcs.getInfo(target.getURL(),null);
  }
 catch (  SVNException e) {
    LOG.debug(e);
  }
  return result;
}
