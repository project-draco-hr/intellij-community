{
  final PostfixTemplateAcceptanceContext acceptanceContext=new PostfixTemplateAcceptanceContext(reference,expression,false);
  for (  final TemplateProviderInfo providerInfo : myProviders) {
    providerInfo.provider.createItems(acceptanceContext);
  }
}
