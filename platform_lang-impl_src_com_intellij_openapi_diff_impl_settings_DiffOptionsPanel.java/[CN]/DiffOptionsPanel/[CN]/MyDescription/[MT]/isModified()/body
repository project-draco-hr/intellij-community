{
  TextAttributes textAttributes=getAttributes();
  return !Comparing.equal(myOriginalBackground,textAttributes.getBackgroundColor()) || !Comparing.equal(myOriginalStripe,textAttributes.getErrorStripeColor());
}
