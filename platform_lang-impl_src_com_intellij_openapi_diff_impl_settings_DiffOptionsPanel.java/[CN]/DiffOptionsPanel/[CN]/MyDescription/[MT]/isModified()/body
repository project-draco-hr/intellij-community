{
  TextAttributes textAttributes=getAttributes();
  return !Comparing.equal(myOriginalBackground,textAttributes.getBackgroundColor()) || !Comparing.equal(myOriginalIgnored,textAttributes.getForegroundColor()) || !Comparing.equal(myOriginalStripe,textAttributes.getErrorStripeColor());
}
