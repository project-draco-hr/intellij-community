{
  if (list.isEmpty()) {
    return;
  }
  builder.append('\t').append(name).append(": ");
  boolean isNotFirst=false;
  for (  String path : list) {
    if (isNotFirst) {
      builder.append(',').append(' ');
    }
 else {
      isNotFirst=true;
    }
    builder.append(path);
  }
}
