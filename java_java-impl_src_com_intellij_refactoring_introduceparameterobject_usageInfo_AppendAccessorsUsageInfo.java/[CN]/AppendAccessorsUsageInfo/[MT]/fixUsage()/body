{
  if (myGenerateAccessors && myField != null) {
    myExistingClass.add(myGetter ? GenerateMembersUtil.generateGetterPrototype(myField) : GenerateMembersUtil.generateSetterPrototype(myField));
  }
}
