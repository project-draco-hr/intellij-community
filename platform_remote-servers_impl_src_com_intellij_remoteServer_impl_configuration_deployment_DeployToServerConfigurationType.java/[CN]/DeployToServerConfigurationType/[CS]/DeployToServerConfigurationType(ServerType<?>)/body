{
  super(getId(serverType),serverType.getPresentableName() + " Deployment","Deploy to " + serverType.getPresentableName() + " run configuration",serverType.getIcon());
  addFactory(new DeployToServerConfigurationFactory());
  myServerType=serverType;
}
