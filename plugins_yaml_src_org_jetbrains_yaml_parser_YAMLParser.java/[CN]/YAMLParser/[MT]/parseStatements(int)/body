{
  final boolean sequencesOnTheSameIndent=isSequenceStart(indent);
  while (!eof() && (isJunk() || !eolSeen || myIndent > indent || myIndent == indent && (!sequencesOnTheSameIndent || isSequenceStart(indent)))) {
    parseSingleStatement(indent);
  }
}
