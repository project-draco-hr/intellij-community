{
  new File(stubsRoot).mkdirs();
  final ProcessOutput run_result=SdkUtil.getProcessOutput(new File(binary_path).getParent(),new String[]{binary_path,PythonHelpersLocator.getHelperPath(GENERATOR3),"-d",stubsRoot,"-b","-u"},getVirtualEnvAdditionalEnv(binary_path),RUN_TIMEOUT * 5);
  if (run_result.getExitCode() != 0) {
    LOG.error(run_result.getStderr() + (run_result.isTimeout() ? "\nTimed out" : "\nExit code " + run_result.getExitCode()));
  }
}
