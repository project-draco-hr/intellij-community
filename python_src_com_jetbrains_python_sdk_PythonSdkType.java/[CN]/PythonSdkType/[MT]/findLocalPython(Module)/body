{
  Sdk moduleSDK=findPythonSdk(module);
  if (moduleSDK != null && !isRemote(moduleSDK)) {
    return moduleSDK;
  }
  List<Sdk> allSdks=getAllSdks();
  for (  Sdk sdk : allSdks) {
    if (!isRemote(sdk)) {
      return sdk;
    }
  }
  return null;
}
