{
  String bound=getExpressionBound(expression);
  if (bound == null) {
    return null;
  }
  return CommonUtils.isNumber(expression.getType()) ? Pair.create(bound,"0") : Pair.create(bound + " - 1","0");
}
