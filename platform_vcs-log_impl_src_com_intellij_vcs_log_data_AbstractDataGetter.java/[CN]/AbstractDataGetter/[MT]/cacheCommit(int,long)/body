{
  if (!myCache.isKeyCached(commitId)) {
    myCache.put(commitId,(T)new LoadingDetails(new Computable<CommitId>(){
      @Override public CommitId compute(){
        return myHashMap.getCommitId(commitId);
      }
    }
,taskNumber));
  }
}
