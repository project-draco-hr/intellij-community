{
  StringBuilder s=new StringBuilder();
  for (int i=0; i < commits.size(); i++) {
    s.append(toShortStr(commits.get(i)));
    if (i != commits.size() - 1) {
      s.append("\n");
    }
  }
  return s.toString();
}
