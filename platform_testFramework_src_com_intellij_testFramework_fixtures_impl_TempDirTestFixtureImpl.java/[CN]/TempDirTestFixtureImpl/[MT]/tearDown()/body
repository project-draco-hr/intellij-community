{
  try {
    if (myTempDir != null) {
      new WriteAction(){
        @Override protected void run(        @NotNull Result result) throws IOException {
          findOrCreateDir("").delete(this);
        }
      }
.execute();
    }
  }
  finally {
    super.tearDown();
  }
}
