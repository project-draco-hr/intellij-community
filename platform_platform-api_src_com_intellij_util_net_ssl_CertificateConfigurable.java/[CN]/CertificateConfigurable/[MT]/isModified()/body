{
  CertificatesManager.Config state=CertificatesManager.getInstance().getState();
  return myCheckHostname.isSelected() != state.checkHostname || myCheckValidityPeriod.isSelected() != state.checkValidity || !getCertificates().equals(myTrustManager.getCertificates());
}
