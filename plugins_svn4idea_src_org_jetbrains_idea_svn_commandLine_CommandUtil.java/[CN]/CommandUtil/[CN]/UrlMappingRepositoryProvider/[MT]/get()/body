{
  RootUrlInfo rootInfo=myTarget.isFile() ? myVcs.getSvnFileUrlMapping().getWcRootForFilePath(myTarget.getFile()) : myVcs.getSvnFileUrlMapping().getWcRootForUrl(myTarget.getURL().toDecodedString());
  return rootInfo != null ? new Repository(rootInfo.getRepositoryUrlUrl()) : null;
}
