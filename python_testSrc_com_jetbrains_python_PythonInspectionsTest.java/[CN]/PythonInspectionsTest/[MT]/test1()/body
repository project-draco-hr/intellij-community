{
  myFixture.configureByText("a.py","def test_property_override_assignment():\n" + "    class B(object):\n" + "        @property\n"+ "        def foo(self):\n"+ "            return \"\"\n"+ "\n"+ "    class C(B):\n"+ "        foo = None\n"+ "\n"+ "        def f(self, x):\n"+ "            self.foo = x\n");
  myFixture.enableInspections(PyPropertyAccessInspection.class);
  myFixture.checkHighlighting(true,false,false);
}
