{
  if (!root.isValid()) {
    return true;
  }
  PsiElement module=resolveInRoot(root,myQualifiedName,myPsiManager,myFootholdFile,myCheckForPackage);
  if (module != null) {
    results.add(module);
  }
  return true;
}
