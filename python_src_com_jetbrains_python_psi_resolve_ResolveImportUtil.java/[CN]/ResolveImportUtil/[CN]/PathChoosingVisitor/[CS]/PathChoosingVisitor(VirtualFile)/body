{
  myFname=file.getPath();
  int pos=myFname.lastIndexOf('.');
  if (pos > 0)   myFname=myFname.substring(0,pos);
  pos=myFname.lastIndexOf(PyNames.INIT);
  if (pos > 0)   myFname=myFname.substring(0,pos - 1);
}
