{
  try {
    InputStream stream=ShowCertificateInfoAction.class.getResourceAsStream("keystore");
    try {
      final KeyStore keyStore=KeyStore.getInstance(KeyStore.getDefaultType());
      keyStore.load(stream,"changeit".toCharArray());
      UntrustedCertificateWarningDialog dialog=new UntrustedCertificateWarningDialog((X509Certificate)keyStore.getCertificate("mykey"),"","");
      LOG.debug("Accepted: " + dialog.showAndGet());
    }
  finally {
      StreamUtil.closeStream(stream);
    }
  }
 catch (  Exception logged) {
    LOG.error(logged);
  }
}
