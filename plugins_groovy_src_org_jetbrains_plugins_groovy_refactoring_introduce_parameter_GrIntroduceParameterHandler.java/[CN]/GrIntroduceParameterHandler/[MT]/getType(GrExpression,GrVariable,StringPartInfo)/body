{
  if (expr != null) {
    return expr.getType();
  }
 else   if (var != null) {
    return var.getDeclaredType();
  }
 else   if (info != null) {
    return info.getLiteral().getType();
  }
  return null;
}
