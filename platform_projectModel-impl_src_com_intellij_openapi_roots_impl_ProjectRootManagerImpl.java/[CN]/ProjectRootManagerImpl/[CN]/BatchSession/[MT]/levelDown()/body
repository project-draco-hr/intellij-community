{
  myBatchLevel-=1;
  if (myChanged && myBatchLevel == 0) {
    AccessToken token=WriteAction.start();
    try {
      fireChange();
    }
  finally {
      try {
        myChanged=false;
      }
  finally {
        token.finish();
      }
    }
  }
}
