{
  restoreExpansionStatus((InspectionTreeNode)tree.getModel().getRoot(),tree);
  if (mySelectionPath != null) {
    if (reloadedNode == null || needRestore(reloadedNode))     mySelectionPath.restore(tree);
  }
}
