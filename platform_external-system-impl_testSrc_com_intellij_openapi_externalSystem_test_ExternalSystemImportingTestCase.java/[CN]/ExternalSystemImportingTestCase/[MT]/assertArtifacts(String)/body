{
  final List<String> actualNames=ContainerUtil.map(ArtifactManager.getInstance(myProject).getAllArtifactsIncludingInvalid(),new Function<Artifact,String>(){
    @Override public String fun(    Artifact artifact){
      return artifact.getName();
    }
  }
);
  assertUnorderedElementsAreEqual(actualNames,expectedNames);
}
