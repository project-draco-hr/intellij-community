{
  init();
  myProjectsViews.add(externalProjectsView);
  externalProjectsView.loadState(myState.getExternalSystemsState().get(externalProjectsView.getSystemId().getId()).getProjectsViewState());
  externalProjectsView.init();
}
