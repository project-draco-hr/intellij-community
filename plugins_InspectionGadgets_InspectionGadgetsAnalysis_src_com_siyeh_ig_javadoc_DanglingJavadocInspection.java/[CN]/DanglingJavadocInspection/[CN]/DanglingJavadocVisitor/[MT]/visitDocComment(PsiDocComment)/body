{
  super.visitDocComment(comment);
  if (comment.getOwner() != null) {
    return;
  }
  if (JavaDocUtil.isInsidePackageInfo(comment) && PsiTreeUtil.skipSiblingsForward(comment,PsiWhiteSpace.class) instanceof PsiPackageStatement) {
    return;
  }
  registerError(comment.getFirstChild());
}
