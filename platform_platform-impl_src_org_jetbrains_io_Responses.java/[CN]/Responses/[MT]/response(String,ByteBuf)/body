{
  HttpResponse response=new DefaultFullHttpResponse(HttpVersion.HTTP_1_1,HttpResponseStatus.OK,content == null ? Unpooled.EMPTY_BUFFER : content);
  if (contentType != null) {
    response.headers().add(HttpHeaders.Names.CONTENT_TYPE,contentType);
  }
  return response;
}
