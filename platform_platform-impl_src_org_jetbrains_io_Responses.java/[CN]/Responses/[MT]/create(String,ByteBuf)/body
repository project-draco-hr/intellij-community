{
  HttpResponse response=content == null ? new DefaultHttpResponse(HttpVersion.HTTP_1_1,HttpResponseStatus.OK) : new DefaultFullHttpResponse(HttpVersion.HTTP_1_1,HttpResponseStatus.OK,content);
  if (contentType != null) {
    response.headers().add(CONTENT_TYPE,contentType);
  }
  return response;
}
