{
  assertResult(type,pushedCommits,from,to,updateResult,actualResult.getResults().get(myRepository));
  assertSameElements("Updated files set is incorrect",getUpdatedFiles(actualResult.getUpdatedFiles()),ContainerUtil.notNullize(updatedFiles));
}
