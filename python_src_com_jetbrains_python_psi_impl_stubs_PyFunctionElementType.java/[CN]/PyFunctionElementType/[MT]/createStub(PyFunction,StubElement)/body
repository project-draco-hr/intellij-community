{
  PyFunctionImpl function=(PyFunctionImpl)psi;
  String message=function.extractDeprecationMessage();
  return new PyFunctionStubImpl(psi.getName(),function.extractDocStringReturnType(),message == null ? null : StringRef.fromString(message),parentStub,getStubElementType());
}
