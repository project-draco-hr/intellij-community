{
  int scopeStart=scope.getTextRange().getStartOffset();
  return PsiTreeUtil.findElementOfClassAtRange(scope.getContainingFile(),myStartOffset + scopeStart,myEndOffset + scopeStart,PsiExpression.class);
}
