{
  if (!node.isValid()) {
    PsiElement el=node.getLastChild();
    if (el != null) {
      if (el.getText().equals("\n") && el.getPrevSibling() != null)       el=el.getPrevSibling();
      markError(el,RestBundle.message("ANN.inline.block"));
    }
  }
}
