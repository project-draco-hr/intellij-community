{
  return ContainerUtil.map(errorOutput,new Function<String,String>(){
    @Override public String fun(    String errorMessage){
      return GitUtil.cleanupErrorPrefixes(errorMessage);
    }
  }
);
}
