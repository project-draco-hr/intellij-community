{
  String pattern=Messages.showInputDialog(myPanel,"Specify wildcard","Stepping Filter",null,item.getFilter(),new NonEmptyInputValidator());
  if (pattern != null) {
    mutator.fun(item).setFilter(pattern);
  }
}
