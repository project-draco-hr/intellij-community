{
  String pattern=Messages.showInputDialog(myPanel,"Specify glob pattern","Stepping Filter",null,item.getFilter(),new NonEmptyInputValidator());
  if (pattern != null) {
    mutator.fun(item).setFilter(pattern);
  }
}
