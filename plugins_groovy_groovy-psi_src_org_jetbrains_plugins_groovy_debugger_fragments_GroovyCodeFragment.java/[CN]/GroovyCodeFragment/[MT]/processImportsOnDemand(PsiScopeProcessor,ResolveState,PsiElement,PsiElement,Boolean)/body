{
  if (processStatic == null)   return processImportsOnDemand(processor,state,parent,place,false) && processImportsOnDemand(processor,state,parent,place,true);
  for (  GrImportStatement anImport : myOnDemandImports) {
    if (processStatic != anImport.isStatic())     continue;
    if (!anImport.processDeclarations(processor,state,parent,place)) {
      return false;
    }
  }
  return true;
}
