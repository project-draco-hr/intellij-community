{
  return super.processImports(processor,state,lastParent,place,importStatements,kind,processStatic) && (kind == ImportKind.ON_DEMAND ? processImportsOnDemand(processor,state,lastParent,place,processStatic) : !(kind == ImportKind.ALIAS && !processSingleImports(processor,state,lastParent,place,processStatic)));
}
