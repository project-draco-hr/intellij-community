{
  try {
    return new TemplateResource[]{new TemplateResource("Default equals",readFile(DEFAULT_EQUALS),true),new TemplateResource("Default hashCode",readFile(DEFAULT_HASH_CODE),true)};
  }
 catch (  IOException e) {
    throw new TemplateResourceException("Error loading default templates",e);
  }
}
