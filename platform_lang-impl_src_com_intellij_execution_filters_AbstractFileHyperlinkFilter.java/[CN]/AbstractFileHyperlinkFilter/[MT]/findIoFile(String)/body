{
  File file=new File(filePath);
  if (file.isFile() && file.isAbsolute()) {
    return file;
  }
  if (myBaseDir != null) {
    file=new File(myBaseDir,filePath);
    if (file.isFile()) {
      return file;
    }
  }
  return null;
}
