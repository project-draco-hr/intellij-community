{
  return ContainerUtil.mapNotNull(vector,new Function<Hashtable<String,?>,String>(){
    @Override public String fun(    Hashtable<String,?> table){
      return StringUtil.nullize((String)table.get("name"));
    }
  }
);
}
