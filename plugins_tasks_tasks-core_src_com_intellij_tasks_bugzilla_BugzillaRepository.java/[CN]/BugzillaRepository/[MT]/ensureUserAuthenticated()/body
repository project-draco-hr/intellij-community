{
  ensureVersionDiscovered();
  if (!myUserLoggedIn) {
    Hashtable<String,Object> response=makeRequest("User.login",false,"login",getUsername(),"password",getPassword());
    myUserLoggedIn=true;
    myAuthenticationToken=(String)response.get("token");
  }
}
