{
  if (ourClient != null) {
    return ourClient;
  }
  ourClient=initializeClient(stepicUser);
  if (ourClient == null) {
    final StepicUser user=login(stepicUser);
    if (user != null) {
      ourClient=initializeClient(stepicUser);
    }
  }
  return ourClient;
}
