{
  if (ourClient != null) {
    return ourClient;
  }
  ourClient=initializeClient(project);
  if (ourClient == null) {
    if (login(project)) {
      ourClient=initializeClient(project);
    }
  }
  return ourClient;
}
