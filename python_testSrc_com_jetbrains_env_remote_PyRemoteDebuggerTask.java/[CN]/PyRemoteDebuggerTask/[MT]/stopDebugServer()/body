{
  if (myDebugProcess != null) {
    ProcessHandler h=myDebugProcess.getProcessHandler();
    h.destroyProcess();
    h.detachProcess();
    if (!h.isStartNotified()) {
      h.startNotify();
    }
  }
}
