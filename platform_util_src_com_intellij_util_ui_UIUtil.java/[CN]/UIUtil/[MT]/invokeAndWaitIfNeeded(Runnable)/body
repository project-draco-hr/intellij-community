{
  if (ourEdtInvocationManager.isEventDispatchThread()) {
    runnable.run();
  }
 else {
    try {
      ourEdtInvocationManager.invokeAndWait(runnable);
    }
 catch (    Exception e) {
      LOG.error(e);
    }
  }
}
