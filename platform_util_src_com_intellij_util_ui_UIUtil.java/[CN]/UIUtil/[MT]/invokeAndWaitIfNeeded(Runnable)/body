{
  if (ourInvocationManager.isEventDispatchThread()) {
    runnable.run();
  }
 else {
    try {
      ourInvocationManager.invokeAndWait(runnable);
    }
 catch (    Exception e) {
      LOG.error(e);
    }
  }
}
