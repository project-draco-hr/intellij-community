{
  JBIterable<Component> all=recursively ? uiTraverser(component).expandAndFilter(visibleOnly ? new Condition<Component>(){
    @Override public boolean value(    Component c){
      return c.isVisible();
    }
  }
 : Conditions.<Component>alwaysTrue()).traverse() : JBIterable.of(component);
  Color fg=enabled ? getLabelForeground() : getLabelDisabledForeground();
  for (  Component c : all) {
    c.setEnabled(enabled);
    if (fg != null && c instanceof JLabel) {
      c.setForeground(fg);
    }
  }
}
