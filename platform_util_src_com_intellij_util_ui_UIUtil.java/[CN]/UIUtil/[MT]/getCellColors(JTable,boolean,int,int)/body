{
  return Couple.of(isSel ? table.getSelectionForeground() : table.getForeground(),isSel ? table.getSelectionBackground() : isUnderNimbusLookAndFeel() && row % 2 == 1 ? TRANSPARENT_COLOR : table.getBackground());
}
