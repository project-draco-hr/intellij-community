{
  try {
    Maven3ServerIndexerImpl result=new Maven3ServerIndexerImpl(new Maven32ServerEmbedderImpl(new MavenServerSettings())){
      @Override public Maven3ServerEmbedder createEmbedder(      MavenServerSettings settings) throws RemoteException {
        return new Maven32ServerEmbedderImpl(settings);
      }
    }
;
    UnicastRemoteObject.exportObject(result,0);
    return result;
  }
 catch (  RemoteException e) {
    throw rethrowException(e);
  }
}
