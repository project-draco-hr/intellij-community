{
  if (text.isEmpty()) {
    processOneLine(text,console);
  }
 else {
    if (StringUtil.countNewLines(text.trim()) > 0) {
      executeMultiLine(text,console);
    }
 else {
      processOneLine(text,console);
    }
  }
}
