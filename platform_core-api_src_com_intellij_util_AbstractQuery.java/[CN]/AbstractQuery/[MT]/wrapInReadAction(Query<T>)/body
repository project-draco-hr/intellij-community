{
  return new AbstractQuery<T>(){
    @Override protected boolean processResults(    @NotNull Processor<T> consumer){
      return query.forEach(ReadActionProcessor.wrapInReadAction(consumer));
    }
  }
;
}
