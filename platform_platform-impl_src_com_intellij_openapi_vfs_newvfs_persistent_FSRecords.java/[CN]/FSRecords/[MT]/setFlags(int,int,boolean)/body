{
  w.lock();
  try {
    if (markAsChange) {
      incModCount(id);
    }
    putRecordInt(id,FLAGS_OFFSET,flags);
  }
 catch (  Throwable e) {
    DbConnection.handleError(e);
  }
 finally {
    w.unlock();
  }
}
