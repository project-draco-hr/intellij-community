{
  w.lock();
  try {
    incModCount(id);
    getRecords().putLong(getOffset(id,TIMESTAMP_OFFSET),value);
  }
 catch (  Throwable e) {
    DbConnection.handleError(e);
  }
 finally {
    w.unlock();
  }
}
