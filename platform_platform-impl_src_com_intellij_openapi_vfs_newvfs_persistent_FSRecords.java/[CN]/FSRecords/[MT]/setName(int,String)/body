{
  w.lock();
  try {
    incModCount(id);
    int nameId=getNames().enumerate(name);
    putRecordInt(id,NAME_OFFSET,nameId);
  }
 catch (  Throwable e) {
    DbConnection.handleError(e);
  }
 finally {
    w.unlock();
  }
}
