{
  final File[] dirs=root.listFiles();
  if (dirs == null)   return;
  for (  File dir : dirs) {
    if (pattern.matcher(dir.getName()).matches() && dir.isFile()) {
      files.add(dir);
    }
  }
}
