{
  if (element != null) {
    String text=getTextByElement(element);
    for (    final String value : myValue) {
      if (value == null || value.equals(text)) {
        return true;
      }
    }
  }
  return false;
}
