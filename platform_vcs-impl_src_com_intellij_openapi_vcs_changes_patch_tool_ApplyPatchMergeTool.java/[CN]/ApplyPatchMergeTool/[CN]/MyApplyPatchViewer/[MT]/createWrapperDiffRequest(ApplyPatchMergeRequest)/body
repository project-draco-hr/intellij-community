{
  VirtualFile file=FileDocumentManager.getInstance().getFile(request.getDocument());
  return new ApplyPatchDiffRequest(request.getPatch(),request.getLocalContent(),file,request.getTitle(),request.getLocalTitle(),request.getResultTitle(),request.getPatchTitle());
}
