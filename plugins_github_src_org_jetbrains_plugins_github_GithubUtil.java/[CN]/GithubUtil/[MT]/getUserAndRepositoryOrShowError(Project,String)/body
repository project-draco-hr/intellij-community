{
  int index;
  if (url.startsWith(getHttpsUrl())) {
    index=url.lastIndexOf('/');
    if (index == -1) {
      GithubNotifications.showError(project,"Cannot extract info about repository: " + url,GithubOpenInBrowserAction.CANNOT_OPEN_IN_BROWSER);
      return null;
    }
    index=url.substring(0,index).lastIndexOf('/');
    if (index == -1) {
      GithubNotifications.showError(project,"Cannot extract info about repository: " + url,GithubOpenInBrowserAction.CANNOT_OPEN_IN_BROWSER);
      return null;
    }
  }
 else {
    index=url.lastIndexOf(':');
    if (index == -1) {
      GithubNotifications.showError(project,"Cannot extract info about repository: " + url,GithubOpenInBrowserAction.CANNOT_OPEN_IN_BROWSER);
      return null;
    }
  }
  String repoInfo=url.substring(index + 1);
  if (repoInfo.endsWith(".git")) {
    repoInfo=repoInfo.substring(0,repoInfo.length() - 4);
  }
  return repoInfo;
}
