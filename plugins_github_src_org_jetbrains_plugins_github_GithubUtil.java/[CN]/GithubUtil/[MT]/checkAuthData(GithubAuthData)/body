{
  if (StringUtil.isEmptyOrSpaces(auth.getHost()) || StringUtil.isEmptyOrSpaces(auth.getLogin()) || StringUtil.isEmptyOrSpaces(auth.getPassword())) {
    return false;
  }
  try {
    return testConnection(auth);
  }
 catch (  AuthenticationException e) {
    return false;
  }
}
