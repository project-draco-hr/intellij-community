{
  GithubAuthData auth=getAuthData();
  boolean valid=false;
  try {
    valid=checkAuthData(auth);
  }
 catch (  IOException e) {
  }
  if (!valid) {
    return getValidAuthData(project,indicator);
  }
 else {
    return auth;
  }
}
