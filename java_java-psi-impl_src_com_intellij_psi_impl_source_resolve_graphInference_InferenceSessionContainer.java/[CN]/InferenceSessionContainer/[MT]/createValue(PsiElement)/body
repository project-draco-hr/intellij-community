{
  if (MethodCandidateInfo.isOverloadCheck()) {
    return startTopLevelInference(parent);
  }
  return CachedValuesManager.getCachedValue(parent,new CachedValueProvider<CompoundInitialState>(){
    @Nullable @Override public Result<CompoundInitialState> compute(){
      return new Result<CompoundInitialState>(startTopLevelInference(parent),PsiModificationTracker.MODIFICATION_COUNT);
    }
  }
);
}
