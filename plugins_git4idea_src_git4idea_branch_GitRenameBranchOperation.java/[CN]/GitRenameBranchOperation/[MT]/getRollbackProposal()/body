{
  return "However rename has succeeded for the following " + repositories() + ":<br/>"+ successfulRepositoriesJoined()+ "<br/>You may rollback (rename branch back to "+ myCurrentName+ ") not to let branches diverge.";
}
