{
  final PsiElement parent=comment.getParent();
  if (parent == null || !(parent instanceof PsiTryStatement)) {
    return false;
  }
  return PsiTreeUtil.getPrevSiblingOfType(comment,PsiCodeBlock.class) != null || PsiTreeUtil.getPrevSiblingOfType(comment,PsiCatchSection.class) != null;
}
