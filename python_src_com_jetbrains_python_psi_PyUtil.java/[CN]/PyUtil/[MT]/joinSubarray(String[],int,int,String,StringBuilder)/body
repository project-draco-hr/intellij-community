{
  boolean made_step=false;
  for (int i=from; i <= upto; i+=1) {
    if (i >= what.length)     break;
    if (made_step) {
      target.append(separator);
    }
 else     made_step=true;
    target.append(what[i]);
  }
  return target;
}
