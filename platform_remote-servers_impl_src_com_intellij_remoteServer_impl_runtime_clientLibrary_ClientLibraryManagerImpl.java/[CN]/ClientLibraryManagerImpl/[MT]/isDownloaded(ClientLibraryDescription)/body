{
  List<File> files=myFiles.get(description.getId());
  if (files == null || files.isEmpty()) {
    return false;
  }
  for (  File file : files) {
    if (!file.exists()) {
      return false;
    }
  }
  return true;
}
