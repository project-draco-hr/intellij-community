{
  if (domain.domain().equals("CSS")) {
    return false;
  }
  return domain.hidden() || domain.domain().equals("DOMDebugger") || domain.domain().equals("Timeline")|| domain.domain().equals("Input");
}
