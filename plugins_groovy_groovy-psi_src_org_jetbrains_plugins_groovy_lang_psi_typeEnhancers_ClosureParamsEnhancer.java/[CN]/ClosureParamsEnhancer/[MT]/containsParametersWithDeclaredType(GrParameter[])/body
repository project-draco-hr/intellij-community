{
  return ContainerUtil.find(parameters,new Condition<GrParameter>(){
    @Override public boolean value(    GrParameter parameter){
      return parameter.getDeclaredType() != null;
    }
  }
) != null;
}
