{
  return new RepeatSvnActionThroughBusy(){
    @Override protected void executeImpl() throws SVNException {
      try {
        vcs.getFactory(file).createDeleteClient().delete(file,force,false);
      }
 catch (      VcsException e) {
        wrapAndThrow(e);
      }
    }
  }
;
}
