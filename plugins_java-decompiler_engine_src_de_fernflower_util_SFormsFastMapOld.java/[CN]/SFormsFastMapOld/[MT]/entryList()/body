{
  List<Entry<Integer,E>> list=new ArrayList<Entry<Integer,E>>();
  for (int i=2; i >= 0; i--) {
    for (    Entry<Integer,E> ent : lstElements.get(i)) {
      if (ent != null) {
        list.add(ent);
      }
    }
  }
  return list;
}
