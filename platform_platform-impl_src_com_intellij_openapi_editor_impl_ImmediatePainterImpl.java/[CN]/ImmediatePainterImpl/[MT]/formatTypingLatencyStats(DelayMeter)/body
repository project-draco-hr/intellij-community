{
  return String.format("Zero-latency: %3s; typing delay, ms: min: %5.1f | max: %5.1f | avg: %5.1f | sigma: %4.1f",isZeroLatencyTypingEnabled() ? "on" : "off",meter.getMin(),meter.getMax(),meter.getMean(),meter.getStandardDeviation());
}
