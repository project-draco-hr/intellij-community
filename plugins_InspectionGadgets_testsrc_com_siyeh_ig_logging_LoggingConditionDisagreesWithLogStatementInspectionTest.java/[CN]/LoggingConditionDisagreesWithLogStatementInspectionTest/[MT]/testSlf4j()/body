{
  doTest("import org.slf4j.Logger;" + "import org.slf4j.LoggerFactory;" + "class X {"+ "  private static final Logger LOG = LoggerFactory.getLogger(X.class);"+ "  void n() {"+ "    if (LOG.isInfoEnabled()) {"+ "      LOG.info(\"nothing to report\");"+ "    }"+ "    if (LOG./*Log condition 'isInfoEnabled()' does not match 'warn()' logging call*/isInfoEnabled/**/()) {"+ "      LOG.warn(\"the sky is falling!\");"+ "    }"+ "  }"+ "}");
}
