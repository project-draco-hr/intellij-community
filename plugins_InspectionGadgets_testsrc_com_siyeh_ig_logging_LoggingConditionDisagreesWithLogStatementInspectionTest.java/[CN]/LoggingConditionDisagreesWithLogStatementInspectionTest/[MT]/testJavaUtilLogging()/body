{
  doTest("import java.util.logging.*;" + "class Loggers {" + "  static final Logger LOG = Logger.getLogger(\"\");"+ "  public void method() {"+ "    if (LOG./*Log condition 'isLoggable()' does not match 'warning()' logging call*/isLoggable/**/(Level.FINE)) {"+ "      LOG.warning(\"asdfasdf\");"+ "    }"+ "    if (LOG.isLoggable(Level.WARNING)) {"+ "      LOG.warning(\"oops!\");"+ "    }"+ "  }"+ "}");
}
