{
  GithubRepo parent=this.parent == null ? null : this.parent.createRepo();
  return new GithubRepoDetailed(name,fullName,description,isPrivate,isFork,htmlUrl,cloneUrl,defaultBranch,owner.createUser(),parent);
}
