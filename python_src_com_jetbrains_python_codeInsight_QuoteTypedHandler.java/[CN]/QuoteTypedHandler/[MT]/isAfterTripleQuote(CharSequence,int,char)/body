{
  if (offset >= 3) {
    return (isNotEscaped(chars,offset - 4) && chars.charAt(offset - 1) == character && chars.charAt(offset - 2) == character && chars.charAt(offset - 3) == character);
  }
  return false;
}
