{
  int pos=offset;
  int escape_count=0;
  pos-=1;
  while (pos >= 0) {
    if (text.charAt(pos) == '\\') {
      escape_count+=1;
      pos-=1;
    }
 else     break;
  }
  return (escape_count % 2 == 0);
}
