{
  char[] chars=CharArrayUtil.fromSequence(actual);
  for (  char c : chars) {
    if (c == '\r') {
      return LineSeparator.CRLF;
    }
 else     if (c == '\n') {
      return LineSeparator.LF;
    }
  }
  return LineSeparator.LF;
}
