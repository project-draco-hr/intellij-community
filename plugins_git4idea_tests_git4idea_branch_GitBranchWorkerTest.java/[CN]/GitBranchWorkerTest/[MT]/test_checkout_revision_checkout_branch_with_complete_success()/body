{
  branchWithCommit(myRepositories,"feature");
  checkoutRevision("feature",new TestUiHandler());
  assertDetachedState("feature");
  assertEquals("Notification about successful branch checkout is incorrect","Checked out " + bcode("feature"),myVcsNotifier.getLastNotification().getContent());
}
