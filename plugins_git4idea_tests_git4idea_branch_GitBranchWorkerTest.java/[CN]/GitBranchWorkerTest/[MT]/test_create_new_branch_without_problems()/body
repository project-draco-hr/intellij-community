{
  checkoutNewBranch("feature",new TestUiHandler());
  assertCurrentBranch("feature");
  assertEquals("Notification about successful branch creation is incorrect","Branch " + bcode("feature") + " was created",myVcsNotifier.getLastNotification().getContent());
}
