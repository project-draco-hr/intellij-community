{
  branchWithCommit(myRepositories,"feature");
  checkoutRevision("feature~1",new TestUiHandler());
  assertDetachedState("master");
  assertEquals("Notification about successful branch checkout is incorrect","Checked out " + bcode("feature~1"),myVcsNotifier.getLastNotification().getContent());
}
