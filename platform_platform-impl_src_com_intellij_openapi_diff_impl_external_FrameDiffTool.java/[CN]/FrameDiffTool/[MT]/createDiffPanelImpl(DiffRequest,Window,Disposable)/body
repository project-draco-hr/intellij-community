{
  DiffPanelImpl panel=(DiffPanelImpl)DiffManagerImpl.createDiffPanel(request,window,parentDisposable,this);
  if (request.getHints().contains(DiffTool.HINT_DIFF_IS_APPROXIMATE)) {
    panel.setPatchAppliedApproximately();
  }
  return panel;
}
