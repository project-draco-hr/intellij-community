{
  TextRange range=node.getTextRange();
  myGlobalVisitor.addToken(new AnonymToken(PARAM_LIST_TYPE,myGlobalVisitor.getBaseOffset() + range.getStartOffset(),myGlobalVisitor.getBaseOffset() + range.getEndOffset()));
}
