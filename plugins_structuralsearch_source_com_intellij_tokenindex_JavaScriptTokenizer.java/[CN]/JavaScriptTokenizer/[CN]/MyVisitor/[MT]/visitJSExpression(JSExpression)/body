{
  TextRange range=expression.getTextRange();
  myGlobalVisitor.getTokens().add(new AnonymToken(EXPRESSION_TYPE,myGlobalVisitor.getBaseOffset() + range.getStartOffset(),myGlobalVisitor.getBaseOffset() + range.getEndOffset()));
}
