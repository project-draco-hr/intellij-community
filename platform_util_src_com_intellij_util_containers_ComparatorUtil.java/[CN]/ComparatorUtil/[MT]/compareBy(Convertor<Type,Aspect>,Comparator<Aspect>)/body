{
  return new Comparator<Type>(){
    @Override public int compare(    Type element1,    Type element2){
      return comparator.compare(aspect.convert(element1),aspect.convert(element2));
    }
  }
;
}
