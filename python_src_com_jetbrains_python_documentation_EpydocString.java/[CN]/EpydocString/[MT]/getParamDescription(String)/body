{
  String value=getTagValue("param",paramName);
  if (value == null) {
    value=getTagValue("param","*" + paramName);
  }
  if (value == null) {
    value=getTagValue("param","**" + paramName);
  }
  return value;
}
