{
  int lastIndex=0;
  for (  CommitParents commit : commits) {
    if (searchHashes.size() == 0) {
      return lastIndex;
    }
    searchHashes.remove(commit.getHash());
    lastIndex++;
  }
  return -1;
}
