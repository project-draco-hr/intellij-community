{
  List<TrelloBoard> boards=myRepository.fetchUserBoards();
  assertEquals(3,boards.size());
  assertObjectsNamed("All boards of the user should be included",boards,BASIC_FUNCTIONALITY_BOARD_NAME,LABELS_AND_COLORS_BOARD_NAME,STATE_UPDATES_BOARD_NAME);
}
