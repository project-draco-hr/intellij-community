{
  PropertyData property=myVcs.getFactory(path).createPropertyClient().getProperty(SvnTarget.fromFile(path),COMMIT_MESSAGE,true,SVNRevision.BASE);
  return property != null ? PropertyValue.toString(property.getValue()) : null;
}
