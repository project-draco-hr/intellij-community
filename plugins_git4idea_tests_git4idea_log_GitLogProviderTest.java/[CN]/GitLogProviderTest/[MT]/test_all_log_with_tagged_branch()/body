{
  prepareSomeHistory();
  createTaggedBranch();
  List<VcsCommitMetadata> expectedLog=log();
  @SuppressWarnings("unchecked") List<TimedVcsCommit> actualLog=myLogProvider.readAllHashes(myProjectRoot,Consumer.EMPTY_CONSUMER);
  assertOrderedEquals(expectedLog,actualLog);
}
