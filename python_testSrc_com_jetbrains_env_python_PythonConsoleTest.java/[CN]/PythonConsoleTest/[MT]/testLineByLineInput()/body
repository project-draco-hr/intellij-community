{
  runPythonTest(new PyConsoleTask(){
    @Override public void testing() throws Exception {
      exec("x = 96");
      exec("x +=1");
      exec("if True:\n" + "  print(x)\n");
      waitForOutput("97");
    }
  }
);
}
