{
  return ContainerUtil.mapNotNull(getTopLevelSymbols(),new Function<PyElement,TopLevelSymbolInfo>(){
    @Override public TopLevelSymbolInfo fun(    PyElement element){
      return new TopLevelSymbolInfo(element);
    }
  }
);
}
