{
  return ContainerUtil.map(myMemberSelectionTable.getSelectedMemberInfos(),new Function<TopLevelSymbolInfo,PyElement>(){
    @Override public PyElement fun(    TopLevelSymbolInfo info){
      return info.getMember();
    }
  }
);
}
