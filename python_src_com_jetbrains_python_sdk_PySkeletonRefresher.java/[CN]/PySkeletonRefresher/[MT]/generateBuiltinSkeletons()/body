{
  new File(mySkeletonsPath).mkdirs();
  String binaryPath=mySdk.getHomePath();
  long startTime=System.currentTimeMillis();
  final ProcessOutput run_result=PySdkUtil.getProcessOutput(new File(binaryPath).getParent(),new String[]{binaryPath,PythonHelpersLocator.getHelperPath(GENERATOR3),"-d",mySkeletonsPath,"-b"},PythonSdkType.getVirtualEnvAdditionalEnv(binaryPath),MINUTE * 5);
  run_result.checkSuccess(LOG);
  LOG.info("Rebuilding builtin skeletons took " + (System.currentTimeMillis() - startTime) + " ms");
}
