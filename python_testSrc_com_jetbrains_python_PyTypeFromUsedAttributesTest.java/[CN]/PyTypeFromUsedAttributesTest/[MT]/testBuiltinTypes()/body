{
  doTestType("def func(x):\n" + "    x.upper()\n" + "    x.decode()","bytearray | str | unicode | unknown");
  doTestType("def func(x):\n" + "    x.pop() and x.update()","dict | set | MutableMapping | unknown");
}
