{
  String type=artifact.getExtension();
  if ("jar".equals(type) && "tests".equals(artifact.getClassifier())) {
    type="test-jar";
  }
  return MavenModuleMap.getInstance().findArtifact(artifact.getGroupId(),artifact.getArtifactId(),type,artifact.getBaseVersion());
}
