{
  mySize=size;
  myStyle=style;
  Font font=new Font(familyName,style,size);
  myFont=ENABLE_OPTIONAL_LIGATURES ? font.deriveFont(Collections.singletonMap(TextAttribute.LIGATURES,TextAttribute.LIGATURES_ON)) : font;
}
