{
  if (isApplying) {
    return item;
  }
  T mutable=modifiedItems.get(item);
  if (mutable == null) {
    mutable=mutableFactory.fun(item);
    modifiedItems.put(item,mutable);
  }
  return mutable;
}
