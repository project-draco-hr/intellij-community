{
  myEditors=ContainerUtil.map(editors,new Function<BinaryEditorHolder,FileEditor>(){
    @Override public FileEditor fun(    BinaryEditorHolder holder){
      return holder.getEditor();
    }
  }
);
}
