{
  Ref<Collection<Url>> ref=Ref.create();
  @SuppressWarnings("deprecation") boolean canHandle=canHandle(element,psiFile,ref);
  if (!ref.isNull()) {
    result.set(ContainerUtil.newHashSet(ref.get()));
  }
  return canHandle;
}
