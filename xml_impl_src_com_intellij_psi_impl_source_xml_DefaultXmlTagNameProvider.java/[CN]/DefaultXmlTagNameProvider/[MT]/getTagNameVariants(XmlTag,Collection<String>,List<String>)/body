{
  final List<String> variants=TagNameVariantCollector.getTagNameVariants(element,namespaces,nsInfo,new Function<XmlElementDescriptor,String>(){
    @Override public String fun(    XmlElementDescriptor descriptor){
      return descriptor.getName(element);
    }
  }
);
  return ArrayUtil.toStringArray(variants);
}
