{
  Status status=changedFile.getStatus();
  FilePath path=changedFile.getFilePath();
  return SvnContentRevision.createBaseRevision(myVcs,forDeleted ? FilePathImpl.createForDeletedFile(status.getFile(),path.isDirectory()) : path,status.getRevision());
}
