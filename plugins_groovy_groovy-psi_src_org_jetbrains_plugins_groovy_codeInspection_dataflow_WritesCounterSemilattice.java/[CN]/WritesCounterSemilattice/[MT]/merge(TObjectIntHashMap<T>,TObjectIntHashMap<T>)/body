{
  from.forEachEntry(new TObjectIntProcedure<T>(){
    @Override public boolean execute(    T key,    int value){
      to.put(key,to.containsKey(key) ? Math.max(to.get(key),value) : value);
      return true;
    }
  }
);
}
